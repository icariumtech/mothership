---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 07
type: execute
wave: 5
depends_on: [24-04, 24-05]
files_modified:
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "janus-add-ship instructs the AI to write ships/<slug>/deckplan.yaml with a `decks:` scaffold (NOT the legacy ships/<slug>/map/main.yaml with `rooms: []`)"
    - "janus-add-ship objective and process consistently reference the canonical deckplan.yaml path — no remaining mention of map/main.yaml"
    - "janus-add-npc id derivation strips non-[a-z0-9_] characters so `Dr. Elena-Kim` yields `dr_elena_kim` (not `dr._elena_kim` or `dr__elena_kim`)"
    - "janus-add-location moon type branch writes to galaxy/<system>/<planet>/<moon-slug>/location.yaml (one level deeper than planet/station); type=ship redirects to /janus-add-ship and aborts"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md"
      provides: "D-17 ship registration — corrected deckplan format (CR-03)"
      contains: "deckplan.yaml"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md"
      provides: "D-12 NPC creator — id formula strips punctuation (WR-01)"
      contains: "re.sub"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md"
      provides: "D-13 location creator — moon depth + ship redirect (WR-03)"
      contains: "moon"
  key_links:
    - from: "janus-add-ship SKILL.md step 13"
      to: "schema-encounters.md deckplan path convention"
      via: "literal path `ships/<ship-slug>/deckplan.yaml`"
      pattern: "ships/<ship-slug>/deckplan\\.yaml"
    - from: "janus-add-ship SKILL.md step 13 stub content"
      to: "schema-encounters.md top-level `decks:` list"
      via: "YAML scaffold with at least one deck entry containing rooms: []"
      pattern: "decks:"
    - from: "janus-add-npc SKILL.md step 3"
      to: "Pitfall P2 (id == filename stem)"
      via: "re.sub strips non-alphanumeric_underscore characters before write"
      pattern: "re\\.sub.*\\[\\^a-z0-9_\\]"
    - from: "janus-add-location SKILL.md step 2"
      to: "/janus-add-ship redirect for type=ship"
      via: "explicit guard in process step"
      pattern: "janus-add-ship"
---

<objective>
Fix three SKILL.md correctness defects from Phase 24 verification + review. All three are AI-instruction defects (no code execution to fix) — but each one causes silent runtime failure if an AI follows the skill literally.

1. **CR-03 (BLOCKER):** `janus-add-ship` step 13 (lines 83-85) and objective (line 20) reference the legacy path `ships/<slug>/map/main.yaml` with `rooms: []`. `schema-encounters.md` defines the canonical format as `ships/<slug>/deckplan.yaml` with a `decks:` list. Ships created with the current skill will not load in `EncounterMapDisplay`.

2. **WR-01 (WARNING, fix opportunistically):** `janus-add-npc` step 3 (line 33) documents the formula `name.lower().replace(" ", "_").replace("-", "_")` and shows the example `Dr. Elena-Kim → dr__elena_kim`. The formula leaves the period in place, producing `dr._elena_kim`, AND the documented double-underscore example is inconsistent with the formula. Pitfall P2 requires `id == filename stem`; periods + double underscores both look broken.

3. **WR-03 (WARNING, fix opportunistically):** `janus-add-location` accepts `type=moon` but never branches on it — the write path (step 9) is hardcoded to `galaxy/<system>/<slug>/location.yaml` regardless of type. Moons must be one level deeper: `galaxy/<system>/<planet>/<moon-slug>/location.yaml`. The skill also silently accepts `type=ship`, which would create a misplaced entry under `galaxy/` instead of `ships/`.

Purpose: Ensure each affected skill produces correct on-disk artifacts when an AI executes it end-to-end. Output: Three modified SKILL.md files and one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-VERIFICATION.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-REVIEW.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-04-SUMMARY.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-05-SUMMARY.md
@/home/gjohnson/mothership/janus-skills/resources/schema-encounters.md

<interfaces>
Canonical deckplan path (from schema-encounters.md):

| Location type    | MCP path                                              |
|------------------|-------------------------------------------------------|
| Mobile ship      | `ships/<ship-slug>/deckplan.yaml`                     |
| Galaxy location  | `galaxy/<system>/<body>/<slug>/deckplan.yaml`         |
| Player ship      | `campaign/ship/deckplan.yaml`                         |

The schema states explicitly: "Legacy format (old locations): galaxy/<system>/<body>/map/manifest.yaml
+ individual deck files. New format: single deckplan.yaml with a decks: list.
Skills should write new format only."

Canonical deckplan scaffold (minimal, ready-to-edit — must match schema):
```yaml
decks:
  - id: main_deck
    name: Main Deck
    level: 1
    default: true
    unit_size: 30
    rooms: []
```

Current janus-add-ship state (defective):
- Line 20 (objective):
    "An optional deckplan stub may also be created at
    `ships/<slug>/map/main.yaml`."
- Lines 83-85 (step 13):
    "13. Ask the user if they want an empty deckplan stub. If yes, build a minimal
        `data/ships/<ship-slug>/map/main.yaml` with an empty `rooms: []` list and call
        `write_file("ships/<ship-slug>/map/main.yaml", stub_content)`."
- Line 88 (confirmation, step 14):
    "- Written path: `ships/<ship-slug>/location.yaml` (and map stub if created)"

WR-01 — janus-add-npc id derivation (current — defective):
- Line 31-32:
    "3. Derive `id`: convert name to lowercase and replace spaces and hyphens with underscores.
       Formula: `name.lower().replace(" ", "_").replace("-", "_")`."
- Line 32:
    "Examples: \"Captain Harrow\" → `captain_harrow`, \"Dr. Elena-Kim\" → `dr__elena_kim`."
- Pitfall P2 reminder is already present on line 35 — leave that intact.

WR-01 fix (canonical, from 24-REVIEW.md):
Formula becomes:
  `re.sub(r'[^a-z0-9_]', '', name.lower().replace(' ', '_').replace('-', '_'))`
Example update: `"Dr. Elena-Kim" → dr_elena_kim` (single underscore, period stripped).
Add a clarifying note: "any character outside [a-z0-9_] is stripped — this prevents periods,
apostrophes, and accents from leaking into the filename stem (Pitfall P2)."

WR-03 — janus-add-location moon depth (current — defective):
- Line 30-32 (step 2):
    "2. Ask the user (or infer from args) for `type` — must be one of:
       `station | planet | moon | system`."
  No special handling for moon. No mention of ship.
- Line 62-63 (step 9):
    "9. Write path: `galaxy/<parent-system>/<location-slug>/location.yaml`.
       Call `write_file(\"galaxy/<parent-system>/<location-slug>/location.yaml\", content)`."

WR-03 fix (from 24-REVIEW.md):
- After step 2's type validation, add two branches:
  * If type=="moon": prompt for a `parent_planet_slug` (must be a direct child of <parent-system>;
    verify via list_files("galaxy/<parent-system>") and refuse if missing).
  * If type=="ship": print an error redirecting user to `/janus-add-ship` and abort the skill.
- In step 9, branch the write path:
  * planet/station/moon-without-planet (shouldn't happen after step 2 guard): unchanged
  * moon WITH parent_planet_slug:
      `galaxy/<parent-system>/<parent-planet-slug>/<location-slug>/location.yaml`
- Update the objective (line 17) to mention that moons are one level deeper than planets/stations.

Pitfall references that already exist and MUST be preserved in all three files:
- P1 (slug case must match dir name exactly — janus-add-location step 5, janus-add-ship step 4)
- P2 (id must match filename stem — janus-add-npc step 3 and step 7)
- P5 (body_slug must be a planet not a moon — janus-add-ship step 7) — note this is the FLIP-SIDE
  of WR-03: P5 says "ship.body_slug can only point at a planet" and WR-03 says "a moon location
  must live one directory below a planet". Both are consistent — moons are nested under planets in
  data/galaxy/, and ships can only orbit planets (system children), never moons.

Verification harness (already in use, e.g. 24-05-PLAN.md task 1):
```
for f in skills/janus-add-ship/SKILL.md skills/janus-add-npc/SKILL.md skills/janus-add-location/SKILL.md; do
  python3 -c "
import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1])
assert fm['name'] in ('janus-add-ship','janus-add-npc','janus-add-location')
assert 'Use when' in fm['description']
assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools'])
print('OK', fm['name'])
"
done
```

Forbidden patterns (must remain absent after edits):
- `@./resources/` or `@resources/` in any SKILL.md (Pitfall P3)
- `map/main.yaml` in janus-add-ship/SKILL.md after the CR-03 fix (zero matches)
- `dr__elena_kim` (double underscore) anywhere in janus-add-npc/SKILL.md after WR-01 fix
- `dr._elena_kim` (literal period) anywhere in janus-add-npc/SKILL.md after WR-01 fix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix janus-add-ship deckplan path + format (CR-03)</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
    /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md
  </read_first>
  <action>
    Apply targeted edits to `skills/janus-add-ship/SKILL.md`. Scope: objective line 20, process step 13 (lines 83-85), confirmation step 14 (line 88). Do not rewrite unrelated steps.

    **Edit 1 — Objective (around line 19-22):**
    Replace the sentence:
    `An optional deckplan stub may also be created at` newline `\`ships/<slug>/map/main.yaml\`.`
    with:
    `An optional deckplan stub may also be created at` newline `\`ships/<slug>/deckplan.yaml\` using the canonical \`decks:\` list format defined in \`schema-encounters.md\` (the legacy \`map/main.yaml\` format is NOT supported by EncounterMapDisplay).`

    **Edit 2 — Step 13 (lines 83-85):**
    Replace the entire numbered item 13 with:

    ```
    13. Ask the user if they want an empty deckplan stub. If yes, build a minimal
        `data/ships/<ship-slug>/deckplan.yaml` containing a single deck with no rooms:

        ```yaml
        decks:
          - id: main_deck
            name: Main Deck
            level: 1
            default: true
            unit_size: 30
            rooms: []
        ```

        Call `write_file("ships/<ship-slug>/deckplan.yaml", stub_content)`. Do NOT write
        `ships/<ship-slug>/map/main.yaml` — that path is the legacy format and will not load
        in EncounterMapDisplay (see schema-encounters.md → "Skills should write new format only").
    ```

    Preserve the surrounding markdown structure: step 13 should sit between step 12 (write
    location.yaml) and step 14 (confirmation). The fenced ```yaml block is inside the numbered
    list — indent it consistently with the surrounding numbered-item body (4-space indent under
    the `13.` marker is the existing convention; match that). The closing comment after the
    ```yaml block should remain part of step 13's body.

    **Edit 3 — Step 14 (confirmation, around line 87-91):**
    Update the "Written path" bullet so it reads:
    `- Written paths: \`ships/<ship-slug>/location.yaml\` and (if the deckplan stub was created) \`ships/<ship-slug>/deckplan.yaml\``
    Replace any remaining reference to "map stub" with "deckplan stub" in that confirmation block.

    **Cross-check — entire file:**
    After applying the three edits, ensure NO occurrence of `map/main.yaml` remains in the file.
    Grep: `! grep -q "map/main.yaml" skills/janus-add-ship/SKILL.md`.
    Conversely, the literal `ships/<ship-slug>/deckplan.yaml` must appear in BOTH the objective
    block AND step 13's write_file call. Frontmatter is unchanged.

    Do NOT touch steps 1-12 or 15+ (if any). Pitfall P5 reference in step 7 stays exactly as-is.

    Verify frontmatter still parses:
    ```
    python3 -c "import yaml; c=open('/home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']=='janus-add-ship'; assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK')"
    ```

    Do NOT commit yet — Task 4 commits.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && python3 -c "import yaml; c=open('skills/janus-add-ship/SKILL.md').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']=='janus-add-ship'; assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK')" && grep -q "ships/<ship-slug>/deckplan.yaml" skills/janus-add-ship/SKILL.md && grep -q "ships/<slug>/deckplan.yaml" skills/janus-add-ship/SKILL.md && ! grep -q "map/main.yaml" skills/janus-add-ship/SKILL.md && grep -q "^        decks:" skills/janus-add-ship/SKILL.md && grep -q "rooms: \[\]" skills/janus-add-ship/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter still parses cleanly via the python yaml check
    - `grep -c "map/main.yaml" skills/janus-add-ship/SKILL.md` returns 0
    - Objective block (the `<objective>` ... `</objective>` section) literally contains the string `ships/<slug>/deckplan.yaml`
    - Step 13 literally contains the string `ships/<ship-slug>/deckplan.yaml` AND the call `write_file("ships/<ship-slug>/deckplan.yaml", stub_content)`
    - Step 13 contains a YAML fenced block whose body includes the keys `decks:`, `id: main_deck`, `name: Main Deck`, `level: 1`, `default: true`, `unit_size: 30`, `rooms: []`
    - Step 14 confirmation no longer says "map stub" — it says "deckplan stub" (or equivalent referencing deckplan.yaml)
    - No other steps (1-12, 14+) were modified beyond the confirmation line touched in Edit 3
    - Pitfall P5 wording in step 7 is unchanged
    - File ends with newline; per-file line count remains within 50-200
  </acceptance_criteria>
  <done>
    janus-add-ship SKILL.md instructs the AI to write the canonical `deckplan.yaml` with a `decks:` scaffold. The legacy `map/main.yaml` path has no remaining references in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix janus-add-npc id derivation formula (WR-01)</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  </read_first>
  <action>
    Replace the formula and example in step 3 of `skills/janus-add-npc/SKILL.md`. Scope: lines 30-37 only. Do not modify other steps or frontmatter.

    **Current text (lines 30-32):**
    ```
    3. Derive `id`: convert name to lowercase and replace spaces and hyphens with underscores.
       Formula: `name.lower().replace(" ", "_").replace("-", "_")`.
       Examples: "Captain Harrow" → `captain_harrow`, "Dr. Elena-Kim" → `dr__elena_kim`.
    ```

    **Replacement text:**
    ```
    3. Derive `id`: convert name to lowercase, replace spaces and hyphens with underscores, then
       strip any character that is not `[a-z0-9_]`. This removes periods, apostrophes, accents,
       and other punctuation that would otherwise leak into the filename stem (Pitfall P2).
       Formula: `re.sub(r'[^a-z0-9_]', '', name.lower().replace(' ', '_').replace('-', '_'))`.
       Examples: "Captain Harrow" → `captain_harrow`, "Dr. Elena-Kim" → `dr_elena_kim`,
       "O'Malley" → `omalley`.
    ```

    Preserve the surrounding numbered-list indentation. Do NOT touch:
    - Step 3's collision-suffix rule and Pitfall P2 reminder (the next two paragraphs of step 3)
    - Step 7's separate Pitfall P2 reference
    - Frontmatter, allowed-tools, schema include
    - Steps 1, 2, 4-9

    Verify frontmatter still parses.

    Do NOT commit yet — Task 4 commits.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && python3 -c "import yaml; c=open('skills/janus-add-npc/SKILL.md').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']=='janus-add-npc'; assert 'Use when' in fm['description']; print('OK')" && grep -q "re.sub" skills/janus-add-npc/SKILL.md && grep -q "\[\\^a-z0-9_\]" skills/janus-add-npc/SKILL.md && grep -q "dr_elena_kim" skills/janus-add-npc/SKILL.md && ! grep -q "dr__elena_kim" skills/janus-add-npc/SKILL.md && ! grep -q "dr\\._elena_kim" skills/janus-add-npc/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter still parses cleanly
    - Step 3 contains the literal string `re.sub(r'[^a-z0-9_]', '', name.lower().replace(' ', '_').replace('-', '_'))`
    - Step 3 example reads `"Dr. Elena-Kim" → dr_elena_kim` (single underscore, no period)
    - The strings `dr__elena_kim` (double underscore) and `dr._elena_kim` (literal period) do NOT appear anywhere in the file
    - Pitfall P2 references in step 3 and step 7 are preserved
    - File ends with newline; line count remains within 50-200
  </acceptance_criteria>
  <done>
    janus-add-npc step 3 documents an id formula that strips non-alphanumeric-underscore characters. The example matches the formula's output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix janus-add-location moon depth + ship redirect (WR-03)</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  </read_first>
  <action>
    Apply targeted edits to `skills/janus-add-location/SKILL.md` to handle moon and ship types correctly. Scope: objective (lines 15-21), step 2 (lines 30-32), step 9 (lines 62-63). Optionally add a brief note at step 4 about parent-planet listing for moons.

    **Edit 1 — Objective (around line 16-21):**
    Append a sentence at the end of the existing objective block:
    `Moons live one level deeper than planets — at \`data/galaxy/<system>/<planet-slug>/<moon-slug>/location.yaml\` — so this skill prompts for a parent planet when type=moon. Ships do NOT live under \`galaxy/\` at all; if type=ship, this skill redirects to \`/janus-add-ship\` and aborts.`

    Keep the existing Pitfall P1 / case sensitivity wording intact in the objective.

    **Edit 2 — Step 2 (currently lines 30-32):**
    Replace step 2 with an expanded version that branches on moon and ship:

    ```
    2. Ask the user (or infer from args) for `type` — must be one of:
       `station | planet | moon | system | ship`.

       - If type is `system`: redirect the user to `/janus-add-system` and abort (that skill
         handles the full system scaffolding with star_map.yaml + system_map.yaml updates).
       - If type is `ship`: redirect the user to `/janus-add-ship` and abort. Ships are NOT
         stored under `data/galaxy/` — they live in `data/ships/<slug>/location.yaml` with a
         body_slug pointer that self-injects onto the orbit map at runtime.
       - If type is `moon`: prompt the user for a `parent_planet_slug` (the planet this moon
         orbits, written as a lowercase-hyphenated directory name under
         `data/galaxy/<parent-system>/`). The moon's `location.yaml` lives one level deeper
         than a planet's — at `galaxy/<parent-system>/<parent-planet-slug>/<moon-slug>/location.yaml`.
         Verify the parent planet exists by calling
         `list_files("galaxy/<parent-system>/<parent-planet-slug>")` — if the call fails or
         returns empty, refuse with: "Parent planet '<parent_planet_slug>' not found under
         system '<parent-system>'. Add the planet first via this skill before adding moons."
       - If type is `planet` or `station`: proceed without additional prompts. The location
         will live at `galaxy/<parent-system>/<location-slug>/location.yaml`.
    ```

    **Edit 3 — Step 9 (currently lines 62-63):**
    Replace step 9 with a branched write path:

    ```
    9. Build the write path based on type:
       - `planet | station`: `galaxy/<parent-system>/<location-slug>/location.yaml`
       - `moon`: `galaxy/<parent-system>/<parent-planet-slug>/<location-slug>/location.yaml`
         (one level deeper — moons sit under the planet they orbit)

       Call `write_file(<write-path>, content)`. Triggers the same SSE broadcast for both types.
    ```

    **Cross-check the rest of the file:**
    - Step 4 (sibling listing for collision detection) still references `list_files("galaxy/<parent-system>")` — that's correct for planets/stations. For moons, the collision check should also be performed against `list_files("galaxy/<parent-system>/<parent-planet-slug>")` (children of the planet). Add a one-sentence note to step 6 (collision suffix logic) clarifying that for moons, the collision check operates against the planet's child listing, not the system's. Keep the existing wording intact otherwise.
    - Frontmatter and allowed-tools are unchanged.

    Verify frontmatter still parses.

    Do NOT commit yet — Task 4 commits.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && python3 -c "import yaml; c=open('skills/janus-add-location/SKILL.md').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']=='janus-add-location'; assert 'Use when' in fm['description']; print('OK')" && grep -q "parent_planet_slug\|parent-planet-slug" skills/janus-add-location/SKILL.md && grep -q "galaxy/<parent-system>/<parent-planet-slug>/<location-slug>/location.yaml\|galaxy/<parent-system>/<parent-planet-slug>/<moon-slug>/location.yaml" skills/janus-add-location/SKILL.md && grep -q "janus-add-ship" skills/janus-add-location/SKILL.md && grep -q "type is .moon.\|type == moon\|type is \`moon\`" skills/janus-add-location/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter still parses cleanly via the python yaml check
    - Step 2 explicitly enumerates the five accepted types (`station | planet | moon | system | ship`) and provides distinct guards for `system`, `ship`, and `moon`
    - For `type=ship`, step 2 directs the user to `/janus-add-ship` and the skill aborts (no write_file call)
    - For `type=moon`, step 2 prompts for `parent_planet_slug` and verifies its existence via `list_files("galaxy/<parent-system>/<parent-planet-slug>")`
    - Step 9 write path branches: moons go to `galaxy/<parent-system>/<parent-planet-slug>/<location-slug>/location.yaml`; planets/stations remain at `galaxy/<parent-system>/<location-slug>/location.yaml`
    - Objective block mentions both moon depth and ship redirect
    - Pitfall P1 case-sensitivity wording (existing) is preserved
    - File ends with newline; line count remains within 50-200
    - No regressions: existing planet/station flow still works (no required field was removed for those types)
  </acceptance_criteria>
  <done>
    janus-add-location correctly handles moon depth (one level under the parent planet) and refuses to misplace ships under `galaxy/`. The skill produces a valid on-disk location.yaml for every accepted type.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit the three SKILL.md gap closure fixes</name>
  <files>
    (no new files — commits artifacts from Tasks 1, 2, and 3)
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  </read_first>
  <action>
    From `/home/gjohnson/mothership/janus-skills`:

    1. Stage the three modified skills:
       `git add skills/janus-add-ship/SKILL.md skills/janus-add-npc/SKILL.md skills/janus-add-location/SKILL.md`

    2. Confirm only those three files are staged: `git status --porcelain` should list exactly three `M ` lines and nothing else. If any other staged/untracked files appear, stop and report — do NOT add them to this commit.

    3. Run the combined frontmatter check on all 8 skills to confirm no collateral damage:
       ```
       for f in /home/gjohnson/mothership/janus-skills/skills/janus-*/SKILL.md; do
         python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']; assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK', fm['name'])" || exit 1
       done
       ```
       All 8 must print `OK`.

    4. Commit with this message (heredoc to preserve formatting):
       ```
       fix(skills): correct deckplan path, NPC id formula, and moon depth

       Three SKILL.md correctness defects from the Phase 24 verification +
       review pass:

       - CR-03 (blocker): janus-add-ship step 13 instructed the AI to write
         the legacy path ships/<slug>/map/main.yaml with rooms: []. The
         canonical format defined in schema-encounters.md is
         ships/<slug>/deckplan.yaml with a decks: list. Ships created with
         the previous skill would silently fail to load in EncounterMapDisplay.
         Step 13 now writes the canonical deckplan stub.
       - WR-01 (warning): janus-add-npc step 3 formula
         name.lower().replace(' ','_').replace('-','_') left periods in the
         derived id ("Dr. Elena-Kim" -> "dr._elena_kim") and the example
         showed an inconsistent double underscore. Formula now wraps the
         result in re.sub(r'[^a-z0-9_]', '', ...) so all non-alphanumeric-
         underscore characters are stripped before the filename stem check
         (Pitfall P2).
       - WR-03 (warning): janus-add-location accepted type=moon but never
         branched on it — the write path always landed at
         galaxy/<system>/<slug>/location.yaml, one level too shallow for
         moons. Step 2 now prompts for parent_planet_slug on moon type and
         step 9 writes to galaxy/<system>/<planet>/<moon-slug>/location.yaml.
         Type=ship now redirects to /janus-add-ship and aborts (it does not
         live under galaxy/ at all).

       All 8 SKILL.md files still pass the frontmatter validation loop.

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```

    5. Verify clean tree: `git status --porcelain` returns empty.
    6. Confirm the new commit subject begins with `fix(skills):` and that `git show --name-only HEAD` lists exactly the three modified SKILL.md files (no others).
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && git log --oneline -1 | grep -q "fix(skills)" && [ -z "$(git status --porcelain)" ] && [ "$(git show --name-only --format= HEAD | sort)" = "$(printf 'skills/janus-add-location/SKILL.md\nskills/janus-add-npc/SKILL.md\nskills/janus-add-ship/SKILL.md\n' | sort)" ] && for f in skills/janus-*/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']; assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools'])" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/gjohnson/mothership/janus-skills log --oneline -1` subject begins with `fix(skills):`
    - `git -C /home/gjohnson/mothership/janus-skills show --name-only HEAD` lists exactly: `skills/janus-add-location/SKILL.md`, `skills/janus-add-npc/SKILL.md`, `skills/janus-add-ship/SKILL.md` — no other files
    - Commit body mentions all three issue IDs: CR-03, WR-01, WR-03
    - `git status --porcelain` is empty
    - All 8 SKILL.md files pass the python yaml frontmatter check
  </acceptance_criteria>
  <done>
    Three SKILL.md gap closure fixes committed in a single atomic commit. Working tree clean. All 8 skills still pass frontmatter validation.
  </done>
</task>

</tasks>

<verification>
- `grep -c "map/main.yaml" skills/janus-add-ship/SKILL.md` returns 0
- `grep -q "ships/<ship-slug>/deckplan.yaml" skills/janus-add-ship/SKILL.md` succeeds
- `grep -q "re.sub" skills/janus-add-npc/SKILL.md` succeeds
- `grep -q "dr__elena_kim\|dr\._elena_kim" skills/janus-add-npc/SKILL.md` returns no match (both wrong examples gone)
- `grep -q "parent_planet_slug\|parent-planet-slug" skills/janus-add-location/SKILL.md` succeeds
- `grep -q "janus-add-ship" skills/janus-add-location/SKILL.md` succeeds (ship redirect present)
- All 8 SKILL.md frontmatters parse cleanly via the python yaml loop
- A single commit on janus-skills `main` with subject `fix(skills):` listing exactly three files
</verification>

<success_criteria>
- VERIFICATION.md truth #8 ("janus-add-ship provides correct deckplan stub guidance") flips from FAILED to VERIFIED
- Anti-pattern table entries for janus-add-ship lines 83-85+20 (CR-03), janus-add-npc line 33 (WR-01), and janus-add-location lines 31-63 (WR-03) all closed
- No regressions: the other 5 SKILL.md files are unchanged and still pass frontmatter validation
- An end-to-end mental walk-through (AI following each fixed skill literally) produces correctly-located, correctly-formatted YAML artifacts on disk for: an orbit ship with deckplan stub, a punctuated NPC name, and a moon under a planet
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-07-SUMMARY.md` recording:
- Commit hash + commit subject
- Per-file diff summary (rough line counts added/removed; the three issue IDs each addresses)
- Confirmation table:
  | File                                    | Defect closed | Key grep match                           |
  |-----------------------------------------|---------------|------------------------------------------|
  | skills/janus-add-ship/SKILL.md          | CR-03         | `ships/<ship-slug>/deckplan.yaml`        |
  | skills/janus-add-npc/SKILL.md           | WR-01         | `re.sub(r'[^a-z0-9_]'...)`               |
  | skills/janus-add-location/SKILL.md      | WR-03         | `parent_planet_slug` + ship redirect     |
- Confirmation all 8 SKILL.md files pass the frontmatter check
</output>
