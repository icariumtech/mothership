---
phase: 27-mcp-image-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - data/campaign/images/logos/.gitkeep
  - data/campaign/images/maps/.gitkeep
  - data/campaign/images/misc/.gitkeep
  - core/views/gm_data.py
  - core/views/__init__.py
  - core/urls.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "Pillow is listed in requirements.txt so Docker builds include it"
    - "data/campaign/images/logos/, maps/, and misc/ directories exist and are git-tracked"
    - "POST /api/gm/upload-image/ returns 200 with saved_path and original_size_bytes for valid portrait input"
    - "POST /api/gm/upload-image/ returns 400 for filenames containing /, .., or \\"
    - "POST /api/gm/upload-image/ returns 400 for unknown image_type values"
    - "Portrait uploads with convert=true produce a converted PNG in data/campaign/NPCs/images/"
    - "Non-portrait types skip conversion regardless of the convert flag"
  artifacts:
    - path: "requirements.txt"
      provides: "Pillow>=10.0.0 dependency"
      contains: "Pillow"
    - path: "data/campaign/images/logos/.gitkeep"
      provides: "git-tracked logos directory"
    - path: "data/campaign/images/maps/.gitkeep"
      provides: "git-tracked maps directory"
    - path: "data/campaign/images/misc/.gitkeep"
      provides: "git-tracked misc directory"
    - path: "core/views/gm_data.py"
      provides: "api_gm_upload_image view function"
      exports: ["api_gm_upload_image"]
    - path: "core/views/__init__.py"
      provides: "re-export of api_gm_upload_image"
    - path: "core/urls.py"
      provides: "URL route path('api/gm/upload-image/', ...)"
  key_links:
    - from: "core/urls.py"
      to: "core/views/gm_data.py"
      via: "api_gm_upload_image import in core/views/__init__.py"
      pattern: "api_gm_upload_image"
    - from: "core/views/gm_data.py"
      to: "scripts/convert_npc_portraits.py"
      via: "sys.path insert + direct import of convert_portrait"
      pattern: "from convert_npc_portraits import convert_portrait"
---

<objective>
Lay the foundation for image upload in the charon repo: ensure Pillow is in requirements.txt, create the three new image subdirectories with .gitkeep files, implement the Django upload view, and wire it into the URL router.

Purpose: All subsequent work (MCP tool in Plan 02, skills in Plan 03) depends on the Django endpoint existing and being reachable. This plan is also the Pillow gap closure — without Pillow in requirements.txt the Docker image cannot convert portraits.

Output: POST /api/gm/upload-image/ endpoint live; three image directories tracked in git; Pillow pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gjohnson/mothership/charon/.planning/PROJECT.md
@/home/gjohnson/mothership/charon/.planning/ROADMAP.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From core/views/gm_data.py (existing file being extended):
- Module uses: django.http.JsonResponse, django.views.decorators.csrf.csrf_exempt,
  django.conf.settings, json, logging, pathlib.Path, core.data_loader, core.sse_broadcaster
- Path traversal pattern: `(data_root / filepath).resolve(); if not target.is_relative_to(data_root)`
- `settings.DATA_DIR` — absolute path to the data/ directory
- `settings.BASE_DIR` — project root (for scripts/ path)
- Existing view shape: @csrf_exempt, POST-only, json.loads(request.body), JsonResponse return

From core/views/__init__.py (existing — add one import block):
- Pattern: `from .gm_data import (api_gm_data_list, api_gm_data_file, api_gm_session_context, api_gm_data_schema,)`
- Append api_gm_upload_image to this block

From core/urls.py (existing — add one path() entry):
- GM Data API section ends at: path('api/gm/data-schema', views.api_gm_data_schema, name='gm_data_schema')
- New entry goes after this line

From scripts/convert_npc_portraits.py:
- Function signature: `def convert_portrait(input_path: str, output_path: str) -> None`
- Does NOT check if output exists — always converts (unlike main() which skips existing)
- Does NOT create output directory — caller must mkdir first
- Raises PIL.UnidentifiedImageError if input is not a valid image file

From requirements.txt (current contents — Pillow is absent):
asgiref==3.10.0, Django==5.2.7, sqlparse==0.5.3, PyYAML>=6.0, anthropic>=0.39.0,
python-dotenv>=1.0.0, gunicorn==26.0.0, gevent==26.4.0, whitenoise==6.12.0,
fastmcp==3.3.1, uvicorn>=0.30.0, httpx>=0.27.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pillow dependency + image directories</name>
  <files>
    requirements.txt,
    data/campaign/images/logos/.gitkeep,
    data/campaign/images/maps/.gitkeep,
    data/campaign/images/misc/.gitkeep
  </files>
  <read_first>
    - /home/gjohnson/mothership/charon/requirements.txt — current contents; Pillow must be appended
    - /home/gjohnson/mothership/charon/data/campaign/images/ — verify logos/, maps/, misc/ are absent; korova-stahl-logo.png is in the parent, NOT in logos/
  </read_first>
  <action>
    Add `Pillow>=10.0.0` as a new line at the end of requirements.txt. Do not change any existing lines.

    Create three empty directories by writing empty .gitkeep files:
    - data/campaign/images/logos/.gitkeep (empty file)
    - data/campaign/images/maps/.gitkeep (empty file)
    - data/campaign/images/misc/.gitkeep (empty file)

    Do NOT move or modify data/campaign/images/korova-stahl-logo.png — it lives in the parent images/ directory and is not being reorganized in this phase.
  </action>
  <verify>
    <automated>grep -c 'Pillow' /home/gjohnson/mothership/charon/requirements.txt && ls /home/gjohnson/mothership/charon/data/campaign/images/logos/.gitkeep /home/gjohnson/mothership/charon/data/campaign/images/maps/.gitkeep /home/gjohnson/mothership/charon/data/campaign/images/misc/.gitkeep</automated>
  </verify>
  <done>
    requirements.txt contains `Pillow>=10.0.0`; all three .gitkeep files exist; korova-stahl-logo.png is undisturbed.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Django upload-image view + URL wiring</name>
  <files>
    core/views/gm_data.py,
    core/views/__init__.py,
    core/urls.py,
    core/tests/test_upload_image.py
  </files>
  <read_first>
    - /home/gjohnson/mothership/charon/core/views/gm_data.py — read in full; append api_gm_upload_image after api_gm_data_schema
    - /home/gjohnson/mothership/charon/core/views/__init__.py — read in full; add api_gm_upload_image to the gm_data import block
    - /home/gjohnson/mothership/charon/core/urls.py — read in full; insert new path() after gm_data_schema entry
    - /home/gjohnson/mothership/charon/core/tests/test_gm_api.py — read for skip pattern and test class structure
  </read_first>
  <behavior>
    - POST /api/gm/upload-image/ with valid JSON body {filename: "npc.png", content_base64: "<valid b64>", image_type: "portrait", convert: false} → 200, response contains saved_path and original_size_bytes
    - POST with filename containing "/" → 400, response contains "error"
    - POST with filename containing ".." → 400, response contains "error"
    - POST with filename containing "\\" → 400, response contains "error"
    - POST with image_type = "unknown" → 400, response contains "error"
    - POST with malformed base64 → 400, response contains "error"
    - POST with image_type = "logo" → saves to data/campaign/images/logos/; no converted_path in response
    - GET /api/gm/upload-image/ → 405
  </behavior>
  <action>
    Write the test file core/tests/test_upload_image.py first (RED), then implement the view (GREEN).

    TEST FILE (core/tests/test_upload_image.py): Write a Django TestCase class UploadImageApiTests covering the behaviors listed above. Import base64 and use base64.b64encode(b"fake image bytes").decode() as valid content. Use @unittest.skip("requires writable DATA_DIR") on any test that actually writes to disk. Path-traversal and image_type validation tests do NOT require a writable DATA_DIR and must NOT be skipped. GET method test must not be skipped.

    VIEW FUNCTION (append to core/views/gm_data.py): Add function api_gm_upload_image with these specifics:
    - Decorated with @csrf_exempt
    - POST-only; return 405 for other methods
    - Parse request.body as JSON; return 400 on parse failure
    - Extract: filename (str, strip), content_base64 (str), image_type (str, strip), convert (bool, default True)
    - Filename safety check: reject if empty OR if any char in ('/', '\\', '..') is present anywhere in the string; return 400 {"error": "Invalid filename"}
    - image_type allowlist: ALLOWED_IMAGE_TYPES = frozenset({'portrait', 'logo', 'map', 'misc'}); return 400 if not in set
    - Normalize convert bool: after json.loads, if not isinstance(convert, bool), set convert = str(convert).lower() not in ('false', '0', 'no') — guards against MCP clients that serialize booleans as strings (Pitfall 6 from RESEARCH.md)
    - Resolve destination using settings.DATA_DIR (Path); type_dirs dict: portrait → campaign/NPCs/images_source, logo → campaign/images/logos, map → campaign/images/maps, misc → campaign/images/misc
    - dest_path = type_dirs[image_type] / filename
    - Decode: raw_bytes = base64.b64decode(content_base64, validate=False); wrap in try/except (binascii.Error, Exception) → return 400 {"error": "Invalid base64 content"}
    - Write: dest_dir.mkdir(parents=True, exist_ok=True); dest_path.write_bytes(raw_bytes); wrap in try/except OSError → log + return 500
    - Build result = {'saved_path': str(dest_path.relative_to(Path(settings.DATA_DIR))), 'original_size_bytes': len(raw_bytes)}
    - Portrait conversion block: if image_type == 'portrait' and convert: compute images_dir = data_root / 'campaign' / 'NPCs' / 'images'; stem = Path(filename).stem; output_path = images_dir / f'{stem}.png'; images_dir.mkdir(parents=True, exist_ok=True); add scripts/ to sys.path with if-guard; import convert_portrait from convert_npc_portraits; call convert_portrait(str(dest_path), str(output_path)); on success add result['converted_path'] = str(output_path.relative_to(data_root)); on Exception log and add result['conversion_warning'] = str(e) (non-fatal)
    - Return JsonResponse(result)
    - Add module-level imports needed: base64, import binascii (for exception type clarity), sys — add to the existing import block at top of gm_data.py; do not duplicate imports already present

    URL WIRING (core/urls.py): After the line `path('api/gm/data-schema', views.api_gm_data_schema, name='gm_data_schema'),` add: `path('api/gm/upload-image/', views.api_gm_upload_image, name='gm_upload_image'),`

    __init__.py: In the `from .gm_data import (...)` block, add `api_gm_upload_image` to the import list.

    Add a module-level docstring update to gm_data.py: add "POST /api/gm/upload-image/ — save base64-encoded binary image to campaign data directory" to the list of endpoints in the existing module docstring.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && python manage.py test core.tests.test_upload_image -v 2 2>&1 | tail -20</automated>
  </verify>
  <done>
    All non-skipped tests in test_upload_image.py pass; python manage.py test core runs without import errors; GET /api/gm/upload-image/ returns 405; path-traversal filenames return 400; unknown image_type returns 400.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → Django API | Untrusted JSON body with filename, base64 content, image_type, convert flag |
| Django view → filesystem | dest_path must stay within DATA_DIR; filename must not contain path separators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | api_gm_upload_image filename param | mitigate | Reject filename containing '/', '\\', '..' before any path construction; character blocklist check in view |
| T-27-01-02 | Tampering | api_gm_upload_image image_type param | mitigate | image_type validated against ALLOWED_IMAGE_TYPES frozenset; path resolution is server-side only |
| T-27-01-03 | DoS | api_gm_upload_image base64 payload | accept | Trust-network model; Django DATA_UPLOAD_MAX_MEMORY_SIZE (2.5MB default) provides basic protection |
| T-27-01-04 | Tampering | base64 content decoded to arbitrary bytes | accept | Files written to campaign data directory only; no execution; homelab trust model |
| T-27-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed via package manager; Pillow already in venv — requirements.txt pin only |
</threat_model>

<verification>
```bash
cd /home/gjohnson/mothership/charon
python manage.py test core.tests.test_upload_image -v 2
python manage.py test core
grep 'Pillow' requirements.txt
grep 'gm_upload_image' core/urls.py
grep 'api_gm_upload_image' core/views/__init__.py
```
All commands exit 0. Test suite runs without import errors.
</verification>

<success_criteria>
- requirements.txt contains `Pillow>=10.0.0`
- data/campaign/images/logos/.gitkeep, maps/.gitkeep, misc/.gitkeep exist
- POST /api/gm/upload-image/ is routed and reachable (Django test client confirms)
- Path traversal filenames return 400
- Unknown image_type returns 400
- Malformed base64 returns 400
- GET method returns 405
- All non-skipped tests in test_upload_image.py pass
- python manage.py test core exits 0
</success_criteria>

<output>
Create `/home/gjohnson/mothership/charon/.planning/phases/27-mcp-image-upload/27-01-SUMMARY.md` when done.
</output>
