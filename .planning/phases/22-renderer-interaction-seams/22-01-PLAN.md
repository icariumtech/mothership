---
phase: 22-renderer-interaction-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useExclusivePopover.ts
  - src/components/domain/encounter/EncounterMapRenderer.tsx
autonomous: true
requirements: []
tags:
  - react-hooks
  - refactor
  - discriminated-union

must_haves:
  truths:
    - "Opening any popover slot (token / door / poi / room) closes any other open slot automatically — one popover visible at a time, enforced by hook state"
    - "EncounterMapRenderer.tsx no longer holds the four prior popover useState hooks (selectedTokenId/Pos, selectedDoor, poiPopup, contextMenu)"
    - "useExclusivePopover.ts is a generic, payload-agnostic hook that imports no encounter domain types"
    - "Visual smoke test in browser: GM right-clicks a room → context menu appears; left-clicks a token → token popup appears AND context menu disappears in the same render"
  artifacts:
    - path: "src/hooks/useExclusivePopover.ts"
      provides: "Generic exclusive-popover state hook"
      exports: ["useExclusivePopover"]
      contains: "export function useExclusivePopover<"
    - path: "src/components/domain/encounter/EncounterMapRenderer.tsx"
      provides: "Renderer consuming useExclusivePopover for all four popover slots"
      contains: "useExclusivePopover<EncounterPopover>"
  key_links:
    - from: "src/components/domain/encounter/EncounterMapRenderer.tsx"
      to: "src/hooks/useExclusivePopover.ts"
      via: "named import"
      pattern: "import \\{ useExclusivePopover \\} from"
    - from: "EncounterMapRenderer.tsx popover JSX (lines ~1219, 1233, 1273, 1287)"
      to: "popover state from useExclusivePopover"
      via: "discriminated-union narrowing (popover?.type === 'token' etc.)"
      pattern: "popover\\.type === '(token|door|poi|room)'"
---

<objective>
Extract the four independent popover `useState` hooks in `EncounterMapRenderer.tsx` (token tooltip, door status, POI hover, room context menu) into a generic `useExclusivePopover<T>` hook at `src/hooks/useExclusivePopover.ts`. Wire the renderer to use the new hook with an `EncounterPopover` discriminated union defined locally. Opening any slot automatically closes all others, replacing the prior loose convention with a hook-enforced invariant.

Purpose: Eliminates the renderer's responsibility to manually coordinate four mutually-exclusive popover states. Continues the Phase 21 decomposition — after this plan, the renderer's popover coordination sits behind a named seam.

Output:
- New file `src/hooks/useExclusivePopover.ts`
- Modified `src/components/domain/encounter/EncounterMapRenderer.tsx` with 4 useState blocks deleted and replaced by 1 hook call + `EncounterPopover` union
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-renderer-interaction-seams/22-CONTEXT.md
@.planning/phases/22-renderer-interaction-seams/22-PATTERNS.md
@src/hooks/usePanZoom.ts
@src/types/encounterMap.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From src/types/encounterMap.ts (already imported in renderer):
- `DoorStatus` — string union: 'OPEN' | 'CLOSED' | 'LOCKED' | 'SEALED' | 'DAMAGED'
- `PoiData` — interface with `{ id, type, room, position, name, icon, status?, description? }`
- `GridRoom` — room interface used as `room` payload in context-menu slot

From src/hooks/usePanZoom.ts (template for hook style):
- Named export `export function usePanZoom(containerRef: RefObject<...>)`
- Returns plain named-field object `{ viewState, containerHandlers, resetView }`
- Uses `useState` + `useCallback` pair

From src/components/domain/encounter/EncounterMapRenderer.tsx (extraction target):
- Lines 137–160: four `useState` blocks to remove
- Lines 302–307: `setSelectedDoor({ id, x, y, status })` call site
- Lines 319–323, 348–352: `setContextMenu({ room, x, y })` call sites
- Line 911: `setPoiPopup({ poi, x, y })` call site
- Line 921: `onMouseLeave={() => setPoiPopup(null)}`
- Lines 1112–1141: token selection (`setSelectedTokenId`, `setSelectedTokenPos`)
- Lines 1219–1297: render blocks that read all four states (TokenPopup, poi popup div, DoorStatusPopup, RoomContextMenu)

Discriminated-union shape to define inside the renderer module (above the component function):
```
type EncounterPopover =
  | { type: 'token'; payload: { id: string; pos: { x: number; y: number } } }
  | { type: 'door';  payload: { id: string; x: number; y: number; status: DoorStatus } }
  | { type: 'poi';   payload: { poi: PoiData; x: number; y: number } }
  | { type: 'room';  payload: { room: GridRoom; x: number; y: number } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useExclusivePopover hook</name>
  <files>src/hooks/useExclusivePopover.ts</files>
  <read_first>
    - src/hooks/usePanZoom.ts (template — interface shape, named export, JSDoc style, useState + useCallback pattern)
    - src/hooks/useRoomRevealAnimations.ts (secondary template — generic-options interface style)
    - .planning/phases/22-renderer-interaction-seams/22-CONTEXT.md (D-03, D-04, D-05 — locked decisions)
    - .planning/phases/22-renderer-interaction-seams/22-PATTERNS.md (lines 19–133 — useExclusivePopover pattern assignments)
  </read_first>
  <action>
    Create new file `src/hooks/useExclusivePopover.ts` per D-05. Per D-04, the hook is generic and payload-agnostic — it imports zero encounter-domain types. Per D-03, exclusivity is enforced by holding a single `useState<T | null>(null)` where `T` is the caller's discriminated union. Imports: only `useState` and `useCallback` from `react`. No `RefObject` param, no antd, no domain types.

    Export a single named function: `export function useExclusivePopover<T>()`. Inside, declare `const [popover, setPopover] = useState<T | null>(null)`. Define two `useCallback` operations with empty dependency arrays: `open: (value: T) => void` that calls `setPopover(value)` (this replaces any prior open slot — exclusivity is structural), and `close: () => void` that calls `setPopover(null)`. Return `{ popover, open, close }` as a plain named-field object — same return style as `usePanZoom`.

    Add a JSDoc block above the export modeled on the JSDoc at usePanZoom lines 21–28: describe that the hook holds at most one popover at a time, that opening any value replaces the prior one (exclusivity), that the caller defines its own discriminated union as the type parameter, and that this hook owns no domain knowledge.

    Do NOT add: tests, additional helpers, "isOpen" booleans (consumers can derive from `popover !== null`), or type-narrowing helpers. Keep the hook surface to the three returned fields per D-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&amp;1 | grep -E "useExclusivePopover" || echo "ok: no typecheck errors in useExclusivePopover.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useExclusivePopover.ts` exists
    - File contains `export function useExclusivePopover<` (generic named export)
    - File contains `useState<T | null>(null)`
    - File contains exactly two `useCallback` calls (one for `open`, one for `close`)
    - File contains `return { popover, open, close }`
    - File imports ONLY from `react` (verifiable: `grep -E "^import" src/hooks/useExclusivePopover.ts` returns exactly one line, `import { useState, useCallback } from 'react';`)
    - File contains JSDoc block (`/**`) above the export
    - `npx tsc --noEmit -p .` does not report errors in this file
  </acceptance_criteria>
  <done>useExclusivePopover.ts compiles cleanly, is generic, imports only react, returns {popover, open, close}.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire EncounterMapRenderer to useExclusivePopover</name>
  <files>src/components/domain/encounter/EncounterMapRenderer.tsx</files>
  <read_first>
    - src/components/domain/encounter/EncounterMapRenderer.tsx (full file — every popover read/write site must be updated)
    - src/hooks/useExclusivePopover.ts (the hook created in Task 1 — confirms its surface)
    - .planning/phases/22-renderer-interaction-seams/22-PATTERNS.md (lines 237–286 — renderer modification pattern, EncounterPopover union shape)
    - src/types/encounterMap.ts (DoorStatus, PoiData, GridRoom types used by the union)
  </read_first>
  <action>
    Step A — Add import: add `import { useExclusivePopover } from '../../../hooks/useExclusivePopover';` alongside the existing `usePanZoom` import at the top of the file (line 10 area). Also add `PoiData` to the existing `import { ... } from '../../../types/encounterMap';` block at lines 12–23 (the renderer currently uses an inline `import('../../../types/encounterMap').PoiData` in the old `poiPopup` useState — switch to a regular named import).

    Step B — Define `EncounterPopover` discriminated union at module scope, immediately above the `export function EncounterMapRenderer(` declaration (between the `WALL_THICKNESS` constant area and the component). The union has exactly four variants with `type` and `payload` fields:
      - `{ type: 'token'; payload: { id: string; pos: { x: number; y: number } } }`
      - `{ type: 'door';  payload: { id: string; x: number; y: number; status: DoorStatus } }`
      - `{ type: 'poi';   payload: { poi: PoiData; x: number; y: number } }`
      - `{ type: 'room';  payload: { room: GridRoom; x: number; y: number } }`

    Step C — Delete the four `useState` blocks at lines 137–160 verbatim (selectedTokenId, selectedTokenPos, selectedDoor, poiPopup, contextMenu — five `useState` calls collapsing the token id+pos pair into one `token` slot). Replace with a single line placed near the existing `usePanZoom` call (line 164):
      `const { popover, open: openPopover, close: closePopover } = useExclusivePopover<EncounterPopover>();`
    Use `openPopover` / `closePopover` as the local names to avoid shadowing.

    Step D — Update every call site that previously set or cleared popover state. Use these concrete replacements (search the file for each pattern, do not rely on line numbers since deletes shift them):
      - `setSelectedDoor({ id: door.id, x: ..., y: ..., status: getEffectiveDoorStatus(door) })` → `openPopover({ type: 'door', payload: { id: door.id, x: ..., y: ..., status: getEffectiveDoorStatus(door) } })`
      - `setContextMenu({ room, x: ..., y: ... })` (both occurrences — `handleRoomContextMenu` and `handleRoomPointerUp`) → `openPopover({ type: 'room', payload: { room, x: ..., y: ... } })`
      - `setPoiPopup({ poi, x: ..., y: ... })` → `openPopover({ type: 'poi', payload: { poi, x: ..., y: ... } })`
      - `setPoiPopup(null)` → `closePopover()`
      - `setSelectedTokenId(id); setSelectedTokenPos({ x: ..., y: ... })` (paired calls, 3 sites in TokenLayer onSelect handler) → `openPopover({ type: 'token', payload: { id, pos: { x: ..., y: ... } } })`
      - `setSelectedTokenId(null); setSelectedTokenPos(null)` (or any standalone pair) → `closePopover()`

    Step E — Update the four render blocks (currently at lines ~1219, 1233, 1273, 1287, but line numbers shift after edits — find by component name). Each guard becomes a discriminated-union narrowing:
      - TokenPopup block (`{selectedTokenId && tokens?.[selectedTokenId] && selectedTokenPos && ...}`) → `{popover?.type === 'token' && tokens?.[popover.payload.id] && (...) }`. Inside, replace `selectedTokenId` with `popover.payload.id`, `selectedTokenPos.x/y` with `popover.payload.pos.x/y`, and the close handler with `() => closePopover()`.
      - POI popup block (`{poiPopup && (...)}`) → `{popover?.type === 'poi' && (...)}`. Inside, replace `poiPopup.x/y` with `popover.payload.x/y` and `poiPopup.poi` with `popover.payload.poi`.
      - DoorStatusPopup block (`{selectedDoor && onDoorStatusChange && (...)}`) → `{popover?.type === 'door' && onDoorStatusChange && (...)}`. Replace `selectedDoor.x/y/id/status` with `popover.payload.x/y/id/status`. The two close-handlers (after onDoorStatusChange call and `onClose`) call `closePopover()`.
      - RoomContextMenu block (`{contextMenu && (...)}`) → `{popover?.type === 'room' && (...)}`. Replace `contextMenu.room/x/y` with `popover.payload.room/x/y`. The two close-handlers call `closePopover()`.

    Step F — Remove `useState` from the React import on line 9 if no other `useState` calls remain (TypeScript `noUnusedLocals` will catch any miss). Verify with grep: after the edit, `grep -c "useState" src/components/domain/encounter/EncounterMapRenderer.tsx` should match only the import line if it still contains useState, otherwise 0.

    Constraint: Do NOT change the behavior of any non-popover code. Geometry, projection, reveal animations, drag-and-drop (handled in Plan 02), pan/zoom, and SVG rendering are out of scope. Do NOT extract `useTokenPlacement` — that is Plan 02.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&amp;1 | grep -E "EncounterMapRenderer\.tsx" | grep -v "^$" | grep -c "error" || echo "ok: 0 typecheck errors"</automated>
    <automated>grep -v '^#' src/components/domain/encounter/EncounterMapRenderer.tsx | grep -cE "setSelectedTokenId|setSelectedTokenPos|setSelectedDoor|setPoiPopup|setContextMenu" | xargs -I{} test {} -eq 0 &amp;&amp; echo "ok: zero references to old popover setters"</automated>
    <automated>grep -v '^#' src/components/domain/encounter/EncounterMapRenderer.tsx | grep -cE "useState<\{|useState<string \| null>|useState<\{ id" | xargs -I{} test {} -eq 0 &amp;&amp; echo "ok: no popover useState remaining"</automated>
    <automated>grep -c "useExclusivePopover<EncounterPopover>" src/components/domain/encounter/EncounterMapRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EncounterMapRenderer.tsx` contains exactly one call to `useExclusivePopover<EncounterPopover>()`
    - `EncounterMapRenderer.tsx` contains the `EncounterPopover` type declaration with four variants (`'token'`, `'door'`, `'poi'`, `'room'`)
    - `grep -v '^#' src/components/domain/encounter/EncounterMapRenderer.tsx | grep -E "setSelectedTokenId|setSelectedTokenPos|setSelectedDoor|setPoiPopup|setContextMenu"` returns zero non-comment matches
    - All four popover render guards use `popover?.type === '<slot>'` narrowing (verifiable by grep for the four patterns `popover\?\.type === 'token'`, `'door'`, `'poi'`, `'room'`)
    - `npx tsc --noEmit -p .` exits 0 (no type errors anywhere in the project)
    - `npm run build` exits 0
    - Behavior preserved: in a manual smoke test (executor records evidence) GM right-clicks a room → context menu shows; left-clicks a token while context menu is open → token popup appears AND context menu disappears within the same render
  </acceptance_criteria>
  <done>Renderer compiles, builds clean, all four popover slots routed through useExclusivePopover, prior useState blocks removed, exclusivity behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundaries. The popover state machine operates entirely within the React render cycle of `EncounterMapRenderer.tsx`. All data flowing into popover payloads (token IDs, door records, POI records, room records) is sourced from `mapData` props that crossed the trust boundary in upstream phases. No network I/O, no user-input parsing, no auth, no external APIs are introduced or touched.

| Boundary | Description |
|----------|-------------|
| None added | Pure internal refactor — no new external attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information disclosure | useExclusivePopover hook | accept | Hook is generic, payload-agnostic, holds only one popover at a time; no privileged data persists beyond a single popover close. No mitigation needed beyond existing renderer-level GM/player gating (unchanged). |
| T-22-02 | Tampering | EncounterPopover discriminated union | accept | The union is internal to the renderer module; consumers cannot inject arbitrary variants because TypeScript narrowing at all render sites prevents type confusion at compile time. Runtime values come from already-validated event handlers (door click, POI hover, etc.). |

This phase is an internal TypeScript refactor with no external attack surface. The extracted hooks operate entirely within the React render cycle on already-validated data. ASVS L1 controls established in prior phases remain in force unchanged.
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit -p .` exits 0
- Build: `npm run build` exits 0
- Grep gate: zero references to old setters (`setSelected*`, `setPoiPopup`, `setContextMenu`) outside comments
- Grep gate: exactly one `useExclusivePopover<EncounterPopover>()` call site
- Manual smoke (recorded in summary): GM context menu + token tooltip exclusivity holds
</verification>

<success_criteria>
- All five popover `useState` calls deleted from `EncounterMapRenderer.tsx`
- Single `useExclusivePopover<EncounterPopover>()` call replaces them
- Four render-guard sites use discriminated-union narrowing
- TypeScript clean, build passes
- Manual smoke confirms exclusivity is now structurally enforced (opening one slot closes others)
</success_criteria>

<output>
After completion, create `.planning/phases/22-renderer-interaction-seams/22-01-SUMMARY.md` per the standard summary template.
</output>
