---
phase: 28.1-close-gap-d-10-data-changed-sse-consumer-phase-28-uat-verifi
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05]
tags: [uat, human-verify, phase-28, file-editor, gap-closure]

must_haves:
  truths:
    - "All 11 tests in 28-UAT.md have a non-pending result (pass / fail / blocked) recorded"
    - "Each failing test has a 'Gaps' entry describing the observed behavior vs expected"
    - "28-UAT.md frontmatter status changes from 'testing' to 'complete'"
    - "Summary block counts (passed / issues / pending / blocked) reflect the actual outcomes and sum to 11"
  artifacts:
    - path: ".planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md"
      provides: "Completed UAT report for all 11 Phase 28 tests"
      contains: "status: complete"
  key_links:
    - from: "28-UAT.md test results"
      to: "28-VERIFICATION.md (Plan 28.1-03)"
      via: "Plan 03 depends on these results to write the verification report"
      pattern: "Each test result line: result: (pass|fail|blocked|skipped)"
---

<objective>
Run the 11 human-driven UAT tests defined in `.planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md` and record the result of each. The Phase 28 FileEditorView is fully implemented (per `28-03-SUMMARY.md`) but no human has yet verified the live behavior end to end. The v2.0 milestone audit flagged this as the reason Phase 28 cannot be marked verified.

Purpose: Close the audit gap "Phase 28 UAT pending" by capturing real human observations against the running app, so Plan 28.1-03 can author a defensible VERIFICATION.md grounded in actual results.

Output: An updated `28-UAT.md` with every test resolved (pass / fail / blocked), gap entries for any failures, and `status: complete` in the frontmatter.

**This plan is NOT autonomous.** It requires a human to drive the GM Console in a real browser. The executor's role is to (a) make sure the dev server is up, (b) walk the human through each test with the exact navigation/observation steps, (c) record the human's verbal verdict into the file in the canonical format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md
@.planning/phases/28-gm-console-data-directory-file-editor/28-CONTEXT.md
@.planning/phases/28-gm-console-data-directory-file-editor/28-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- 28-UAT.md result format (per existing convention) -->
<!-- For each test, change `result: [pending]` to one of: -->

```
result: pass
```

```
result: fail
notes: <one-line observation of the unexpected behavior>
```

```
result: blocked
reason: <one-line reason the test could not be evaluated>
```

<!-- Frontmatter to set on completion -->
status: complete
updated: <ISO 8601 timestamp at completion>

<!-- Summary block update template -->
total: 11
passed: <n>
issues: <n_failures>
pending: 0
skipped: <n>
blocked: <n>

<!-- Gaps section: one entry per failure -->
### Gap N: <short title>
- Test: <test number + name>
- Expected: <copied from test>
- Observed: <human-reported actual behavior>
- Severity: <low | medium | high>
- Action: <follow-up plan or "tracked in tech debt">
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ensure dev server is running and open the GM Console</name>
  <files>(none modified)</files>
  <read_first>
    - .planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md
    - CLAUDE.md (Quick Reference section — `./start_server.sh` and URL conventions)
  </read_first>
  <action>
    Check whether a Django dev server is already listening on port 8000 with `curl -sS -o /dev/null -w '%{http_code}\n' http://localhost:8000/gmconsole/ 2>/dev/null` (or equivalent). If the response is non-200 or curl fails, start the server in the background per CLAUDE.md: `./start_server.sh` (or `pnpm run dev` in a separate worktree if Vite is needed for hot reload — Vite is at port 5173 by default; check vite.config for the actual port). The standard path used by other GM smoke tests is the Django-served bundle at http://localhost:8000/gmconsole/.

    Once the server responds 200, tell the human in plain text:

    "GM Console is up at http://localhost:8000/gmconsole/. Open it in a browser tab. I'm going to walk you through 11 UAT tests for the FILE_EDITOR view. For each one, I'll describe what to do and what to look for, then ask 'pass / fail / blocked?'. Type your verdict (and a short note if anything's odd) and I'll record it."

    Do not proceed to Task 2 until the human acknowledges the GM Console is loaded.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w '%{http_code}\n' http://localhost:8000/gmconsole/ 2>/dev/null | grep -q '^200$' &amp;&amp; echo "server up" || echo "server NOT up"</automated>
  </verify>
  <acceptance_criteria>
    - The curl probe against http://localhost:8000/gmconsole/ returns HTTP 200
    - Human has confirmed (in chat) that the GM Console is loaded in their browser
  </acceptance_criteria>
  <done>
    Dev server is reachable and the human has the GM Console open in a browser.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk through all 11 UAT tests and record each verdict</name>
  <files>.planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md</files>
  <read_first>
    - .planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md (full file — 75 lines)
  </read_first>
  <what-built>
    The FileEditorView from Phase 28: a full-screen GM-console view with a left-side data directory tree, a Monaco editor on the right, a Save button with amber dirty indicator, Ctrl+S save, YAML validation error banner, image preview fallback, and localStorage-persisted last-opened file.
  </what-built>
  <how-to-verify>
    For each of the 11 tests in `28-UAT.md`, do the following:

    1. Read the test number, name, and `expected:` block to the human verbatim.
    2. State the navigation/observation instructions implied by the test (paraphrased from the expected behavior — e.g. "Click the FILE_EDITOR icon at the very bottom of the left ViewRail. Tell me whether it sits at the bottom, below a separator, with a code/editor glyph.").
    3. Wait for the human's reply.
    4. Record the verdict by replacing `result: [pending]` with one of:
       - `result: pass` (no further notes needed)
       - `result: fail` (add a `notes:` line below with the one-line human-reported observation)
       - `result: blocked` (add a `reason:` line below)
       - `result: skipped` (add a `reason:` line — use sparingly, only if the test depends on data that doesn't exist in the campaign)
    5. Move to the next test. Do not batch verdicts — record each one to the file immediately so partial progress survives an interrupted session.

    Test inventory (from 28-UAT.md):
    1. FILE_EDITOR button in ViewRail (bottom placement)
    2. DISPLAY button disabled when FILE_EDITOR active
    3. Split-pane layout fills GM Console content area
    4. Data directory tree loads (campaign / galaxy / ships etc.)
    5. Lazy folder expansion (click loads children, click again collapses)
    6. YAML file opens in Monaco editor (editable, file path shown)
    7. Dirty indicator turns Save button amber after edit
    8. Ctrl+S saves the file (green flash + disk write)
    9. Markdown file opens editable in Monaco
    10. Image file shows `<img>` preview (not Monaco)
    11. localStorage persists last-opened file across view switch

    For Test 8 (Ctrl+S), after the human reports a successful save, run `ls -la --time=ctime data/campaign/janus.yaml 2>/dev/null | head -1` (or whatever file they edited — confirm the path with them) to objectively verify the file's ctime is fresh. Include this evidence in the result line as `notes: ctime <timestamp> after save`.

    After all 11 results are recorded:
    - Update the `## Summary` block: total: 11, passed: <count>, issues: <count of fails>, pending: 0, skipped: <count>, blocked: <count>
    - Update the `## Gaps` section: replace `[none yet]` with one numbered entry per failure (see Gap format in `<interfaces>` above); if zero failures, write `No gaps — all 11 tests passed.`
    - Update the frontmatter: `status: complete`, `updated: <current ISO 8601 timestamp>`
    - Remove the `## Current Test` block at the top (it tracks in-progress state and becomes stale)
  </how-to-verify>
  <resume-signal>
    Once all 11 verdicts are recorded and the file is saved, ask the human to type "uat-done" to confirm the run is complete. If the human wants to amend a result, they can edit the file directly or ask you to update it.
  </resume-signal>
  <acceptance_criteria>
    - `grep -c "result: \[pending\]" .planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md` returns 0
    - `grep -cE "^result: (pass|fail|blocked|skipped)" .planning/phases/28-gm-console-data-directory-file-editor/28-UAT.md` returns 11
    - The frontmatter `status:` line reads `status: complete`
    - The Summary block's `passed + issues + skipped + blocked` equals 11
    - For every `result: fail` line, the immediately-following `notes:` line is present
    - The Gaps section either lists one entry per failure or says "No gaps — all 11 tests passed."
  </acceptance_criteria>
  <done>
    All 11 UAT tests have recorded human verdicts, the summary counts match, and the file is marked `status: complete`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verdict → 28-UAT.md | Trust the human; document any verdict they give. The verdict is the artifact. |
| Dev server reachability | If the server isn't up, no test is meaningful — Task 1 gates Task 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.1-02-01 | Integrity | Test 8 disk-write verification | mitigate | Cross-check ctime of the edited file with `ls -la --time=ctime` immediately after the human reports save — catches a UI that flashes "saved" without writing |
| T-28.1-02-02 | Repudiation | Human verdict captured incorrectly | mitigate | Record verdicts to the file one at a time, not batched — survives mid-session interruption and lets the human re-read what was captured |
| T-28.1-02-03 | DoS | Save test corrupts a real campaign file | accept | The Save test asks the human to edit a file of their choice and they own the rollback; this is a single-user dev tool with git-tracked data |
</threat_model>

<verification>
- All 11 tests have a verdict recorded (grep gate above)
- Frontmatter `status: complete` is present
- Summary counts sum to 11
- For Test 8 (save), file ctime evidence is captured as a note
</verification>

<success_criteria>
- 28-UAT.md is no longer in `status: testing` — it is `status: complete`
- The audit gap "Phase 28 UAT pending — 11 tests ALL PENDING" is closed
- Plan 28.1-03 can now author 28-VERIFICATION.md grounded in real human results
</success_criteria>

<output>
Create `.planning/phases/28.1-close-gap-d-10-data-changed-sse-consumer-phase-28-uat-verifi/28.1-02-SUMMARY.md` when done. Include the final pass/fail/blocked counts and a short note for each failure (if any).
</output>
