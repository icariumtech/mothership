---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 06
type: execute
wave: 5
depends_on: [24-03]
files_modified:
  - /home/gjohnson/mothership/janus-skills/install.sh
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "install.sh --global and --project modes symlink ALL 8 skills/janus-*/ subdirectories (live smoke test reports 'Skills installed: 8')"
    - "install.sh --mcp-config writes settings.json atomically — a process interruption during write never truncates an existing settings.json"
    - "install.sh runs on macOS default userland (no coreutils) without failing on realpath: the script resolves symlink targets via a portable shim"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/install.sh"
      provides: "D-08/D-09/D-10 installer — fixed skills glob, atomic settings write, portable realpath"
      contains: "skills/janus-"
  key_links:
    - from: "install.sh"
      to: "skills/janus-*/"
      via: "for-loop glob with * wildcard"
      pattern: "skills/janus-\"\\*/"
    - from: "install.sh --mcp-config python heredoc"
      to: ".claude/settings.json"
      via: "tempfile.mkstemp + os.replace (atomic rename)"
      pattern: "os\\.replace"
    - from: "install.sh symlink loops"
      to: "absolute target paths"
      via: "portable _realpath shim (python3 fallback)"
      pattern: "_realpath|python3 -c .*os\\.path\\.realpath"
---

<objective>
Fix three install.sh defects from the Phase 24 verification + review pass. All three live in the same file so they ship in one plan.

1. **CR-01 (BLOCKER):** Line 148 glob `"$REPO_DIR/skills/janus-"/` is missing the `*` wildcard. Bash expands it as a literal path that does not exist; `[[ -d ]] || continue` silently skips it; `Skills installed: 0`. One-character fix: insert `*` before the trailing `/`.

2. **CR-02 (WARNING, fix opportunistically):** The python3 heredoc at lines 226-228 writes `settings.json` non-atomically (`open(..., "w")` then `json.dump(...)`). A crash between truncation and dump completion destroys the user's existing MCP server entries. Replace with `tempfile.mkstemp` + `os.replace`.

3. **WR-06 (WARNING, fix opportunistically):** `realpath` calls at lines 139 and 152 are GNU-coreutils-only — on macOS default userland they fail under `set -euo pipefail`. Add a portable `_realpath` shim using `python3 -c "import os; print(os.path.realpath(...))"` (python3 is already required by `--mcp-config`).

Purpose: Restore the installer to functional state so all 8 skills install and settings.json crash safety is preserved. Output: One modified `install.sh` and one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-VERIFICATION.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-REVIEW.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-03-SUMMARY.md

<interfaces>
install.sh structure (current — pre-fix):

Line 13:  REPO_DIR resolved via `cd "$(dirname "${BASH_SOURCE[0]}")" && pwd` (already absolute)
Line 119: RESOURCES_DIR="$HOME/.claude/janus-skills/resources"

Resources loop (working, but uses realpath):
  Line 136-141:
    for f in "$REPO_DIR/resources/"*.md; do
      [[ -e "$f" ]] || continue
      target="$RESOURCES_DIR/$(basename "$f")"
      ln -sfn "$(realpath "$f")" "$target"      # WR-06: realpath GNU-only
      RESOURCE_COUNT=$((RESOURCE_COUNT + 1))
    done

Skills loop (BROKEN — CR-01):
  Line 147-154:
    for skill_dir in "$REPO_DIR/skills/janus-"/; do   # CR-01: missing * before /
      [[ -d "$skill_dir" ]] || continue
      skill_name="$(basename "$skill_dir")"
      target="$SKILLS_DIR/$skill_name"
      ln -sfn "$(realpath "$skill_dir")" "$target"    # WR-06: realpath again
      SKILL_COUNT=$((SKILL_COUNT + 1))
    done

Skills note (line 158-160): NOTE message printed when SKILL_COUNT==0. Predates plans 04/05 populating the directory. Remove or relax once CR-01 is fixed (skills directory is now permanently populated).

Python heredoc (CR-02 — lines 193-231):
  - python3 - "$SETTINGS_FILE" "$REPO_DIR/mcp-config-template.json" "$HOMELAB_IP" <<'PYEOF'
  - Loads existing settings.json (or empty dict)
  - Ensures mcpServers dict
  - Loads template, replaces HOMELAB_IP in url
  - existing["mcpServers"]["JanusGM"] = janus_entry
  - Lines 226-228 (non-atomic):
      with open(settings_path, "w") as fh:
        json.dump(existing, fh, indent=2)
        fh.write("\n")
  - Trailing print("MCP config written to {}".format(settings_path))

Atomic write replacement (CR-02 fix template — from 24-REVIEW.md):
  import tempfile, os
  tmp_fd, tmp_path = tempfile.mkstemp(dir=os.path.dirname(settings_path), prefix=".settings_tmp_")
  try:
      with os.fdopen(tmp_fd, "w") as fh:
          json.dump(existing, fh, indent=2)
          fh.write("\n")
      os.replace(tmp_path, settings_path)
  except:
      os.unlink(tmp_path)
      raise

Portable realpath shim (WR-06 fix template — from 24-REVIEW.md):
  _realpath() { python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "$1"; }
  # then ln -sfn "$(_realpath "$f")" "$target"

Pre-flight note: install.sh already requires python3 only when --mcp-config is set (line 168).
WR-06's python3 fallback would extend that dependency to the general install case. Acceptable tradeoff
per 24-REVIEW.md: python3 is universally available on dev workstations including macOS (system
python3 ships with Xcode CLT). If the shim is preferred over a python3 dependency, the alternative
mentioned in 24-REVIEW.md is to drop the realpath call entirely since `$REPO_DIR` is already
absolute — `$f` (a glob match against `$REPO_DIR/...`) is therefore already absolute, and
`$skill_dir` (basename "%s/" with trailing slash) needs only the trailing slash trimmed.

Selected approach for this plan: drop realpath entirely (simpler, zero new dependency).
- For resources: `ln -sfn "$f" "$target"` — `$f` is already absolute (REPO_DIR-anchored glob)
- For skills: strip trailing slash via `"${skill_dir%/}"` before ln, since `$skill_dir` will be
  absolute (also REPO_DIR-anchored) — use that stripped path as the link target
- `basename` already works correctly on a trailing-slash path; no change needed there

Verification commands (existing infrastructure):
- `bash -n install.sh` — syntax check
- `bash install.sh --help` — exits 0
- `bash install.sh --project /tmp/janus-verify-test` — should report `Skills installed: 8`
- `find /tmp/janus-verify-test/.claude/skills -name SKILL.md | wc -l` — should equal 8
- For atomic write: confirm `os.replace` literal appears in script + a smoke test against a real
  settings.json that uses --mcp-config (manual — skip in automated verify, the static grep is enough)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix install.sh — skills glob (CR-01), atomic settings write (CR-02), portable realpath (WR-06)</name>
  <files>
    /home/gjohnson/mothership/janus-skills/install.sh
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/install.sh
  </read_first>
  <action>
    Apply three edits to `/home/gjohnson/mothership/janus-skills/install.sh`. Do not refactor unrelated code.

    **Edit 1 — CR-01 skills glob (line ~148):**
    Change the for-loop glob from `"$REPO_DIR/skills/janus-"/` to `"$REPO_DIR/skills/janus-"*/`. The single missing `*` is the entire bug. The trailing `/` after the `*` is intentional — it restricts the glob to directories only.

    **Edit 2 — WR-06 drop realpath (lines ~139, ~152):**
    Remove both `realpath` invocations entirely. The variables already hold absolute paths because `$REPO_DIR` is computed via `cd ... && pwd` at line 13.

    For the resources loop (line ~139):
    - Replace `ln -sfn "$(realpath "$f")" "$target"` with `ln -sfn "$f" "$target"`.

    For the skills loop (line ~152):
    - The glob produces paths with a trailing slash (e.g. `/repo/skills/janus-add-npc/`). Trim the trailing slash for the symlink target so the link points at the directory, not a path with a stray slash that some `ln` versions reject. Use parameter expansion: `link_target="${skill_dir%/}"`, then `ln -sfn "$link_target" "$target"`.
    - `basename "$skill_dir"` already handles the trailing slash correctly — leave that line alone.

    **Edit 3 — CR-02 atomic settings.json write (python heredoc, lines ~226-228):**
    Inside the `python3 - ... <<'PYEOF' ... PYEOF` block, replace the non-atomic write with a tempfile-and-rename pattern. The exact replacement:

    Before (lines 225-228):
    ```
    # Write back with 2-space indentation
    with open(settings_path, "w") as fh:
      json.dump(existing, fh, indent=2)
      fh.write("\n")
    ```

    After:
    ```
    # Write back atomically (tempfile + rename — survives crash mid-write)
    import tempfile
    tmp_fd, tmp_path = tempfile.mkstemp(dir=os.path.dirname(settings_path) or ".",
                                         prefix=".settings_tmp_")
    try:
      with os.fdopen(tmp_fd, "w") as fh:
        json.dump(existing, fh, indent=2)
        fh.write("\n")
      os.replace(tmp_path, settings_path)
    except Exception:
      try:
        os.unlink(tmp_path)
      except OSError:
        pass
      raise
    ```

    Notes:
    - `os` is already imported at the top of the heredoc (line ~196). `tempfile` is not — `import tempfile` must be added inline at the start of the new block (Python allows local imports). Use the `or "."` fallback for `dirname()` so the heredoc still works if `settings_path` is given without a directory component.
    - `os.replace` is atomic on POSIX (rename within same filesystem) and on Windows (replaces target).
    - The `except Exception` block deletes the temp file on any failure; the bare `except OSError: pass` on `unlink` handles the case where `mkstemp` succeeded but `os.fdopen` failed before any path was written (in which case the file may already be gone, or the FD close already cleaned up).
    - DO NOT change indentation style of surrounding lines (the file uses 2-space indent inside the heredoc — keep it).

    **Cleanup — empty-skills note (line ~158-160):**
    The block:
    ```
    if [[ $SKILL_COUNT -eq 0 ]]; then
      echo "NOTE: No skill directories found yet (skills/ is empty — run after plans 04/05 populate it)."
    fi
    ```
    The reference to "plans 04/05" is stale (those plans shipped). Replace the message body with:
    `echo "NOTE: No skill directories found in $REPO_DIR/skills/ (expected skills/janus-*/ subdirectories)."` Leave the `if` guard intact — it still serves as a defensive check for an empty repo checkout.

    **Verification after edits:**
    1. `bash -n /home/gjohnson/mothership/janus-skills/install.sh` — syntax check must pass.
    2. `bash /home/gjohnson/mothership/janus-skills/install.sh --help` — exits 0 unchanged.
    3. Smoke test the glob fix:
       ```
       rm -rf /tmp/janus-verify-test && mkdir -p /tmp/janus-verify-test
       bash /home/gjohnson/mothership/janus-skills/install.sh --project /tmp/janus-verify-test
       ```
       Output must contain `Skills installed: 8 skill(s)` (or higher if more skills added later).
       `find /tmp/janus-verify-test/.claude/skills -name SKILL.md | wc -l` must equal `8`.
    4. Verify settings.json atomic block: `grep -q 'os.replace' /home/gjohnson/mothership/janus-skills/install.sh` and `grep -q 'tempfile.mkstemp' /home/gjohnson/mothership/janus-skills/install.sh`.
    5. Verify realpath is gone: `! grep -q 'realpath' /home/gjohnson/mothership/janus-skills/install.sh` (zero matches).
    6. Clean up: `rm -rf /tmp/janus-verify-test`.

    Do NOT commit yet — Task 2 commits.
  </action>
  <verify>
    <automated>bash -n /home/gjohnson/mothership/janus-skills/install.sh && bash /home/gjohnson/mothership/janus-skills/install.sh --help > /dev/null && rm -rf /tmp/janus-verify-test && mkdir -p /tmp/janus-verify-test && bash /home/gjohnson/mothership/janus-skills/install.sh --project /tmp/janus-verify-test | grep -E "Skills installed: [1-9]" && [ "$(find /tmp/janus-verify-test/.claude/skills -name SKILL.md 2>/dev/null | wc -l)" = "8" ] && grep -q 'os.replace' /home/gjohnson/mothership/janus-skills/install.sh && grep -q 'tempfile.mkstemp' /home/gjohnson/mothership/janus-skills/install.sh && ! grep -q 'realpath' /home/gjohnson/mothership/janus-skills/install.sh && grep -E 'for skill_dir in "\$REPO_DIR/skills/janus-"\*/' /home/gjohnson/mothership/janus-skills/install.sh && rm -rf /tmp/janus-verify-test</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0
    - Skills glob on the for-loop line literally matches `"$REPO_DIR/skills/janus-"*/` (verified by grep with the explicit pattern above)
    - `bash install.sh --project /tmp/janus-verify-test` reports `Skills installed: 8 skill(s)` (or 8+) and produces 8 SKILL.md files in the target tree
    - `tempfile.mkstemp` and `os.replace` literals both appear in the python heredoc
    - The substring `realpath` does not appear anywhere in install.sh (zero matches)
    - `--help` still works (exit 0)
    - The empty-skills NOTE no longer references "plans 04/05"
    - No other code paths in install.sh were modified (diff confined to the three edit sites + the NOTE message)
  </acceptance_criteria>
  <done>
    install.sh installs all 8 skills, writes settings.json atomically, and resolves symlink targets without depending on GNU realpath. Live smoke test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit install.sh gap closure fixes</name>
  <files>
    (no new files — commits artifact from Task 1)
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/install.sh
  </read_first>
  <action>
    From `/home/gjohnson/mothership/janus-skills`:

    1. Stage the modified installer:
       `git add install.sh`

    2. Confirm only install.sh is staged: `git status --porcelain` should show a single `M  install.sh` line (or `MM` if there were any unstaged tweaks — in which case re-add). No other staged/untracked files belong in this commit.

    3. Commit with this exact message (use a heredoc to preserve formatting):
       ```
       fix(install): repair skills glob, add atomic settings write, drop realpath

       Three defects from the Phase 24 verification + review pass:

       - CR-01 (blocker): for-loop glob missing * wildcard meant 0 of 8
         skills were ever installed. Script exited 0 with "Skills installed: 0"
         and a stale NOTE about empty skills/ directory. One-character fix:
         skills/janus- -> skills/janus-*
       - CR-02 (warning): settings.json was written non-atomically; a crash
         between open() and json.dump() completion would truncate the user's
         existing MCP server config. Replaced with tempfile.mkstemp +
         os.replace pattern.
       - WR-06 (warning): realpath is GNU-coreutils-only and aborts on macOS
         default userland under set -euo pipefail. Dropped the calls entirely
         since REPO_DIR is already absolute from cd ... && pwd at script
         entry.

       Live smoke test now reports "Skills installed: 8 skill(s)" with all
       8 SKILL.md files symlinked into the target .claude/skills/ tree.

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```

    4. Verify clean tree: `git status --porcelain` returns empty.
    5. Confirm the new commit subject begins with `fix(install):` by inspecting `git log --oneline -1`.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && git log --oneline -1 | grep -q "fix(install)" && [ -z "$(git status --porcelain)" ] && git log --oneline -1 | grep -qE "skills glob|atomic settings|realpath"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/gjohnson/mothership/janus-skills log --oneline -1` subject begins with `fix(install):`
    - `git -C /home/gjohnson/mothership/janus-skills status --porcelain` is empty
    - Commit body mentions all three issue IDs: CR-01, CR-02, WR-06
    - No files outside `install.sh` were touched by this commit (`git show --name-only HEAD` lists exactly `install.sh`)
  </acceptance_criteria>
  <done>
    install.sh gap closure committed atomically. Working tree clean.
  </done>
</task>

</tasks>

<verification>
- `bash -n install.sh` passes
- `bash install.sh --project /tmp/janus-verify-test` reports `Skills installed: 8 skill(s)`
- `find /tmp/janus-verify-test/.claude/skills -name SKILL.md | wc -l` returns 8
- `grep -c realpath install.sh` returns 0
- `grep -q 'tempfile.mkstemp\|os.replace' install.sh` succeeds
- Git log shows a single new commit with `fix(install):` subject
</verification>

<success_criteria>
- VERIFICATION.md truth #4 ("install.sh implements --global and --project modes") flips from PARTIAL to VERIFIED
- Anti-pattern table entries for line 148 (CR-01), line 226-228 (CR-02), and line 139/152 (WR-06) all closed
- No regression in --mcp-config flow — manual smoke test still successful (write a JanusGM block into an existing settings.json containing an unrelated mcpServers entry; the unrelated entry survives the atomic rename)
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-06-SUMMARY.md` recording:
- Commit hash
- Per-edit summary: CR-01 line number + before/after snippet (1 line each), CR-02 line range + atomic-block snippet, WR-06 removed lines + replacement
- Smoke test output: full line `Skills installed: 8 skill(s) -> /tmp/janus-verify-test/.claude/skills`
- Confirmation that `grep -c realpath install.sh == 0` and that `grep -E 'tempfile\.mkstemp|os\.replace' install.sh` finds both literals
</output>
