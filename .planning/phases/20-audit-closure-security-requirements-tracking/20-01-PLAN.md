---
phase: 20-audit-closure-security-requirements-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - terminal/views.py
  - src/services/encounterApi.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "POST /api/gm/ship/set-location/ returns 302/redirect for unauthenticated requests (matches Phase 11 api_ship_update_integrity pattern)"
    - "POST /api/bridge-selection/ remains accepting unauthenticated requests (player terminal must continue to work)"
    - "encounterApi module no longer exports getRoomVisibility or setRoomVisibility, but the function definitions remain in the file"
    - "TypeScript compilation succeeds (npm run typecheck exits 0)"
  artifacts:
    - path: "terminal/views.py"
      provides: "@login_required decorator added to api_set_ship_location; api_bridge_selection docstring expanded to mark intentionally public"
      contains: "def api_set_ship_location"
    - path: "src/services/encounterApi.ts"
      provides: "Export object with getRoomVisibility/setRoomVisibility entries removed; functions retained"
      contains: "async function getRoomVisibility"
  key_links:
    - from: "terminal/views.py api_set_ship_location"
      to: "django.contrib.auth.decorators.login_required"
      via: "@login_required decorator"
      pattern: "@login_required\\ndef api_set_ship_location"
    - from: "src/services/encounterApi.ts showAllRooms (line ~128)"
      to: "src/services/encounterApi.ts setRoomVisibility (line ~115)"
      via: "module-internal function reference"
      pattern: "return setRoomVisibility"
---

<objective>
Close the two v1.0 audit gaps that touch source code: secure `api_set_ship_location` with `@login_required`, document `api_bridge_selection` as intentionally public (per R-02 / RESEARCH.md), and remove the unnecessary `getRoomVisibility` / `setRoomVisibility` exports from `encounterApi.ts` (per D-03).

Purpose: Eliminate the unauthenticated GM-write security gap on `/api/gm/ship/set-location/` and shrink the frontend module's public surface area without breaking internal callers. Player terminal callers of `api_bridge_selection` must continue working; the audit-flagged "low-severity" status of that endpoint is correct and is documented in code, not fixed with auth.

Output: Two surgical edits — one Python file (decorator + docstring update), one TypeScript file (export object trim).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/20-audit-closure-security-requirements-tracking/20-CONTEXT.md
@.planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md
@.planning/phases/11-close-functional-security-gaps/11-VERIFICATION.md

<interfaces>
<!-- Phase 11 reference pattern for @login_required on a GM write endpoint. -->
<!-- Verified at terminal/views.py line 1898–1899. -->

```python
# terminal/views.py — Phase 11 pattern (api_ship_update_integrity)
@login_required
def api_ship_update_integrity(request):
    """GM only - updates runtime overrides in ActiveView."""
```

```python
# terminal/views.py current state at line 705–706 (target of this plan's edit)
@csrf_exempt
def api_set_ship_location(request):
    """GM action: set the ship's current galactic position. Writes to ship.yaml + broadcasts SSE."""
```

```python
# terminal/views.py current state at line 672–673 (NOT to receive @login_required — player terminal calls it)
@csrf_exempt
def api_bridge_selection(request):
    """
    Public API endpoint to update the player's current bridge map selection.
    Called by the player terminal when navigating the bridge galaxy/system/orbit map.
    POST: { location_slug?: string, tab?: string }
    """
```

<!-- encounterApi.ts current export object at lines 235–252 -->
```typescript
// BEFORE (current)
export const encounterApi = {
  getMapData,
  getAllDecks,
  switchLevel,
  toggleRoom,
  getRoomVisibility,    // line 240 — REMOVE
  setRoomVisibility,    // line 241 — REMOVE
  showAllRooms,
  hideAllRooms,
  setDoorStatus,
  placeToken,
  moveToken,
  removeToken,
  updateTokenStatus,
  clearAllTokens,
  getTokenImages,
  togglePortrait,
};
```

<!-- Internal callers that MUST keep working (functions stay in the file): -->
- encounterApi.ts line 128: `return setRoomVisibility(visibility);` inside `showAllRooms()`
- encounterApi.ts line 137: `return setRoomVisibility(visibility);` inside `hideAllRooms()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add @login_required to api_set_ship_location and document api_bridge_selection as intentionally public</name>
  <files>terminal/views.py</files>
  <read_first>
    - terminal/views.py (current state of both endpoints, lines 670–730)
    - .planning/phases/11-close-functional-security-gaps/11-VERIFICATION.md (Phase 11 pattern: @login_required alone on api_ship_update_integrity at line 1744 — keep that style)
    - .planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md (Pattern 1 + Pattern 2 — confirms api_bridge_selection must NOT receive @login_required)
  </read_first>
  <action>
    Two edits in `terminal/views.py`:

    1. **Add `@login_required` to `api_set_ship_location`** (per audit security gap closure, mirroring Phase 11):
       - At line 705 (currently `@csrf_exempt`), insert a new line above so the decorator stack reads:
         ```python
         @csrf_exempt
         @login_required
         def api_set_ship_location(request):
         ```
       - `@csrf_exempt` is retained because Axios may not always send the CSRF token; `@login_required` is the innermost decorator (runs first at call time) and is the actual security control. Django evaluates decorators bottom-up, so unauthenticated callers get redirected before the body runs.
       - The `from django.contrib.auth.decorators import login_required` import already exists at line 2 of the file (do NOT re-add).

    2. **Document `api_bridge_selection` as intentionally public** (per RESEARCH Pattern 2 — adding `@login_required` would break the unauthenticated player terminal):
       - The endpoint currently has only `@csrf_exempt` at line 672. Do NOT add `@login_required`.
       - Replace the existing docstring at lines 674–678 with an expanded version that explicitly marks it intentionally public and states the rationale. The new docstring (exact text):
         ```
         """
         Public API endpoint to update the player's current bridge map selection.

         INTENTIONALLY UNAUTHENTICATED: Called by the player terminal at
         `/terminal/`, which is itself an unauthenticated route. Adding
         `@login_required` here would return a 302 redirect for player
         sessions and break bridge map navigation.

         Writes ephemeral in-memory state only (view_slug / bridge_tab /
         bridge_map_mode / bridge_label) — no persistent data side effect.
         The audit flagged this as low-severity (no persistent write); the
         resolution is documentation, not authentication. See Phase 20
         RESEARCH.md Pattern 2 for the full caller list.

         Called by the player terminal when navigating the bridge galaxy/system/orbit map.
         POST: { location_slug?: string, tab?: string, map_mode?: string, label?: string }
         """
         ```
       - Keep the rest of the function body unchanged.

    Both changes are surgical — do not refactor any other code in this file.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon &amp;&amp; grep -B1 "^def api_set_ship_location" terminal/views.py | grep -c "@login_required"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -B2 "^def api_set_ship_location" terminal/views.py` outputs both `@csrf_exempt` AND `@login_required` (the `@login_required` line is immediately above `def`)
    - `grep -B2 "^def api_bridge_selection" terminal/views.py` outputs `@csrf_exempt` but does NOT include `@login_required` (Pattern 2)
    - `grep -c "INTENTIONALLY UNAUTHENTICATED" terminal/views.py` outputs `1` (new docstring marker present)
    - `grep -c "^from django.contrib.auth.decorators import login_required" terminal/views.py` outputs `1` (existing import not duplicated)
    - `python -c "import ast; ast.parse(open('terminal/views.py').read())"` exits 0 (file parses as valid Python)
    - The string `def api_set_ship_location(request):` appears exactly once (function not duplicated)
    - The string `def api_bridge_selection(request):` appears exactly once
  </acceptance_criteria>
  <done>
    `api_set_ship_location` has both `@csrf_exempt` and `@login_required` decorators; `api_bridge_selection` has only `@csrf_exempt` and a docstring containing "INTENTIONALLY UNAUTHENTICATED" explaining why. File parses as valid Python.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove getRoomVisibility/setRoomVisibility from encounterApi export object</name>
  <files>src/services/encounterApi.ts</files>
  <read_first>
    - src/services/encounterApi.ts (entire file; specifically lines 100–145 for the function definitions and internal callers, and lines 235–252 for the export object)
    - .planning/phases/20-audit-closure-security-requirements-tracking/20-CONTEXT.md (D-03 — exports only, functions stay)
    - .planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md (Pitfall 4 — do NOT delete the function definitions)
  </read_first>
  <action>
    In `src/services/encounterApi.ts`, modify the `export const encounterApi = { ... }` object (currently spanning lines 235–252).

    **Remove exactly two lines from the object body:**
    - Line 240: `  getRoomVisibility,`
    - Line 241: `  setRoomVisibility,`

    **Do NOT modify:**
    - The function definition at line 107 (`async function getRoomVisibility`) — kept because nothing external uses it now, but removing it is out of scope per D-03.
    - The function definition at line 115 (`async function setRoomVisibility`) — kept because internal callers `showAllRooms` (line 128) and `hideAllRooms` (line 137) call it via `return setRoomVisibility(visibility);`.
    - Any other property in the export object.
    - The `export type { DeckWithRooms, AllDecksResponse };` line at the bottom.

    Result: the export object goes from 17 entries to 15 entries; the file's line count drops by 2.

    Per D-03, this trims unnecessary public API surface without breaking internal callers (which use the function name directly, not via `encounterApi.setRoomVisibility(...)`).
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (no TypeScript errors anywhere in src/)
    - `grep -c "^async function getRoomVisibility" src/services/encounterApi.ts` outputs `1` (function definition still present)
    - `grep -c "^async function setRoomVisibility" src/services/encounterApi.ts` outputs `1` (function definition still present)
    - `grep -c "  getRoomVisibility," src/services/encounterApi.ts` outputs `0` (export entry removed — note the leading two-space indent; this matches the export object syntax and excludes the function definition line)
    - `grep -c "  setRoomVisibility," src/services/encounterApi.ts` outputs `0` (export entry removed)
    - `grep -c "return setRoomVisibility(visibility);" src/services/encounterApi.ts` outputs `2` (both internal callers `showAllRooms` and `hideAllRooms` still reference the function)
    - `grep -c "^export const encounterApi = {" src/services/encounterApi.ts` outputs `1` (export object intact)
  </acceptance_criteria>
  <done>
    Export object has `getRoomVisibility` and `setRoomVisibility` entries removed but the underlying function definitions and their two internal callers (`showAllRooms`, `hideAllRooms`) are untouched. `npm run typecheck` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player browser → Django (`api_bridge_selection`) | Untrusted input from unauthenticated player terminal — accepted by design (ephemeral state only) |
| GM browser → Django (`api_set_ship_location`) | Authenticated GM session required after this plan — was previously open |
| Frontend module boundary (`encounterApi.ts` exports) | Public surface narrowed; internal callers retained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `terminal/views.py api_set_ship_location` | mitigate | Add `@login_required` decorator (per Phase 11 pattern at views.py line 1898) so unauthenticated POSTs are redirected to `/login/` before the handler writes to `ship.yaml` or broadcasts SSE |
| T-20-02 | Tampering | `terminal/views.py api_bridge_selection` | accept | Player terminal is unauthenticated by design; endpoint writes only ephemeral in-memory state with no persistent side effect; documented as INTENTIONALLY UNAUTHENTICATED in updated docstring |
| T-20-03 | Information Disclosure | `src/services/encounterApi.ts` export object | mitigate | Remove `getRoomVisibility` and `setRoomVisibility` exports — functions are no longer reachable from outside the module, reducing the JS module's attack/misuse surface (per D-03) |
</threat_model>

<verification>
## Phase-level checks (run after both tasks complete)

1. `cd /home/gjohnson/mothership/charon && npm run typecheck` exits 0.
2. `cd /home/gjohnson/mothership/charon && python -c "import ast; ast.parse(open('terminal/views.py').read())"` exits 0.
3. `grep -B2 "^def api_set_ship_location" terminal/views.py` shows BOTH `@csrf_exempt` and `@login_required`.
4. `grep -B2 "^def api_bridge_selection" terminal/views.py` shows ONLY `@csrf_exempt` (no `@login_required`).
5. `grep -c "INTENTIONALLY UNAUTHENTICATED" terminal/views.py` outputs `1`.
6. `grep -c "  getRoomVisibility," src/services/encounterApi.ts` outputs `0`.
7. `grep -c "  setRoomVisibility," src/services/encounterApi.ts` outputs `0`.
8. `grep -c "^async function setRoomVisibility" src/services/encounterApi.ts` outputs `1` (function intact).
</verification>

<success_criteria>
- The audit's `api_set_ship_location` security gap is closed: unauthenticated POSTs are blocked by `@login_required`.
- The audit's `api_bridge_selection` finding is documented as accepted (with rationale in the docstring).
- `getRoomVisibility` and `setRoomVisibility` are no longer exported from the `encounterApi` module, but their internal callers continue to work and the TypeScript compiler reports no errors.
- No other behavior changes; no other files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/20-audit-closure-security-requirements-tracking/20-01-SUMMARY.md`
</output>
