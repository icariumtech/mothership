---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md
  - /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
  - /home/gjohnson/mothership/janus-skills/resources/schema-ships.md
  - /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md
  - /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Each of the 5 schema resource files is a condensed schema reference (not a full copy of DATA_DIRECTORY_GUIDE)"
    - "Every required and optional field documented in resources matches a field present in live data examples or DATA_DIRECTORY_GUIDE"
    - "Each resource file lists the MCP path conventions for the entities it documents (paths relative to data/)"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/resources/schema-campaign.md"
      provides: "Crew, NPC, corporation, standby schemas — consumed by janus-add-npc"
      contains: "ACTIVE | INACTIVE | DECEASED | UNKNOWN"
    - path: "/home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md"
      provides: "star_map, system_map, orbit_map, location.yaml — consumed by add-location/add-system/add-body/update-galaxy/generate-context"
      contains: "location_slug"
    - path: "/home/gjohnson/mothership/janus-skills/resources/schema-ships.md"
      provides: "Ship location.yaml + slug-pointer model + orbit injection — consumed by add-ship"
      contains: "body_slug"
    - path: "/home/gjohnson/mothership/janus-skills/resources/schema-encounters.md"
      provides: "Deckplan, rooms, doors, POIs — consumed by add-location/add-body for deckplan stubs"
      contains: "GridRoom"
    - path: "/home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md"
      provides: "janus.yaml format — consumed by generate-context + session-prep"
      contains: "generated:"
  key_links:
    - from: "resources/schema-*.md"
      to: "DATA_DIRECTORY_GUIDE.md sections"
      via: "field-by-field condensation (NOT a copy)"
      pattern: "id:|location_slug:|body_slug:"
---

<objective>
Author the 5 condensed schema resource files under `~/mothership/janus-skills/resources/`. These are NOT copies of DATA_DIRECTORY_GUIDE.md — they are condensed (~150–300 line) references listing file paths, YAML field names, required vs optional fields, naming conventions, and the MCP path conventions per D-04, D-05, D-06.

Purpose: Skills @-include only the slice they need. Schema bundling is the core context-efficiency mechanism of this phase. Resources must be accurate (every field present in DATA_DIRECTORY_GUIDE or the live example files) and minimal (no prose narrative, no design philosophy).

Output: 5 markdown files in `resources/` ready to be referenced from SKILL.md frontmatter via `@$HOME/.claude/janus-skills/resources/schema-*.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-01-SUMMARY.md
@DATA_DIRECTORY_GUIDE.md
@mcp_server.py
@data/galaxy/anchor-system/veil-station/location.yaml
@data/galaxy/anchor-system/veil-station/janus.yaml
@data/campaign/crew/elena_vasquez.yaml
@data/campaign/npcs/captain_harrow.yaml
@data/ships/patrol_gunboat/location.yaml

<interfaces>
<!-- MCP tool path convention (verified mcp_server.py): all paths are relative to data/ -->
- list_files(dir)     — dir relative to data/ (e.g., "campaign/npcs", "galaxy/anchor-system")
- read_file(path)     — path relative to data/ (e.g., "galaxy/star_map.yaml")
- write_file(path, content) — path relative to data/; triggers SSE broadcast
- get_data_schema()   — returns full schema, used as a fallback per D-07
- get_session_context() — returns live state for session-prep

<!-- Field sets verified in RESEARCH.md Section "JANUS Data Schema Reference (Verified)" -->
Status enum (NPCs, locations): ACTIVE | INACTIVE | DECEASED | UNKNOWN | OPERATIONAL
Location type enum: ship | station | planet | moon | system
NPC class enum: Teamster | Scientist | Marine | Android
Stats range: 2–10 (5 average)
Saves range: 0–99 (percentage)
Slug rules: lowercase, hyphens for location slugs (tau-ceti-f), underscores for character ids (captain_harrow)

<!-- Critical pitfall references (RESEARCH.md "Common Pitfalls") -->
P1: body_slug case must match directory name exactly (lowercase-hyphenated)
P2: id field must match filename stem exactly
P5: body_slug must target a planet (direct system child), not a moon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write schema-campaign.md and schema-janus-context.md</name>
  <files>
    /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md
    /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/DATA_DIRECTORY_GUIDE.md
    /home/gjohnson/mothership/charon/data/campaign/crew/elena_vasquez.yaml
    /home/gjohnson/mothership/charon/data/campaign/npcs/captain_harrow.yaml
    /home/gjohnson/mothership/charon/data/galaxy/anchor-system/veil-station/janus.yaml
  </read_first>
  <action>
    Create both files in `/home/gjohnson/mothership/janus-skills/resources/`. Each must be a CONDENSED reference per D-06 — list fields with one-line descriptions, not prose. Target 150–250 lines per file.

    **schema-campaign.md** — must document, in this order, with these section headings:
    1. `# Campaign Schema Reference`
    2. `## MCP Path Conventions` — list the data/campaign/ directories: `campaign/crew/`, `campaign/npcs/`, `campaign/ship/`, `campaign/corporation/`, `campaign/standby/`. State that all MCP paths are relative to `data/`.
    3. `## NPC YAML (data/campaign/npcs/<id>.yaml)` with two sub-tiers per the CONTEXT.md NPC tier note:
       - "Background NPC (minimal)" — list the 6 required fields: `id`, `name`, `role`, `faction`, `status`, `description` + optional `location`, `portrait`. Show id derivation rule (lowercase, underscores; must match filename stem — cite Pitfall P2).
       - "Combatant/significant NPC (full)" — add `class`, `stats` (strength/speed/intellect/combat), `saves` (sanity/fear/body), `stress`, `health` (current/max), `wounds`, `armor`, `background`, `motivation`.
       - Status enum: `ACTIVE | INACTIVE | DECEASED | UNKNOWN`
    4. `## Crew YAML (data/campaign/crew/<id>.yaml)` — same schema as full NPC; id must match filename stem.
    5. `## Corporation YAML (data/campaign/corporation/)` — list fields per DATA_DIRECTORY_GUIDE.md (read the corporation section and condense).
    6. `## Standby YAML (data/campaign/standby/)` — list the standby message fields per DATA_DIRECTORY_GUIDE.md.
    7. `## Common Pitfalls` — bullet list referencing P2 (id/filename match) verbatim summary.
    8. One short YAML example block per tier (background NPC, full NPC) showing minimal valid content.

    **schema-janus-context.md** — much shorter (~80–120 lines):
    1. `# JANUS Context Schema Reference`
    2. `## Purpose` — one paragraph: janus.yaml is generated per-location, holds AI-curated GM context blob.
    3. `## Path Convention` — `data/galaxy/<system>/.../janus.yaml` (always sibling to location.yaml).
    4. `## Required Fields` — `generated` (ISO 8601 UTC timestamp string), `context` (multiline YAML literal block `|`).
    5. `## Context Body Conventions` — describe the structure from the veil-station/janus.yaml example (header line, status line, operational paragraph).
    6. One complete YAML example block based on the veil-station/janus.yaml content.

    Both files end with a trailing newline. Use Markdown headings, not HTML. Do NOT inline copy DATA_DIRECTORY_GUIDE prose — condense to field lists only.
  </action>
  <verify>
    <automated>test -f /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md && test -f /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md && grep -q "ACTIVE | INACTIVE | DECEASED | UNKNOWN" /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md && grep -q "campaign/npcs" /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md && grep -q "generated" /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md && grep -q "context" /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md</automated>
  </verify>
  <acceptance_criteria>
    - `schema-campaign.md` contains the literal heading `## NPC YAML (data/campaign/npcs/<id>.yaml)`
    - `schema-campaign.md` lists all six required NPC fields by name (verifiable: `grep -E "^- \\\`id\\\`|^- \\\`name\\\`|^- \\\`role\\\`|^- \\\`faction\\\`|^- \\\`status\\\`|^- \\\`description\\\`" schema-campaign.md` finds at least 6 lines)
    - `schema-campaign.md` documents the status enum as `ACTIVE | INACTIVE | DECEASED | UNKNOWN`
    - `schema-campaign.md` documents both NPC stat fields `strength`, `speed`, `intellect`, `combat` (verifiable via grep)
    - `schema-campaign.md` mentions id/filename match rule (Pitfall P2) — grep for `filename` finds a match
    - `schema-campaign.md` line count between 120 and 320 (`wc -l` — condensed but complete)
    - `schema-janus-context.md` contains the `generated` and `context` field names
    - `schema-janus-context.md` line count between 60 and 180 (`wc -l`)
    - Both files end with a newline character (`tail -c 1 ... | xxd` shows `0a`)
    - Neither file contains the string `"DATA_DIRECTORY_GUIDE"` (no leakage of full guide; condense, do not reference)
  </acceptance_criteria>
  <done>
    `schema-campaign.md` and `schema-janus-context.md` exist with the documented section structure, accurate field sets matching live data examples, and condensed prose per D-06.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write schema-galaxy.md and schema-ships.md</name>
  <files>
    /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
    /home/gjohnson/mothership/janus-skills/resources/schema-ships.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/DATA_DIRECTORY_GUIDE.md
    /home/gjohnson/mothership/charon/data/galaxy/anchor-system/veil-station/location.yaml
    /home/gjohnson/mothership/charon/data/ships/patrol_gunboat/location.yaml
  </read_first>
  <action>
    Create both files in `/home/gjohnson/mothership/janus-skills/resources/`. Condensed references per D-06.

    **schema-galaxy.md** (~250–350 lines — this is the largest resource because 4 skills depend on it):
    1. `# Galaxy Schema Reference`
    2. `## MCP Path Conventions` — `galaxy/star_map.yaml`, `galaxy/<system>/system_map.yaml`, `galaxy/<system>/<body>/orbit_map.yaml`, `galaxy/<system>/<body>/location.yaml`, `galaxy/<system>/<body>/<sub-location>/location.yaml`. State that paths are relative to `data/`.
    3. `## Slug Rules` — lowercase + hyphens (e.g., `tau-ceti-f`); must match directory name exactly (cite Pitfall P1: case mismatch silently breaks orbit injection).
    4. `## star_map.yaml (data/galaxy/star_map.yaml)` — list fields: `camera` (position/lookAt/fov), `systems[]` (name, position, color, size, type, label, location_slug, info{description, population}), `routes[]` (from, to, color, route_type, travel_time_days), `nebulae[]` (name, position, color, size, particle_count, opacity, type).
    5. `## system_map.yaml` — list: `star` (name/color/size), `camera`, `bodies[]` (name, type, location_slug, orbital_radius, orbital_period, size, color, clickable, has_orbit_map, info{description}). State: location_slug must match subdirectory name exactly.
    6. `## orbit_map.yaml` — list: `planet` (name/type/size/texture), `camera`, `moons[]` (name, location_slug, orbital_radius/period/angle, inclination, size, color, clickable, has_facilities). Note: only moons go here; ships/stations self-register via parent_type=orbit + body_slug.
    7. `## location.yaml (permanent installation — station/planet/moon)` — list fields from veil-station example: `name`, `type` (station|planet|moon|system), `parent_system`, `orbital_body`, `orbital_position`, `status` (OPERATIONAL etc.), `population`, `crew_capacity`, `established`, `description`, optional `lore`, optional `janus` block.
    8. `## location.yaml (system-level — data/galaxy/<system>/location.yaml)` — list the minimal fields a system root needs per DATA_DIRECTORY_GUIDE.
    9. `## Body type enum` — `ship | station | planet | moon | system`
    10. `## Common Pitfalls` — P1 (slug case), P5 (body_slug must target a planet, not a moon).
    11. Two short YAML example blocks (one star_map entry, one location.yaml).

    **schema-ships.md** (~150–220 lines):
    1. `# Ship Schema Reference`
    2. `## MCP Path Conventions` — `ships/<ship-slug>/location.yaml`, optional `ships/<ship-slug>/<deck>/location.yaml` for decks, optional `ships/<ship-slug>/map/` for deckplan.
    3. `## Slug-Pointer Model` — ships live under `data/ships/`; they self-register on orbit maps via `parent_type: orbit` + `body_slug` + `system_slug` in their `location.yaml`. The data loader injects the ship into the orbit view of the named planet.
    4. `## location.yaml (mobile ship — orbit-injected)` — list fields from patrol_gunboat example: `name`, `type: "ship"`, `description`, `status` (OPERATIONAL), `parent_type` (orbit | surface), `body_slug` (lowercase-hyphenated, exact match to planet dir), `system_slug` (exact match to system dir), `orbital{radius, period, angle, inclination, size, icon_type}`. icon_type enum: `ship | station | shipyard`.
    5. `## location.yaml (player ship — campaign/ship)` — minimal fields: `name`, `type: "ship"`, `slug`, `status`.
    6. `## Common Pitfalls` — P1 (slug case), P5 (body_slug only resolves to planets — direct system children, NOT moons; if user provides a moon slug, warn).
    7. One YAML example based on patrol_gunboat/location.yaml.

    Both files end with a trailing newline.
  </action>
  <verify>
    <automated>test -f /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md && test -f /home/gjohnson/mothership/janus-skills/resources/schema-ships.md && grep -q "star_map.yaml" /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md && grep -q "system_map.yaml" /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md && grep -q "orbit_map.yaml" /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md && grep -q "location_slug" /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md && grep -q "body_slug" /home/gjohnson/mothership/janus-skills/resources/schema-ships.md && grep -q "parent_type" /home/gjohnson/mothership/janus-skills/resources/schema-ships.md</automated>
  </verify>
  <acceptance_criteria>
    - `schema-galaxy.md` contains all four file names: `star_map.yaml`, `system_map.yaml`, `orbit_map.yaml`, `location.yaml`
    - `schema-galaxy.md` documents location type enum containing `ship`, `station`, `planet`, `moon`, `system` (all 5 tokens present)
    - `schema-galaxy.md` line count between 180 and 450
    - `schema-galaxy.md` mentions slug case mismatch warning (grep for `case` finds a match)
    - `schema-ships.md` contains exact tokens `body_slug`, `system_slug`, `parent_type`, `orbital`
    - `schema-ships.md` documents `icon_type` enum with all three values (`ship`, `station`, `shipyard`)
    - `schema-ships.md` line count between 100 and 280
    - `schema-ships.md` mentions Pitfall P5 (moon vs planet) — grep for `moon` and `planet` both find matches
    - Both files end with newline character
    - Neither file contains a fence-block copy >40 lines from DATA_DIRECTORY_GUIDE.md (no large copies — condensed only)
  </acceptance_criteria>
  <done>
    `schema-galaxy.md` and `schema-ships.md` exist with condensed schema references covering all four galaxy data files, slug rules, and the orbit injection model.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write schema-encounters.md + commit all 5 resources</name>
  <files>
    /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/DATA_DIRECTORY_GUIDE.md
  </read_first>
  <action>
    Create `/home/gjohnson/mothership/janus-skills/resources/schema-encounters.md` (~150–250 lines). Condensed deckplan reference per D-06.

    Sections (in order, with these exact headings):
    1. `# Encounter / Deckplan Schema Reference`
    2. `## Purpose` — one paragraph: deckplan YAML describes grid-based interior maps used by EncounterMapDisplay. Optional for any location.
    3. `## MCP Path Conventions` — deckplans live under the location they describe, typically `<location-path>/map/<deck-name>.yaml` (e.g., `galaxy/tau-ceti/somnus/map/deck1.yaml`, `ships/patrol_gunboat/map/main.yaml`). Paths relative to `data/`.
    4. `## Top-level fields` — `name`, `unit_size` (pixels per cell), `width`, `height` (grid dims), `rooms[]`, `corridors[]` (optional), `pois[]` (optional).
    5. `## GridRoom fields` — `id`, `name` (optional display label), `rects[]` (each: x, y, width, height, chamfer optional), `circle` (optional: cx, cy, r), `polygon` (optional: list of [x,y] pairs), `doors[]`.
    6. `## Door fields` — explicit position format: `{x, y, angle (0=horizontal, 90=vertical), type, status (CLOSED|OPEN|LOCKED|BROKEN)}`; legacy rect-only format: `{wall (north|south|east|west), position (0–1), type, status}`. Door id is synthetic: `${room.id}_door_${index}`.
    7. `## POI fields` — `id`, `name`, `icon` (lowercase name matching SVG file prefix — e.g., `reactor core`, `emergency capsule`), `x`, `y`, optional `description`.
    8. `## Corridor fields` — `id` (auto-generated `corridor_N` allowed), `rects[]`, `doors[]` — connect rooms.
    9. `## Tooling Note` — `tools/svg_to_map.py` (in charon repo) generates these files from Inkscape SVGs; key flags `--detect-doors`, `--grid-scale`, `--unit-size`. Skills do not run this — it is a manual GM workflow.
    10. One short YAML example showing a 2-room deckplan with one door.

    File must end with a newline.

    After writing this file (and verifying Tasks 1 and 2 wrote their 4 files), commit all 5 resources together. From `/home/gjohnson/mothership/janus-skills`:
    1. `git add resources/schema-campaign.md resources/schema-galaxy.md resources/schema-ships.md resources/schema-encounters.md resources/schema-janus-context.md`
    2. Also `git rm resources/.gitkeep` (the directory is now populated, .gitkeep no longer needed).
    3. Commit with message:
       ```
       docs(resources): add 5 condensed schema reference files

       Splits DATA_DIRECTORY_GUIDE into topic-specific resources (D-04, D-05, D-06):
       - schema-campaign.md (crew, NPC, corporation, standby)
       - schema-galaxy.md (star_map, system_map, orbit_map, location hierarchy)
       - schema-ships.md (ship location.yaml, slug-pointer, orbit injection)
       - schema-encounters.md (deckplan, rooms, doors, POIs)
       - schema-janus-context.md (janus.yaml format)

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```
  </action>
  <verify>
    <automated>test -f /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md && grep -q "GridRoom" /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md && grep -q "doors" /home/gjohnson/mothership/janus-skills/resources/schema-encounters.md && [ "$(ls /home/gjohnson/mothership/janus-skills/resources/*.md | wc -l)" = "5" ] && git -C /home/gjohnson/mothership/janus-skills log --oneline | head -2 | grep -q "docs(resources)"</automated>
  </verify>
  <acceptance_criteria>
    - `schema-encounters.md` exists with the section `## GridRoom fields`
    - `schema-encounters.md` documents door status enum containing `CLOSED`, `OPEN`, `LOCKED`, `BROKEN` (all 4 present)
    - `schema-encounters.md` mentions door id format `${room.id}_door_${index}` (verbatim or escaped)
    - `schema-encounters.md` mentions `tools/svg_to_map.py` (tooling note)
    - `schema-encounters.md` line count between 100 and 300
    - Directory `resources/` contains exactly 5 `.md` files (`ls resources/*.md | wc -l` == 5) and no `.gitkeep`
    - `git -C /home/gjohnson/mothership/janus-skills log --oneline` shows exactly 2 commits (initial + resources commit)
    - `git -C /home/gjohnson/mothership/janus-skills status --porcelain` returns empty
  </acceptance_criteria>
  <done>
    All 5 resource files exist in `resources/` with the documented section structure. `.gitkeep` removed. Commit on `main` records the addition with reference to D-04/D-05/D-06.
  </done>
</task>

</tasks>

<verification>
- `ls /home/gjohnson/mothership/janus-skills/resources/` shows exactly: `schema-campaign.md`, `schema-encounters.md`, `schema-galaxy.md`, `schema-janus-context.md`, `schema-ships.md`
- For each file: `head -1 <file>` shows a `# ` heading; `tail -c 1 <file> | xxd` ends with newline (`0a`)
- `git -C /home/gjohnson/mothership/janus-skills log --oneline` lists exactly 2 commits, working tree clean
- Sanity check: no file in `resources/` exceeds 500 lines (`wc -l resources/*.md` — none over 500; condensed per D-06)
</verification>

<success_criteria>
- 5 schema resource files exist and faithfully condense their DATA_DIRECTORY_GUIDE sections (no full copies)
- Each file lists MCP path conventions for the entities it documents
- Field sets match live data examples + DATA_DIRECTORY_GUIDE
- Status enums and slug rules documented exactly
- All committed in one logical commit on `main`
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-02-SUMMARY.md` recording:
- Final line count per resource file
- Commit hash for the resources commit
- Confirmation that all 5 files end with newline + use only `@$HOME`-friendly content (no embedded absolute paths to charon)
</output>
