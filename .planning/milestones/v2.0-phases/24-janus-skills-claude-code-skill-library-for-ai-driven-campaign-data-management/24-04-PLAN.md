---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 04
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-system/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-body/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "/janus-add-npc creates data/campaign/npcs/<id>.yaml with id derived from name (lowercase + underscores), enforcing id-equals-filename rule"
    - "/janus-add-location creates a galaxy location directory with location.yaml using lowercase-hyphenated slug"
    - "/janus-add-system adds a star_map.yaml entry AND creates the system directory with system_map.yaml + location.yaml"
    - "/janus-add-body updates system_map.yaml (or orbit_map.yaml for moons) AND creates the body directory with location.yaml"
    - "All four SKILL.md files have valid YAML frontmatter parseable by python3 -c 'import yaml; yaml.safe_load(...)' on the first --- block"
    - "All four skills use only mcp__JanusGM__ tool prefix (case-sensitive) in allowed-tools"
    - "All four skills @-include only the resource files they actually need"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md"
      provides: "D-12 — NPC creation skill"
      contains: "@$HOME/.claude/janus-skills/resources/schema-campaign.md"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md"
      provides: "D-13 — galaxy location creation skill"
      contains: "@$HOME/.claude/janus-skills/resources/schema-galaxy.md"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-system/SKILL.md"
      provides: "D-15 — star system creation skill (star_map + system dir)"
      contains: "star_map.yaml"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-body/SKILL.md"
      provides: "D-16 — planet/moon/station addition skill"
      contains: "system_map.yaml"
  key_links:
    - from: "All 4 SKILL.md files"
      to: "MCP tools"
      via: "mcp__JanusGM__ prefix (verbatim, case-sensitive — must match FastMCP('JanusGM'))"
      pattern: "mcp__JanusGM__"
    - from: "All 4 SKILL.md files"
      to: "resources/"
      via: "@$HOME/.claude/janus-skills/resources/schema-*.md (NOT @./resources/...)"
      pattern: "@\\$HOME/\\.claude/janus-skills/resources/"
---

<objective>
Author the four "creation" skills for the campaign/galaxy data: `/janus-add-npc` (D-12), `/janus-add-location` (D-13), `/janus-add-system` (D-15), `/janus-add-body` (D-16). Each is a self-contained SKILL.md file in its own directory under `skills/`.

Purpose: These are the primary write-path skills for adding new entities to the campaign. They define the patterns (id/slug derivation, MCP path conventions, frontmatter shape) that the second batch of skills (plan 24-05) will replicate.

Output: Four SKILL.md files with valid YAML frontmatter, correct mcp__JanusGM__ tool names, and stable `@$HOME/.claude/janus-skills/resources/...` @-includes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-02-SUMMARY.md
@mcp_server.py

<interfaces>
<!-- MCP tools available (FastMCP server "JanusGM") -->
mcp__JanusGM__list_files       — list_files(dir)        dir relative to data/
mcp__JanusGM__read_file        — read_file(path)        path relative to data/
mcp__JanusGM__write_file       — write_file(path, content)  triggers SSE broadcast
mcp__JanusGM__get_session_context
mcp__JanusGM__get_data_schema  — fallback for edge cases per D-07

<!-- Reference SKILL.md frontmatter shape (verified pattern from gsd-add-tests) -->
---
name: <slash-command-name>
description: "<one sentence including 'Use when...'>"
argument-hint: "<placeholder>"
allowed-tools:
  - mcp__JanusGM__<tool>
  - ...
---

<!-- @-include path rule (Pitfall P3, RESEARCH.md) -->
USE:  @$HOME/.claude/janus-skills/resources/schema-<name>.md
NOT:  @./resources/...   (resolves from CWD, breaks for global install)
NOT:  @resources/...     (same)

<!-- Skill-to-resource mapping (RESEARCH.md Skill-to-Resource Mapping table) -->
janus-add-npc       → schema-campaign.md
janus-add-location  → schema-galaxy.md
janus-add-system    → schema-galaxy.md
janus-add-body      → schema-galaxy.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author janus-add-npc and janus-add-location skills</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/data/campaign/npcs/captain_harrow.yaml
    /home/gjohnson/mothership/charon/data/galaxy/anchor-system/veil-station/location.yaml
    /home/gjohnson/mothership/janus-skills/resources/schema-campaign.md
    /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
    /home/gjohnson/.claude/skills/gsd-add-tests/SKILL.md
  </read_first>
  <action>
    Create both SKILL.md files in their own subdirectories under `/home/gjohnson/mothership/janus-skills/skills/`. Each file must follow the verified Claude Code skill pattern (YAML frontmatter + `<objective>` + `<schema>` + `<process>` sections per D-03).

    **`skills/janus-add-npc/SKILL.md`**
    Frontmatter fields (exact values):
    - `name`: `janus-add-npc`
    - `description`: Begins with a verb and contains the literal substring `Use when` (e.g., `"Create a new NPC entry in data/campaign/npcs/. Use when adding an NPC, character, or contact to the campaign roster."`)
    - `argument-hint`: `"<npc-name> [faction] [role]"`
    - `allowed-tools`: YAML list containing exactly these two entries (no Read/Write/Bash):
      - `mcp__JanusGM__list_files`
      - `mcp__JanusGM__write_file`

    Body sections (in order):
    - `<objective>`: One paragraph stating what the skill produces (`data/campaign/npcs/<id>.yaml`) and via what (`JANUS MCP server`).
    - `<schema>` containing the single line `@$HOME/.claude/janus-skills/resources/schema-campaign.md` (literal).
    - `<process>` containing a numbered list (7–10 steps) covering:
      1. Parse `$ARGUMENTS` for name + optional faction/role.
      2. Call `list_files("campaign/npcs")` to enumerate existing ids and avoid collision.
      3. Derive `id`: `name.lower().replace(" ", "_").replace("-", "_")`. If id collides, append numeric suffix (`_2`, `_3`, ...) and warn user.
      4. Ask user for any missing required fields (faction, role, status, description). Status must be one of `ACTIVE | INACTIVE | DECEASED | UNKNOWN`.
      5. Ask whether NPC is "background" (minimal — id/name/role/faction/status/description) or "combatant" (adds stats, saves, stress, health, wounds, armor, class).
      6. For combatant: prompt for stats (strength/speed/intellect/combat, range 2–10, default 5); saves (sanity/fear/body, range 0–99, defaults 30/40/40); stress (default 2); health (max default 10–14); wounds (default 1); armor (default 0); class (Teamster | Scientist | Marine | Android).
      7. Build YAML string. CRITICAL: `id:` field must EXACTLY equal the filename stem (Pitfall P2). Use double-quoted strings for free-text fields.
      8. Call `write_file("campaign/npcs/<id>.yaml", content)`.
      9. Confirm to user: report the written path and any defaulted fields.

    **`skills/janus-add-location/SKILL.md`**
    Frontmatter:
    - `name`: `janus-add-location`
    - `description`: Contains `Use when` (e.g., `"Create a new galaxy location (station, planet, or moon) with its location.yaml. Use when adding a location to an existing star system."`)
    - `argument-hint`: `"<location-name> [type] [parent-system]"`
    - `allowed-tools`: list with exactly:
      - `mcp__JanusGM__list_files`
      - `mcp__JanusGM__read_file`
      - `mcp__JanusGM__write_file`

    Body:
    - `<objective>`: states output: `data/galaxy/<system>/<location-slug>/location.yaml`.
    - `<schema>` containing exactly: `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
    - `<process>` numbered list (8–12 steps):
      1. Parse `$ARGUMENTS` for name, optional type, optional parent-system.
      2. Ask user (or infer from args): location `type` — must be one of `station | planet | moon | system`.
      3. Ask for parent system (the directory name under `data/galaxy/`) unless `type == system` (in which case parent is root).
      4. Call `list_files("galaxy/<parent-system>")` to enumerate sibling slugs; verify parent system directory exists. If not, instruct user to run `/janus-add-system` first and abort.
      5. Derive slug: `name.lower().replace(" ", "-").replace("_", "-")` (lowercase + hyphens — Pitfall P1). All other characters must be `[a-z0-9-]`; strip or replace others.
      6. Check slug collision against sibling list. If collision, append numeric suffix.
      7. Ask user for required fields: `status` (e.g., OPERATIONAL), `description`. Optional: `population`, `crew_capacity`, `established`, `orbital_body`, `orbital_position`.
      8. Build location.yaml content. Required keys: `name`, `type`, `parent_system` (the system slug), `status`, `description`. Add optional keys only if user provided them.
      9. Path to write: `galaxy/<parent-system>/<location-slug>/location.yaml`. Call `write_file(path, content)`.
      10. Inform user the location directory was created. Suggest follow-up: `/janus-update-galaxy` to add it to system_map.yaml if it should appear on the system map.

    Both files end with a trailing newline. Do NOT include a `<execution_context>` block — these are simple skills, not orchestrators.

    After writing, validate frontmatter parses:
    ```
    for f in skills/janus-add-npc/SKILL.md skills/janus-add-location/SKILL.md; do
      python3 -c "
    import sys, yaml
    content = open('$f').read()
    parts = content.split('---')
    assert len(parts) >= 3, 'missing frontmatter'
    fm = yaml.safe_load(parts[1])
    assert fm['name'], 'name missing'
    assert 'Use when' in fm['description'], 'description missing trigger'
    assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']), 'wrong tool prefix'
    print('OK: ' + '$f')
    "
    done
    ```
    Both must print OK.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && for f in skills/janus-add-npc/SKILL.md skills/janus-add-location/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name'] in ('janus-add-npc','janus-add-location'); assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK',f)" || exit 1; done && grep -q "schema-campaign.md" skills/janus-add-npc/SKILL.md && grep -q "schema-galaxy.md" skills/janus-add-location/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Both SKILL.md files exist at the exact paths listed in `<files>`
    - Each parses with `python3` yaml split-on-`---` test (frontmatter valid YAML, name matches directory, description contains `Use when`, all allowed-tools start with `mcp__JanusGM__`)
    - `skills/janus-add-npc/SKILL.md` contains the exact line `@$HOME/.claude/janus-skills/resources/schema-campaign.md`
    - `skills/janus-add-location/SKILL.md` contains the exact line `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
    - Neither file contains `@./resources/` or `@resources/` (forbidden patterns — Pitfall P3): grep returns no matches
    - `skills/janus-add-npc/SKILL.md` mentions id-equals-filename rule (grep for `filename` or `Pitfall` finds a match)
    - `skills/janus-add-location/SKILL.md` mentions slug case rule (grep for `lowercase` or `hyphen` finds a match)
    - `skills/janus-add-npc/SKILL.md` `allowed-tools` is exactly the two tools `list_files` and `write_file` (verify by parsing the list)
    - Each file ends with newline character
    - Each file line count between 40 and 150 (focused, not bloated)
  </acceptance_criteria>
  <done>
    `janus-add-npc` and `janus-add-location` SKILL.md files exist with valid frontmatter, correct MCP tool names, stable resource @-includes, and step-by-step process documentation including slug/id derivation rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author janus-add-system and janus-add-body skills</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-system/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-body/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/DATA_DIRECTORY_GUIDE.md
    /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-location/SKILL.md
  </read_first>
  <action>
    Create both SKILL.md files. Each must use the same frontmatter shape as the skills in Task 1 (D-03) — name/description/argument-hint/allowed-tools.

    **`skills/janus-add-system/SKILL.md`** (D-15)
    Frontmatter:
    - `name`: `janus-add-system`
    - `description`: includes `Use when` (e.g., `"Add a new star system to the galaxy: appends an entry to data/galaxy/star_map.yaml and scaffolds the system directory with system_map.yaml + location.yaml. Use when introducing a new star system to the campaign map."`)
    - `argument-hint`: `"<system-name> [coordinates]"`
    - `allowed-tools`: list with exactly:
      - `mcp__JanusGM__read_file`
      - `mcp__JanusGM__write_file`

    Body:
    - `<objective>`: states this skill creates THREE files: appends to `galaxy/star_map.yaml`, creates `galaxy/<system-slug>/system_map.yaml`, and creates `galaxy/<system-slug>/location.yaml`.
    - `<schema>`: `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
    - `<process>` (numbered, 10–14 steps):
      1. Parse `$ARGUMENTS` for system name + optional 3D coordinates `[x,y,z]`.
      2. Call `read_file("galaxy/star_map.yaml")` to load current systems list. Parse YAML.
      3. Derive system slug: `name.lower().replace(" ", "-")` (e.g., `Tau Ceti` → `tau-ceti`).
      4. Check slug collision against existing `systems[].location_slug` values. If collision, refuse and ask user for a different name.
      5. Ask user for required visual props with defaults: `position` (default `[0, 0, 0]` — warn user), `color` (default `0xFFFFAA`), `size` (default 2.5), `type` (default `star`), `label` (default true), `info.description`, `info.population`.
      6. Build new system entry. Append to `systems[]`. Preserve all existing fields (camera, routes, nebulae) — do not lose data.
      7. Call `write_file("galaxy/star_map.yaml", updated_content)`.
      8. Build `system_map.yaml` content: `star` block (name, color matching star_map entry, size 4.5 default), `camera` (default position `[0, 127, 127]`, lookAt `[0,0,0]`, fov 75), empty `bodies: []`.
      9. Call `write_file("galaxy/<system-slug>/system_map.yaml", content)`.
      10. Build `location.yaml` content for the system root: `name`, `type: "system"`, `status: "OPERATIONAL"`, `description`.
      11. Call `write_file("galaxy/<system-slug>/location.yaml", content)`.
      12. Confirm to user: list all three files written. Suggest follow-up: `/janus-add-body` to add planets, `/janus-update-galaxy` for travel routes.

    **`skills/janus-add-body/SKILL.md`** (D-16)
    Frontmatter:
    - `name`: `janus-add-body`
    - `description`: includes `Use when` (e.g., `"Add a planet, moon, or orbital station to an existing star system. Updates system_map.yaml (or orbit_map.yaml for moons) and creates the body directory with location.yaml. Use when adding a body to a star system."`)
    - `argument-hint`: `"<body-name> <type> <parent-system> [parent-planet-for-moons]"`
    - `allowed-tools`: list with exactly:
      - `mcp__JanusGM__list_files`
      - `mcp__JanusGM__read_file`
      - `mcp__JanusGM__write_file`

    Body:
    - `<objective>`: states the skill branches on `type`:
      - `planet` or `station` → appends to system's `system_map.yaml` bodies list + creates `galaxy/<system>/<body-slug>/location.yaml`
      - `moon` → reads/creates parent planet's `orbit_map.yaml`, appends to its `moons[]`, creates `galaxy/<system>/<planet>/<moon-slug>/location.yaml`
    - `<schema>`: `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
    - `<process>` (numbered, 12–16 steps):
      1. Parse `$ARGUMENTS`: name, type (`planet` | `moon` | `station`), parent system slug. If type=moon, also parent planet slug.
      2. Verify parent system exists: `read_file("galaxy/<system>/system_map.yaml")` — if fails, abort with instruction to run `/janus-add-system` first.
      3. If type=moon: verify parent planet exists: `read_file("galaxy/<system>/<planet>/location.yaml")` — abort if missing.
      4. Derive slug: `name.lower().replace(" ", "-")` (Pitfall P1 — lowercase + hyphens).
      5. Check slug collision in appropriate context (siblings under system, or siblings under planet for moons).
      6. Branch on type:
         - **planet / station path:**
           a. Read `system_map.yaml`, parse. Ask user for: `orbital_radius`, `orbital_period`, `size`, `color`, `info.description`, `clickable` (default true), `has_orbit_map` (default true for planets, false for stations).
           b. Append entry with `name`, `type`, `location_slug` (EXACT match to dir to be created — Pitfall P1), and the user-supplied fields.
           c. Write updated `system_map.yaml`.
         - **moon path:**
           a. Attempt `read_file("galaxy/<system>/<planet>/orbit_map.yaml")`. If file does not exist, build a new orbit_map.yaml: `planet` block (read planet name from planet's location.yaml), `camera` defaults, `moons: []`.
           b. Ask user for moon fields: `orbital_radius`, `orbital_period`, `orbital_angle`, `inclination`, `size`, `color`, `has_facilities` (default false).
           c. Append moon entry with `name`, `location_slug` (exact slug match), the user-supplied fields.
           d. Write updated `orbit_map.yaml`.
      7. Build `location.yaml` for the new body: `name`, `type`, `parent_system: <system>`, `orbital_body: <planet-name>` (only for moons), `status: "OPERATIONAL"`, `description`.
      8. Path: `galaxy/<system>/<slug>/location.yaml` for planet/station, `galaxy/<system>/<planet>/<slug>/location.yaml` for moon.
      9. Call `write_file(path, content)`.
      10. Confirm: list all files modified/written, mention Pitfall P5 warning if user named a moon (moons cannot host ships via body_slug).

    Both files end with newline. Run the same frontmatter validation as Task 1:
    ```
    for f in skills/janus-add-system/SKILL.md skills/janus-add-body/SKILL.md; do
      python3 -c "import yaml; ..." (same as Task 1)
    done
    ```

    After both skills are valid, commit all 4 SKILL.md files from this plan together. From `/home/gjohnson/mothership/janus-skills`:
    1. `git add skills/janus-add-npc/SKILL.md skills/janus-add-location/SKILL.md skills/janus-add-system/SKILL.md skills/janus-add-body/SKILL.md`
    2. Commit message:
       ```
       feat(skills): add 4 creation skills (D-12, D-13, D-15, D-16)

       - janus-add-npc        (data/campaign/npcs/)
       - janus-add-location   (galaxy location with location.yaml)
       - janus-add-system     (star_map + system dir scaffold)
       - janus-add-body       (planet/moon/station into system_map or orbit_map)

       All skills use mcp__JanusGM__ tool prefix and reference
       resources via @$HOME/.claude/janus-skills/resources/ for portability
       across global + project installs (Pitfall P3).

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && for f in skills/janus-add-system/SKILL.md skills/janus-add-body/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name'] in ('janus-add-system','janus-add-body'); assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK',f)" || exit 1; done && grep -q "star_map.yaml" skills/janus-add-system/SKILL.md && grep -q "system_map.yaml" skills/janus-add-body/SKILL.md && grep -q "orbit_map.yaml" skills/janus-add-body/SKILL.md && git -C /home/gjohnson/mothership/janus-skills log --oneline | head -1 | grep -q "feat(skills)"</automated>
  </verify>
  <acceptance_criteria>
    - Both SKILL.md files exist at the listed paths
    - Each parses with the Task 1 python yaml validation snippet (frontmatter valid, name matches dir, `Use when` in description, all allowed-tools prefixed with `mcp__JanusGM__`)
    - `skills/janus-add-system/SKILL.md` contains the literal strings `star_map.yaml`, `system_map.yaml`, and `location.yaml` (all three target files documented in process)
    - `skills/janus-add-body/SKILL.md` contains literal strings `system_map.yaml`, `orbit_map.yaml`, and branches on `type` (grep for `moon` and `planet` both find matches)
    - `skills/janus-add-system/SKILL.md` `allowed-tools` is exactly `[mcp__JanusGM__read_file, mcp__JanusGM__write_file]` (2 entries) — verify by parsing
    - `skills/janus-add-body/SKILL.md` `allowed-tools` is exactly 3 entries: list_files, read_file, write_file
    - Neither file contains `@./resources/` or `@resources/` (Pitfall P3)
    - All 4 SKILL.md files (from Tasks 1 and 2) are committed; `git log --oneline | head -1` starts with `feat(skills):`
    - `git status --porcelain` returns empty
    - Total SKILL.md count under `skills/` = 4 (verify: `find skills -name SKILL.md | wc -l` == 4)
  </acceptance_criteria>
  <done>
    All 4 creation skills exist, parse cleanly, declare the correct narrow tool surface, reference the correct schema resource, and are committed in one logical commit.
  </done>
</task>

</tasks>

<verification>
- `find /home/gjohnson/mothership/janus-skills/skills -name SKILL.md` returns exactly 4 files
- Loop validation: for each SKILL.md, frontmatter parses as YAML, has name/description/allowed-tools, allowed-tools entries all start with `mcp__JanusGM__`
- No SKILL.md contains the forbidden patterns `@./resources/` or `@resources/`
- `git -C /home/gjohnson/mothership/janus-skills status --porcelain` empty; latest commit subject begins `feat(skills):`
</verification>

<success_criteria>
- All four "creation" skills (D-12, D-13, D-15, D-16) exist with the documented frontmatter and process sections
- Each skill declares only the MCP tools it actually uses (minimal permissions)
- Resource @-includes use the stable HOME-anchored path
- id/slug derivation rules and pitfall warnings are embedded in the process steps
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-04-SUMMARY.md` recording:
- Commit hash
- Per-skill line count
- Confirmation of frontmatter validation pass for all 4
- Final allowed-tools list per skill (so plan 24-05 can sanity-check tool prefix consistency)
</output>
