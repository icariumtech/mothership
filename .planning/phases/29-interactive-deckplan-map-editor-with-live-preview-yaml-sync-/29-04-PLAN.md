---
phase: 29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-
plan: 04
type: execute
wave: 4
depends_on: ['29-03']
files_modified:
  - src/components/gm/views/FileEditorView.tsx
  - src/components/gm/views/FileEditorView.css
  - src/components/gm/views/deckplan/DeckplanPreviewPane.tsx
autonomous: false
requirements: [EDIT-04, EDIT-05, EDIT-06]
must_haves:
  truths:
    - "Clicking a room or POI on the preview reveals + highlights the matching deck-scoped YAML line in Monaco"
    - "Dragging a POI writes a surgical position: {x, y} patch into the Monaco buffer (no reserialize)"
    - "Clicking an empty cell inserts a POI stub into the selected deck's poi: list (creating the list if absent)"
    - "Added/moved POI changes are saved via the existing PUT /api/gm/data/{path} flow (Ctrl+S)"
    - "Diffing a saved file after a POI edit shows only the targeted lines changed — flow-style + comments preserved"
  artifacts:
    - path: "src/components/gm/views/FileEditorView.tsx"
      provides: "Monaco edit wiring: jumpToElement (reveal+decoration) + applyEdit for POI move/add"
      contains: "revealRangeInCenter"
    - path: "src/components/gm/views/FileEditorView.css"
      provides: ".deckplan-jump-highlight decoration class"
      contains: "deckplan-jump-highlight"
  key_links:
    - from: "DeckplanPreviewPane callbacks"
      to: "FileEditorView Monaco editor instance"
      via: "buildIdRangeMap / buildPositionEdit / buildAddPoiEdit → executeEdits"
      pattern: "executeEdits|applyEdits|deltaDecorations"
---

<objective>
Wire the preview's interaction callbacks (from Plan 03) to the live Monaco editor using the
pure builders (from Plan 02): click-to-jump (EDIT-04) via `revealRangeInCenter` +
`deltaDecorations`, drag-to-move (EDIT-05) and click-to-add (EDIT-06) via `executeEdits`
applying the `buildPositionEdit` / `buildAddPoiEdit` text patches. Then a human-verify
checkpoint confirms the full interactive loop end-to-end.

Purpose: Closes the last three requirements. All map-driven writes are surgical text edits
into Monaco's buffer (D-12) — never a reserialize — so hand-authored flow-style and comments
survive. Saves reuse the existing PUT flow (D-15).

Output: Wired FileEditorView + decoration CSS + checkpoint sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-RESEARCH.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-PATTERNS.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md

<interfaces>
<!-- Plan 02 builders (Monaco-agnostic — return 1-based line/col TextEdit or Map entries): -->
import { buildIdRangeMap, IdRangeEntry } from './deckplan/useDeckplanModel';
import { buildPositionEdit, buildAddPoiEdit, TextEdit } from './deckplan/deckplanYamlEdits';
buildIdRangeMap(yamlText) -> Map<`${deckId}|${kind}|${id}`, IdRangeEntry>
buildPositionEdit(yamlText, deckId, poiId, newX, newY) -> TextEdit | null
buildAddPoiEdit(yamlText, deckId, x, y, opts?) -> TextEdit | null

<!-- Plan 03 callback surface on DeckplanPreviewPane (currently placeholders to wire): -->
onRoomClick(roomId) / onPoiClick(poiId) / onPoiMove(poiId, x, y) / onEmptyCellClick(x, y)
plus the currently-selected deckId from DeckSelector state.

<!-- Monaco APIs (via editorRef from onEditorMount, FileEditorView.tsx ~105-110): -->
editor.getModel().getValue()                      // SOURCE OF TRUTH at edit time (Pitfall 4)
editor.revealRangeInCenter(new monaco.Range(...), monaco.editor.ScrollType.Smooth)
editor.deltaDecorations(prevIds, [{range, options:{isWholeLine:true, className:'deckplan-jump-highlight'}}])
editor.executeEdits('deckplan', [{range: new monaco.Range(sl,sc,el,ec), text}])
// monaco instance is available in onEditorMount; persist editorRef + a monacoRef.

<!-- Existing save flow (UNCHANGED, D-15): edits mutate content via onChange → handleSave → writeDataFile → PUT /api/gm/data/{path} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire click-to-jump (EDIT-04) — reveal + decoration on deck-scoped id match</name>
  <files>src/components/gm/views/FileEditorView.tsx, src/components/gm/views/FileEditorView.css</files>
  <read_first>
    - src/components/gm/views/FileEditorView.tsx (onEditorMount ~105-110 — capture editor + monaco instance; renderEditorContent deckplan branch from Plan 03; content state)
    - src/components/gm/views/deckplan/useDeckplanModel.ts (buildIdRangeMap, IdRangeEntry from Plan 02)
    - src/components/gm/views/deckplan/DeckplanPreviewPane.tsx (onRoomClick/onPoiClick callbacks + selectedDeckId surface from Plan 03)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-RESEARCH.md (Pattern 2 reveal+decoration; Pitfall 3 deck-scoped; Pitfall 4 rebuild map from live model value)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md (highlight: background rgba(201,160,80,0.18) + border-left 2px #c9a050, isWholeLine, persists until next jump / manual caret move, no timer)
  </read_first>
  <action>
    In onEditorMount, store BOTH the editor instance and the `monaco` namespace in refs so
    jump/edit handlers can construct `monaco.Range`. Implement a `jumpToElement(kind, id)`
    handler in FileEditorView: rebuild the id→range map from `editor.getModel().getValue()`
    (the LIVE model value, not debounced content state — Pitfall 4), look up
    `${selectedDeckId}|${kind}|${id}` (deck-scoped — Pitfall 3), and if found call
    `editor.revealRangeInCenter(new monaco.Range(line,1,line,1), ScrollType.Smooth)` and
    `editor.deltaDecorations(prevDecorationIds, [{range: whole-line range, options:{isWholeLine:true,
    className:'deckplan-jump-highlight'}}])`, storing the returned decoration ids in a ref so the
    next jump clears them. If no match, surface the MAP SYNC ERROR copy (transient inline/toast)
    per UI-SPEC. Pass `jumpToElement('room', id)` as DeckplanPreviewPane's onRoomClick and
    `jumpToElement('poi', id)` as onPoiClick. Clear the decoration when the user moves the caret
    manually (onDidChangeCursorPosition) — do NOT auto-clear on a timer.
    In FileEditorView.css add `.deckplan-jump-highlight { background: rgba(201,160,80,0.18);
    border-left: 2px solid #c9a050; }`.
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -3 && grep -q "deckplan-jump-highlight" src/components/gm/views/FileEditorView.css && grep -qE "revealRangeInCenter|deltaDecorations" src/components/gm/views/FileEditorView.tsx && echo wired</automated>
  </verify>
  <acceptance_criteria>
    - `jumpToElement` rebuilds the id→range map from `editor.getModel().getValue()` (not the debounced content state)
    - Lookup is deck-scoped using `${selectedDeckId}|${kind}|${id}`
    - On match: `revealRangeInCenter` + `deltaDecorations` applied; previous decoration cleared on the next jump
    - `.deckplan-jump-highlight` defined in CSS with `rgba(201,160,80,0.18)` background + `2px solid #c9a050` left border
    - On no-match, `MAP SYNC ERROR` copy is shown (not a silent no-op)
    - `pnpm run typecheck` passes
  </acceptance_criteria>
  <done>Clicking a room/POI scrolls Monaco to and highlights the correct deck-scoped YAML line.</done>
</task>

<task type="auto">
  <name>Task 2: Wire POI drag-to-move (EDIT-05) and click-to-add (EDIT-06) as surgical edits</name>
  <files>src/components/gm/views/FileEditorView.tsx, src/components/gm/views/deckplan/DeckplanPreviewPane.tsx</files>
  <read_first>
    - src/components/gm/views/FileEditorView.tsx (jumpToElement + editor/monaco refs from Task 1; content/onChange/handleSave flow)
    - src/components/gm/views/deckplan/deckplanYamlEdits.ts (buildPositionEdit, buildAddPoiEdit, TextEdit from Plan 02)
    - src/components/gm/views/deckplan/DeckplanPreviewPane.tsx (onPoiMove/onEmptyCellClick callbacks + selectedDeckId from Plan 03)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-RESEARCH.md (Pattern 3 executeEdits; Pitfall 4 live-model source-of-truth; Pitfall 5 block→flow position acceptable)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md (no confirm dialog; add-stub defaults name "New POI"/type+icon "marker"; on add, also trigger the jump-highlight on the inserted lines so the GM sees the new text)
  </read_first>
  <action>
    Implement `applyDeckplanEdit(textEdit: TextEdit | null)` in FileEditorView: if null, no-op;
    else convert the 1-based TextEdit to a `monaco.Range`, call
    `editor.executeEdits('deckplan', [{range, text}])` (this fires onChange → updates `content`
    → preview re-renders live and `isDirty` flips; D-12/D-15 — never reserialize). Then create:
    - onPoiMove(poiId, x, y): build the edit via
      `buildPositionEdit(editor.getModel().getValue(), selectedDeckId, poiId, x, y)` and apply it.
      No confirmation dialog (UI-SPEC) — Ctrl+Z undoes it.
    - onEmptyCellClick(x, y): build via
      `buildAddPoiEdit(editor.getModel().getValue(), selectedDeckId, x, y)` (defaults name
      "New POI", type/icon "marker") and apply it; after applying, call jumpToElement-style
      reveal+highlight on the inserted POI's id line so the GM immediately sees/edits the new
      stub (text-only per D-13).
    ALWAYS read `editor.getModel().getValue()` at the moment of the interaction as the source of
    truth (Pitfall 4) — never the debounced content snapshot. Pass these two handlers (and the
    Task-1 jump handlers) down through DeckplanPreviewPane to replace the Plan 03 placeholders.
    Confirm the SAVE button / Ctrl+S persists via the existing unchanged handleSave → writeDataFile
    → PUT /api/gm/data/{path} (no new endpoint, D-15).
  </action>
  <verify>
    <automated>pnpm run typecheck && pnpm run build 2>&1 | tail -5 && grep -qE "executeEdits" src/components/gm/views/FileEditorView.tsx && echo wired</automated>
  </verify>
  <acceptance_criteria>
    - `onPoiMove` applies a `buildPositionEdit` patch via `editor.executeEdits` (no `YAML.stringify` / full-buffer setValue anywhere in the file)
    - `onEmptyCellClick` applies a `buildAddPoiEdit` patch and then highlights the inserted POI line
    - Both handlers read `editor.getModel().getValue()` at interaction time, not debounced state
    - POI add stub defaults to name "New POI", type/icon "marker"
    - Save remains the existing `handleSave` → `writeDataFile` → `PUT /api/gm/data/{path}` path (no new save code)
    - `pnpm run typecheck && pnpm run build` succeed
  </acceptance_criteria>
  <done>POI drag patches position surgically; empty-cell click inserts a POI stub; both flow through the existing save path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the full editor loop (EDIT-02..06)</name>
  <action>
    PAUSE for human verification. The GM runs the app and walks the steps below to confirm the
    full editor loop (live preview, deck selector, click-to-jump, POI drag, POI add) works
    end-to-end and that writes are surgical (verified via git diff). No code is written in this
    task — it is a blocking sign-off gate before /gsd:verify-work.
  </action>
  <what-built>
    The deckplan map editor: live preview below Monaco, deck selector, click-to-jump,
    POI drag-to-move, and click-to-add — all writing surgical text edits into the Monaco
    buffer and saving via the existing PUT flow.
  </what-built>
  <how-to-verify>
    1. Start the app (`pnpm run dev` + Django server, or `./start_server.sh`) and open the GM Console file editor.
    2. Open `data/ships/patrol_gunboat/deckplan.yaml`. CONFIRM: a live map preview appears below Monaco (EDIT-02).
    3. Use the deck selector. CONFIRM: switching tabs re-renders the chosen deck (EDIT-03).
    4. Click a room, then a POI (or add one first), on the preview. CONFIRM: Monaco scrolls to and amber-highlights the matching `id:` line; clicking the same room on a different deck (if ids collide) jumps to the correct deck's line (EDIT-04).
    5. Drag a POI to a new cell. CONFIRM: the `position:` line updates to `{x: N, y: N}`, the preview moves the POI live, and only that line changed.
    6. Click an empty cell. CONFIRM: a `New POI` (type/icon `marker`) stub is inserted into the selected deck's `poi:` list and the inserted line is highlighted.
    7. Press Ctrl+S. Then `git diff data/ships/patrol_gunboat/deckplan.yaml`. CONFIRM: only the edited POI lines changed — flow-style formatting and comments elsewhere are untouched (D-12). Note: a position authored block-style collapsing to single-line flow is expected/acceptable (Pitfall 5).
    8. Open a non-deckplan YAML (e.g. any `location.yaml`). CONFIRM: no preview pane appears.
    9. (Optional) Revert the patrol_gunboat changes with `git checkout` if you don't want to keep the test POIs.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any step that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview interaction → Monaco buffer | Surgical edits inserted into hand-authored YAML |
| Monaco buffer → PUT /api/gm/data | Saved text validated server-side via yaml.safe_load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | stale id→range map corrupting adjacent YAML | mitigate | All edits rebuild the map from `editor.getModel().getValue()` at interaction time (Pitfall 4); checkpoint step 7 diffs the saved file to confirm only targeted lines changed |
| T-29-09 | Tampering | surgical edit producing invalid YAML | mitigate | `safe_write_yaml()` validates via `yaml.safe_load` on PUT and rejects malformed content with 400 (D-15, existing) — surfaced as the VALIDATION ERROR banner |
| T-29-10 | Tampering | YAML injection via inserted POI stub | accept | Stub is a fixed template with numeric x/y + literal strings; no user-controlled tag syntax constructible via the UI |
| T-29-SC | Tampering | npm installs | mitigate | No new packages; N/A |
</threat_model>

<verification>
- `pnpm run typecheck && pnpm run build` succeed
- No `YAML.stringify` or full-buffer `setValue` in the deckplan edit path (surgical only, D-12)
- Human-verify checkpoint (Task 3) confirms EDIT-02..06 end-to-end, including a git diff proving surgical writes
</verification>

<success_criteria>
- EDIT-04: click room/POI reveals + highlights the correct deck-scoped Monaco line
- EDIT-05: POI drag writes a surgical `position: {x, y}` patch
- EDIT-06: empty-cell click inserts a POI stub into the deck's `poi:` list
- All writes are surgical (D-12) and saved via the existing PUT flow (D-15); checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-04-SUMMARY.md` when done
</output>
