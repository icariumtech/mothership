---
phase: 28.1-close-gap-d-10-data-changed-sse-consumer-phase-28-uat-verifi
plan: "01"
subsystem: ui
tags: [sse, eventsource, react, typescript, data-changed, player-terminal]

# Dependency graph
requires:
  - phase: 28-gm-console-data-directory-file-editor
    provides: data-changed SSE events emitted from gm_data.py after every GM write
provides:
  - optional onDataChanged callback in useSSE hook registers 'data-changed' EventSource listener
  - SharedConsole re-fetches /api/active-view/ on data-changed and pipes through applyActiveView helper
  - GM-edits-YAML → player-terminal-auto-reloads E2E flow is now complete
affects: [player-terminal, sse-consumers, active-view-refresh]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "applyActiveView helper: extract shared view-application logic for reuse across SSE event types"
    - "Gated EventSource listener: register named listener only when optional callback is provided"

key-files:
  created: []
  modified:
    - src/hooks/useSSE.ts
    - src/entries/SharedConsole.tsx

key-decisions:
  - "Blanket re-fetch on data-changed (ignores path/action payload in v1) — simple, correct, cheap"
  - "applyActiveView extracted from onEvent inline body to share logic; avoids duplication"
  - "onDataChanged useCallback deps include applyActiveView (stable ref) to keep SSE connection stable"

patterns-established:
  - "Pattern: gated optional SSE listeners — if (onOptionalHandler) { es.addEventListener(...) }"

requirements-completed: [D-10]

# Metrics
duration: 12min
completed: 2026-06-05
---

# Phase 28.1 Plan 01: Close Gap D-10 — data-changed SSE Consumer Summary

**`data-changed` EventSource listener wired from useSSE through SharedConsole, closing the last broken E2E flow: GM saves YAML → player terminal auto-refreshes via /api/active-view/ re-fetch**

## Performance

- **Duration:** 12 min
- **Started:** 2026-06-05T00:00:00Z
- **Completed:** 2026-06-05T00:12:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Extended `UseSSEOptions` with `onDataChanged?: (data: { path: string; action: string }) => void` placed between `onShipStatusEvent` and `onConnect`
- Added gated `data-changed` EventSource listener in useSSE (mirrors shipstatus pattern exactly — try/catch, conditional registration, dependency array entry)
- Extracted `applyActiveView` useCallback helper in SharedConsole housing the full view-update + transition logic previously inline in `onEvent`
- Wired `onDataChanged` in SharedConsole: logs payload, fetches `/api/active-view/`, pipes result through `applyActiveView`
- GMConsole.tsx untouched — GM already sees live saves in FileEditorView; no double-reload needed

## Task Commits

1. **Task 1 + Task 2: Add onDataChanged to useSSE + wire in SharedConsole** - `0f0a5ab` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/hooks/useSSE.ts` - Added `onDataChanged` interface field, destructure, conditional `data-changed` listener, dependency array entry
- `src/entries/SharedConsole.tsx` - Extracted `applyActiveView` helper; refactored `onEvent` to call it; added `onDataChanged` handler with fetch + applyActiveView

## Verification Results

| Check | Result |
|-------|--------|
| `grep -c "addEventListener('data-changed'" src/hooks/useSSE.ts` | 1 (pass) |
| `grep -c "onDataChanged" src/hooks/useSSE.ts` | 5 (pass, ≥4) |
| `grep -c "onDataChanged" src/entries/SharedConsole.tsx` | 1 (pass, ≥1) |
| `grep -c "applyActiveView" src/entries/SharedConsole.tsx` | 5 (pass, ≥3) |
| `grep -c "fetch('/api/active-view/')" src/entries/SharedConsole.tsx` | 1 (pass) |
| `grep -c "onDataChanged" src/entries/GMConsole.tsx` | 0 (pass) |
| `pnpm run typecheck` | exit 0 |
| `pnpm run build` | exit 0 |

## Decisions Made
- Blanket re-fetch on `data-changed` ignores payload path/action in v1 — correct because `/api/active-view/` aggregates all per-view data in one round trip
- `applyActiveView` useCallback has empty dependency array (reads live state via refs), matching existing `onEvent` and `onShipStatusEvent` patterns for SSE connection stability
- Tasks 1 and 2 committed in a single commit since they form one atomic feature (the listener is useless without the consumer)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. Live smoke test (optional): start dev server, open GM console + player terminal in separate tabs, edit any campaign YAML in FileEditorView, observe `[SSE] data-changed received` in player browser console and a fresh `/api/active-view/` network request within ~1s.

## Next Phase Readiness
- D-10 audit gap closed — `data-changed` SSEs are no longer silently discarded by the player terminal
- v2.0 milestone audit gaps: D-10 complete; remaining gaps (D-09 auth, D-11 cache) are separate plans
- No regressions to ship-status SSE path or GMConsole behavior

---
*Phase: 28.1-close-gap-d-10-data-changed-sse-consumer-phase-28-uat-verifi*
*Completed: 2026-06-05*
