---
phase: 30-ai-map-editing-element-resolver-targeted-edit-mcp-tools
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mcp_server.py
  - docs/schemas/schema-encounters.md
autonomous: true
requirements:
  - E-01
  - E-02
  - E-03
  - E-04
  - E-05

must_haves:
  truths:
    - "The AI has a find_map_element tool that returns candidate ids + labels without loading the deckplan"
    - "The AI has an edit_map_element tool that edits one element via set/add_poi/remove_poi"
    - "An ambiguous edit_map_element call surfaces the candidates so the AI can ask the GM"
    - "The schema doc tells the AI to prefer these tools over read_file/write_file for single-element map edits"
  artifacts:
    - path: "mcp_server.py"
      provides: "find_map_element + edit_map_element MCP tools wrapping the data-map-edit endpoint"
      contains: "edit_map_element"
    - path: "docs/schemas/schema-encounters.md"
      provides: "Documentation of the resolver + edit tools and a pitfall steering away from whole-file rewrites"
      contains: "edit_map_element"
  key_links:
    - from: "mcp_server.py edit_map_element"
      to: "/api/gm/data-map-edit/<path>"
      via: "httpx POST"
      pattern: "data-map-edit"
    - from: "mcp_server.py find_map_element"
      to: "/api/gm/data-map-edit/<path>?resolve=1 (or dedicated resolve route)"
      via: "httpx GET/POST"
      pattern: "find_map_element"
---

<objective>
Expose the Plan 30-01 backend through two MCP tools and document them. `find_map_element(deckplan,
query)` returns candidate elements (id, kind, label, deck) for a natural reference — read-only,
tiny. `edit_map_element(deckplan, target, set?, add_poi?, remove_poi?)` performs the targeted
edit, returning the candidate list (not an error the AI should retry blindly) when the target is
ambiguous so the AI knows to ask the GM. Update `docs/schemas/schema-encounters.md` so the AI
reaches for these instead of read-modify-write.

Purpose: closes E-01..E-05 at the tool surface. Depends on 30-01 (the endpoint + resolver). The
backend should already expose resolution (either a `?resolve` mode on data-map-edit or a sibling
route — follow whatever 30-01 implemented).

Output: two MCP tools + schema documentation.
</objective>

<execution_context>
@/home/gjohnson/.claude/get-shit-done/workflows/execute-plan.md
@/home/gjohnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gjohnson/mothership/charon/.planning/phases/30-ai-map-editing-element-resolver-targeted-edit-mcp-tools/30-CONTEXT.md
@/home/gjohnson/mothership/charon/.planning/phases/30-ai-map-editing-element-resolver-targeted-edit-mcp-tools/30-01-PLAN.md
</context>

<interfaces>
From mcp_server.py (existing tool pattern — mirror it exactly):
  @mcp.tool
  async def patch_yaml(path: str, patch: dict) -> dict:
      async with httpx.AsyncClient() as client:
          r = await client.patch(f"{DJANGO_BASE_URL}/api/gm/data/{path}", json=patch, headers=...)
          r.raise_for_status()
          return r.json()
  - DJANGO_BASE_URL is module-level. append_list_item shows the POST-with-body idiom.
  - Tool docstrings are the AI's only guidance — be explicit about when to use each tool.

From Plan 30-01 backend:
  POST /api/gm/data-map-edit/<path>  body {target, set?, add_poi?, remove_poi?}
    200 {ok, element, kind, op, deck} | 404 {error, suggestions} | 409 {error:'ambiguous', candidates:[...]}
  Resolution surface (resolve-only) — confirm the exact route/param 30-01 chose before wiring.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: find_map_element + edit_map_element MCP tools</name>
  <files>mcp_server.py</files>
  <read_first>
    - mcp_server.py — read patch_yaml and append_list_item to copy the @mcp.tool + httpx + raise_for_status idiom and the DJANGO_BASE_URL usage
    - 30-01-PLAN.md / 30-01-SUMMARY.md — confirm the exact endpoint path, resolve surface, and response shapes implemented
  </read_first>
  <action>
    Add two tools mirroring the existing style.

    find_map_element(deckplan: str, query: str) -> dict
      - Calls the resolver surface from 30-01; returns {"matches":[{id,kind,label,deck}], "strategy", "suggestions"?}.
      - Docstring: "Resolve a natural map reference (room/corridor/door name, id, or glob like
        'coolant_tanks*') to candidate element ids WITHOUT loading the deckplan. Use this first when
        you are unsure which element the GM means, or call edit_map_element directly and let it tell
        you when the reference is ambiguous."

    edit_map_element(deckplan: str, target: str, set: dict = None, add_poi: dict = None,
                     remove_poi: str | int = None) -> dict
      - POSTs {target, set, add_poi, remove_poi} (omit None fields) to /api/gm/data-map-edit/<deckplan>.
      - On 409 ambiguous: return the candidates dict to the caller (do NOT raise) so the AI can ask the
        GM which element. On 404: return suggestions. On 200: return the result.
      - Docstring must state: exactly ONE of set / add_poi / remove_poi per call; `set` deep-merges
        fields (description, label_offset, position, door status, ...); `add_poi` appends a POI to a
        room ({icon, label, type?, position?, description?}); `remove_poi` takes a POI label or 0-based
        index. "Prefer this over read_file+write_file for changing a single room/door/POI — it does
        not load the whole deckplan." Note POIs are addressed through their room, never directly.
      - Keep ASCII-only guidance consistent with schema P1 (no em-dashes in YAML string values).
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && python -c "import ast; ast.parse(open('mcp_server.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both tools registered with @mcp.tool and follow the existing httpx idiom
    - edit_map_element omits None operation fields from the request body
    - 409 ambiguous returns candidates to the caller rather than raising
    - Docstrings clearly state the one-operation rule and steer away from whole-file rewrites
    - mcp_server.py parses
  </acceptance_criteria>
  <done>find_map_element + edit_map_element exposed; ambiguity surfaced as candidates; docstrings guide correct use.</done>
</task>

<task type="auto">
  <name>Task 2: Document the tools in schema-encounters.md</name>
  <files>docs/schemas/schema-encounters.md</files>
  <read_first>
    - docs/schemas/schema-encounters.md — read the MCP Path Conventions section and the existing pitfalls (P1, ...) to match tone/format
    - CLAUDE.md "Schema Sync" — this file is canonical and auto-syncs to janus-skills; edit it here, not the synced copy
  </read_first>
  <action>
    Add a short "Editing a single element" subsection near the MCP conventions:
      - find_map_element(deckplan, query) -> candidates (id/kind/label); use for disambiguation.
      - edit_map_element(deckplan, target, set|add_poi|remove_poi) -> targeted edit; one op per call.
      - Worked examples matching the discussion:
          edit_map_element(dp, "mess_hall", add_poi={icon: airlock, label: Airlock, position: {x:1,y:1}})
          edit_map_element(dp, "coolant_tanks", set={label_offset: [0, 2]})   # 409 if >1 coolant tank -> ask GM
          edit_map_element(dp, "executive_office__main_corridor", set={status: LOCKED})
      - Add a pitfall (next Pn number): "Do NOT read_file + write_file to change one room/door/POI —
        use edit_map_element; it edits server-side without loading the whole deckplan."
      - Note the id conventions from Plan 30-02 (slug room ids, <roomA>__<roomB> door ids).
    Keep tables tight and ASCII-only per P1.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && grep -n "edit_map_element" docs/schemas/schema-encounters.md | head</automated>
  </verify>
  <acceptance_criteria>
    - schema-encounters.md documents both tools with worked examples
    - A new pitfall steers the AI away from whole-file rewrites for single-element edits
    - Id conventions (room slugs, two-room door ids) are noted
    - Edited the canonical docs/schemas copy (not the janus-skills synced copy)
  </acceptance_criteria>
  <done>Schema doc explains find_map_element/edit_map_element with examples + a no-whole-file-rewrite pitfall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MCP client→Django | Tools call the unauthenticated data-map-edit endpoint (trust-network model, D-09) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-30-03-01 | Info disclosure | tool docstrings | accept | Docstrings describe behavior only; no secrets |
| T-30-03-02 | Tampering | edit via MCP | inherit | All validation/path-guarding handled by the 30-01 endpoint |
</threat_model>

<verification>
- mcp_server.py parses (ast.parse)
- With the dev server running: find_map_element on a real deckplan returns candidates; edit_map_element
  with set changes one field; an ambiguous target returns candidates not an exception
- schema-encounters.md contains the new subsection + pitfall
</verification>

<success_criteria>
- find_map_element and edit_map_element are available MCP tools wrapping the 30-01 endpoint
- Ambiguous references surface candidates for the AI to ask the GM
- Schema doc documents the tools and discourages whole-file rewrites
</success_criteria>

<output>
Create .planning/phases/30-ai-map-editing-element-resolver-targeted-edit-mcp-tools/30-03-SUMMARY.md when done
</output>
