---
phase: 27-mcp-image-upload
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - mcp_server.py
autonomous: true
requirements:
  - D-02
  - D-03

must_haves:
  truths:
    - "upload_image MCP tool exists in mcp_server.py with correct @mcp.tool signature"
    - "Tool accepts filename, content_base64, image_type, and optional convert (default True)"
    - "Tool POSTs to /api/gm/upload-image/ over httpx with timeout=60.0"
    - "Tool returns the JSON response dict from Django"
  artifacts:
    - path: "mcp_server.py"
      provides: "upload_image async tool function"
      exports: ["upload_image"]
  key_links:
    - from: "mcp_server.py upload_image"
      to: "Django /api/gm/upload-image/"
      via: "httpx.AsyncClient POST with JSON body"
      pattern: "client.post.*upload-image"
---

<objective>
Add the `upload_image` MCP tool to mcp_server.py. The tool is a thin HTTP delegate — it receives base64-encoded image data and forwards it to the Django endpoint created in Plan 01. It follows the exact pattern of the existing five MCP tools in the file.

Purpose: This is the interface the janus-skills (Plan 03) and any Claude Code agent will call. Without this tool the skills have no mechanism to upload images.

Output: Sixth @mcp.tool in mcp_server.py; mcp_server.py still passes Python syntax check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gjohnson/mothership/charon/.planning/ROADMAP.md

<interfaces>
<!-- Extracted from mcp_server.py (Plan 01 does not modify this file) -->

Existing tool pattern in mcp_server.py:
  @mcp.tool
  async def write_file(path: str, content: str) -> dict:
      """..."""
      async with httpx.AsyncClient() as client:
          r = await client.put(
              f"{DJANGO_BASE_URL}/api/gm/data/{path}",
              content=content,
              headers={"Content-Type": "application/x-yaml"},
          )
          r.raise_for_status()
          return r.json()

Module-level constant: DJANGO_BASE_URL = os.environ.get("DJANGO_BASE_URL", "http://app:8000")

Django endpoint created by Plan 01:
  POST /api/gm/upload-image/
  JSON body: {filename: str, content_base64: str, image_type: str, convert: bool}
  Returns: {saved_path, converted_path?, original_size_bytes}

FastMCP bool default syntax (verified FastMCP 3.3.1):
  async def my_tool(flag: bool = True) -> dict:  — works as-is; no special annotation needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add upload_image tool to mcp_server.py</name>
  <files>mcp_server.py</files>
  <read_first>
    - /home/gjohnson/mothership/charon/mcp_server.py — read in full; append new tool after the get_data_schema function, before the if __name__ == "__main__" guard
  </read_first>
  <action>
    Read mcp_server.py in full. Append the upload_image async tool function after get_data_schema and before the `if __name__ == "__main__":` block.

    The new function must have:
    - Decorator: @mcp.tool
    - Signature: async def upload_image(filename: str, content_base64: str, image_type: str, convert: bool = True) -> dict:
    - Docstring (multi-line): explain what the tool does, list the four image_type values and where each saves to, explain the convert parameter (portrait only, default True, triggers amber-gradient 512x512 conversion). End docstring with the return shape: {saved_path, converted_path?, original_size_bytes}.
    - Body: async with httpx.AsyncClient(timeout=60.0) as client: — use timeout=60.0 because Pillow conversion of a large portrait can take several seconds
    - POST to f"{DJANGO_BASE_URL}/api/gm/upload-image/"
    - json= body: {"filename": filename, "content_base64": content_base64, "image_type": image_type, "convert": convert}
    - r.raise_for_status()
    - return r.json()

    No other changes to mcp_server.py. Do not modify existing tools, imports, or the __main__ block.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && python -c "import mcp_server; print('OK')" && python -c "import ast; ast.parse(open('mcp_server.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    mcp_server.py imports without error; upload_image function is defined with @mcp.tool; timeout=60.0 is present; POST target is /api/gm/upload-image/; convert parameter has default True.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → mcp_server.py | Untrusted tool call arguments from Claude Code agent |
| mcp_server.py → Django API | HTTP POST; Django view is the authoritative security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | upload_image filename param | transfer | Django view (Plan 01) is the authoritative path-traversal guard; MCP tool passes through unchanged |
| T-27-02-02 | DoS | large content_base64 string | accept | Trust-network model; httpx timeout=60.0 bounds the request duration |
| T-27-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; existing httpx and fastmcp used |
</threat_model>

<verification>
```bash
cd /home/gjohnson/mothership/charon
python -c "import mcp_server; print([t for t in dir(mcp_server) if 'upload' in t])"
grep -n 'upload_image' mcp_server.py
grep -n 'timeout=60' mcp_server.py
```
</verification>

<success_criteria>
- mcp_server.py imports without error
- upload_image is defined with @mcp.tool decorator
- Function signature: (filename: str, content_base64: str, image_type: str, convert: bool = True) -> dict
- httpx.AsyncClient(timeout=60.0) used (not the default no-timeout form)
- POST target: /api/gm/upload-image/
- JSON body passes all four parameters through to Django
</success_criteria>

<output>
Create `/home/gjohnson/mothership/charon/.planning/phases/27-mcp-image-upload/27-02-SUMMARY.md` when done.
</output>
