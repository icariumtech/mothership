---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - /home/gjohnson/mothership/janus-skills/install.sh
  - /home/gjohnson/mothership/janus-skills/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "install.sh --global symlinks all skills/janus-*/ subdirectories to ~/.claude/skills/"
    - "install.sh --project <path> symlinks all skills/janus-*/ subdirectories to <path>/.claude/skills/"
    - "install.sh always copies/symlinks resources/*.md to ~/.claude/janus-skills/resources/ regardless of skills mode"
    - "install.sh --mcp-config merges (not overwrites) the JanusGM block into target .claude/settings.json"
    - "install.sh prompts the user for the homelab server IP and substitutes HOMELAB_IP in the merged config"
    - "README.md covers prerequisites, clone + install steps, --mcp-config usage, and all 8 skills with one-line descriptions"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/install.sh"
      provides: "Installation script implementing D-08 (modes), D-09 (symlinks), D-10 (MCP config)"
      min_lines: 80
    - path: "/home/gjohnson/mothership/janus-skills/README.md"
      provides: "Setup guide for users — prerequisites, install steps, skill reference table"
  key_links:
    - from: "install.sh"
      to: "~/.claude/janus-skills/resources/"
      via: "always creates this dir (required for @$HOME @-includes to resolve)"
      pattern: "HOME/.claude/janus-skills/resources"
    - from: "install.sh --mcp-config"
      to: ".claude/settings.json"
      via: "python3 JSON merge — never full overwrite"
      pattern: "json.load|json.dump"
---

<objective>
Author `~/mothership/janus-skills/install.sh` implementing D-08 (two install modes), D-09 (symlinks), and D-10 (optional MCP config injection). The script must be safe (set -euo pipefail), idempotent (rerunning replaces existing symlinks cleanly), and self-validating (refuse conflicting flags, refuse missing project path).

Purpose: This is the deployment surface for the entire phase. Users install janus-skills by running `./install.sh --global` or `./install.sh --project <path>`. The script is the only thing standing between an empty repo and a working `/janus-*` skill set in Claude Code.

Output: One executable bash script, ~120–180 lines, fully documented inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-01-SUMMARY.md

<interfaces>
<!-- Install destinations -->
- Global skills:  $HOME/.claude/skills/
- Project skills: <project>/.claude/skills/
- Resources:      $HOME/.claude/janus-skills/resources/   (ALWAYS, regardless of mode — per Pitfall P3)
- MCP settings:   $HOME/.claude/settings.json (global)  OR  <project>/.claude/settings.json (project)

<!-- Skill directory layout in this repo (populated by plans 04 + 05) -->
$REPO/skills/janus-add-npc/SKILL.md
$REPO/skills/janus-add-location/SKILL.md
$REPO/skills/janus-add-system/SKILL.md
$REPO/skills/janus-add-body/SKILL.md
$REPO/skills/janus-add-ship/SKILL.md
$REPO/skills/janus-update-galaxy/SKILL.md
$REPO/skills/janus-generate-context/SKILL.md
$REPO/skills/janus-session-prep/SKILL.md

<!-- MCP config template (written by plan 01, available at $REPO/mcp-config-template.json) -->
{
  "mcpServers": {
    "JanusGM": {
      "type": "http",
      "url": "http://HOMELAB_IP:8001/mcp/",
      "description": "JANUS GM campaign data server — reads and writes campaign YAML files"
    }
  }
}

<!-- CLI surface to implement -->
./install.sh --global                            # install skills to ~/.claude/skills/, resources to ~/.claude/janus-skills/resources/
./install.sh --project <path>                    # install skills to <path>/.claude/skills/, resources same as above
./install.sh --global --mcp-config               # additionally merge MCP block into ~/.claude/settings.json (prompts for IP)
./install.sh --project <path> --mcp-config       # additionally merge MCP block into <path>/.claude/settings.json
./install.sh --help                              # usage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author install.sh implementing all three flags + JSON merge helper</name>
  <files>
    /home/gjohnson/mothership/janus-skills/install.sh
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
    /home/gjohnson/mothership/janus-skills/mcp-config-template.json
  </read_first>
  <action>
    Create `/home/gjohnson/mothership/janus-skills/install.sh` as a bash script implementing the contract in `<interfaces>`. After writing, run `chmod +x install.sh` so it is executable.

    **Required structural elements (each must be present, verifiable by grep):**

    1. Shebang line: exactly `#!/usr/bin/env bash`
    2. Safety: `set -euo pipefail` near the top
    3. `REPO_DIR` computed as `cd "$(dirname "${BASH_SOURCE[0]}")" && pwd` so the script works from any cwd
    4. Flag parsing loop accepting: `--global`, `--project <path>`, `--mcp-config`, `--help` / `-h`. Unknown flags exit with usage.
    5. Validation:
       - Reject if both `--global` and `--project` provided (error message + exit 1)
       - Reject if neither provided (print usage + exit 1)
       - If `--project <path>`: require path to be an existing directory (use `[[ -d "$PROJECT_PATH" ]]`)
    6. Compute `SKILLS_DIR`:
       - `--global` → `$HOME/.claude/skills`
       - `--project <path>` → `<path>/.claude/skills`
    7. Compute `RESOURCES_DIR` (ALWAYS): `$HOME/.claude/janus-skills/resources` — per Pitfall P3 (must be stable HOME path so `@$HOME/.claude/janus-skills/resources/...` @-includes resolve regardless of install mode).
    8. `mkdir -p` both directories before symlinking.
    9. Symlink resources: loop over `$REPO_DIR/resources/*.md`, for each file create symlink `$RESOURCES_DIR/$(basename "$f")` pointing to `$(realpath "$f")`. Use `ln -sfn` (force, no-deref) so reruns replace existing links without nesting.
    10. Symlink skills: loop over `$REPO_DIR/skills/janus-*/` directories. For each `<skill-dir>` create symlink at `$SKILLS_DIR/$(basename "$skill_dir")` → `$(realpath "$skill_dir")`. Use `ln -sfn`.
    11. After both loops, print a summary count (e.g., `Installed N skills + M resources`).
    12. If `--mcp-config` flag set:
        a. Prompt user for homelab IP using `read -r -p "Homelab server IP (e.g., 192.168.1.42): " HOMELAB_IP`
        b. Validate `HOMELAB_IP` is non-empty (refuse blank input with retry or error)
        c. Determine target settings file: `$HOME/.claude/settings.json` (global) or `$PROJECT_PATH/.claude/settings.json` (project)
        d. Ensure target directory exists (`mkdir -p` parent)
        e. Use a heredoc-embedded Python 3 snippet to merge the JanusGM block — NOT a full overwrite. The Python must:
           - Load existing settings.json if it exists (else start with `{}`)
           - Ensure `mcpServers` key exists as a dict
           - Load `$REPO_DIR/mcp-config-template.json`, extract `mcpServers.JanusGM`, replace the literal string `HOMELAB_IP` in its `url` with the user's IP
           - Set existing[`mcpServers`][`JanusGM`] = merged block (overwrites only the JanusGM entry; preserves other servers)
           - Write back with `json.dump(..., indent=2)`
        f. Print confirmation: "MCP config written to <path>"
        g. Pre-flight check: if `python3` not on PATH, error with message "python3 required for --mcp-config" and exit 1
    13. Exit 0 on success.

    **Idempotency requirement:** Running `./install.sh --global` twice in a row MUST succeed both times and produce no nested symlinks. (Achieved by using `ln -sfn`.)

    **Help text:** `--help` / `-h` prints usage covering all flags + an example invocation, then exits 0.

    Do NOT inline fenced code blocks describing implementation — write the actual bash. Use comments inside the script to explain each section.

    After writing, run `bash -n install.sh` to verify it parses (syntax check). If any error, fix and retry.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && bash -n install.sh && [ -x install.sh ] && grep -q "set -euo pipefail" install.sh && grep -q "HOME/.claude/janus-skills/resources" install.sh && grep -q "ln -sfn" install.sh && grep -q "mcp-config-template.json" install.sh && grep -q 'python3' install.sh && grep -q -- "--global" install.sh && grep -q -- "--project" install.sh && grep -q -- "--mcp-config" install.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/home/gjohnson/mothership/janus-skills/install.sh` with mode bits including user-execute (`test -x install.sh` succeeds)
    - First line is exactly `#!/usr/bin/env bash`
    - Contains literal `set -euo pipefail`
    - `bash -n install.sh` returns 0 (parses cleanly)
    - Contains all three flag strings as literals: `--global`, `--project`, `--mcp-config`
    - Contains the literal path fragment `HOME/.claude/janus-skills/resources` (used for `RESOURCES_DIR`)
    - Uses `ln -sfn` (verifiable via grep) — never `ln -s` without `-f`
    - Contains `realpath` invocation for symlink targets
    - Contains a `python3` invocation (heredoc) for JSON merge — NOT `jq`
    - Contains the literal string `HOMELAB_IP` (placeholder replacement target)
    - Line count between 80 and 250 (`wc -l install.sh`)
    - Smoke test: `bash install.sh --help` exits 0 and prints usage to stdout containing all three flag names
    - Smoke test: `bash install.sh` (no args) exits non-zero and prints usage
    - Smoke test: `bash install.sh --global --project /tmp/test` (both flags) exits non-zero with error about conflicting flags
    - Smoke test: `bash install.sh --project /nonexistent/path/xyz123` exits non-zero (path validation)
    - Functional smoke test: create `/tmp/janus-test-proj/.claude/`, then `bash install.sh --project /tmp/janus-test-proj` succeeds AND creates symlinks at `/tmp/janus-test-proj/.claude/skills/` for each skill dir present in `$REPO/skills/` (since skills/ is empty at this wave, the loop should still succeed without error — verify with `bash install.sh --project /tmp/janus-test-proj 2>&1` returning 0)
    - Functional smoke test: after above run, `ls -la $HOME/.claude/janus-skills/resources/` shows symlinks for the 5 schema files (resources exist from plan 24-02)
    - Rerun smoke test: running the same `bash install.sh --project /tmp/janus-test-proj` a second time also exits 0 (idempotent)
  </acceptance_criteria>
  <done>
    `install.sh` is executable, parses cleanly, handles all three flags correctly (validation + happy path), implements stable resources path, uses safe JSON merge via python3, and is idempotent on rerun.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README.md</name>
  <files>
    /home/gjohnson/mothership/janus-skills/README.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
    /home/gjohnson/mothership/janus-skills/install.sh
    /home/gjohnson/mothership/janus-skills/mcp-config-template.json
  </read_first>
  <action>
    Create `/home/gjohnson/mothership/janus-skills/README.md` covering exactly these sections in order:
    1. `# JANUS Skills` — one-sentence project description and a link/mention of `github.com/icariumtech/janus-skills`
    2. `## Prerequisites` — JANUS server running via docker compose (Phase 23), Claude Code ≥ latest stable, Python 3 (for --mcp-config)
    3. `## Installation` — three code blocks: (a) clone + install globally via `./install.sh --global`, (b) add MCP config via `./install.sh --global --mcp-config` (prompts for homelab IP), (c) project-local alternative via `./install.sh --project <path>`
    4. `## Skills Reference` — markdown table with columns: Skill | Description | Example; one row per skill (8 total). Use the exact skill names from CONTEXT.md D-11 through D-18 (`/janus-generate-context`, `/janus-add-npc`, `/janus-add-location`, `/janus-session-prep`, `/janus-add-system`, `/janus-add-body`, `/janus-add-ship`, `/janus-update-galaxy`). Keep descriptions to one line.
    5. `## Schema Resources` — brief paragraph noting that `resources/` contains condensed YAML schema references installed to `~/.claude/janus-skills/resources/`. Skills @-include the relevant slice.
    6. `## Troubleshooting` — two items: (a) skills not appearing — restart Claude Code after install; (b) MCP connection refused — verify JANUS server is running (`docker compose ps`), check port 8001, confirm `JanusGM` key in settings.json.
    Target: ~60–90 lines. No internal implementation details — user-facing only.
  </action>
  <verify>
    <automated>test -f /home/gjohnson/mothership/janus-skills/README.md && grep -q "janus-generate-context" /home/gjohnson/mothership/janus-skills/README.md && grep -q "janus-session-prep" /home/gjohnson/mothership/janus-skills/README.md && grep -q "install.sh" /home/gjohnson/mothership/janus-skills/README.md && grep -q "Prerequisites" /home/gjohnson/mothership/janus-skills/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/home/gjohnson/mothership/janus-skills/README.md`
    - Contains all 8 skill names (grep for `janus-generate-context` AND `janus-session-prep` AND `janus-add-ship`)
    - Contains heading `## Prerequisites`
    - Contains heading `## Installation`
    - Contains heading `## Skills Reference`
    - Contains heading `## Troubleshooting`
    - Contains the literal string `install.sh --global`
    - Contains the literal string `install.sh --mcp-config` (or `--global --mcp-config`)
    - Line count between 50 and 120 (`wc -l README.md`)
  </acceptance_criteria>
  <done>
    README.md written covering prerequisites, install steps (global + project + MCP config), skill reference table (8 skills), schema resources note, and troubleshooting.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit install.sh + README.md + clean up smoke test artifacts</name>
  <files>
    /home/gjohnson/mothership/janus-skills/install.sh (already created in Task 1)
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/install.sh
    /home/gjohnson/mothership/janus-skills/README.md
  </read_first>
  <action>
    Clean up smoke test artifacts created in Task 1 verification:
    1. `rm -rf /tmp/janus-test-proj` (smoke test project)
    2. Remove any test symlinks accidentally created in `$HOME/.claude/skills/janus-*` — but ONLY if Task 1 ran the global smoke test. The `--project` smoke test is contained under `/tmp/janus-test-proj` so only it needs removal. Resources at `$HOME/.claude/janus-skills/resources/` should be LEFT in place (they are the real install — plans 04 and 05 will rely on them).

    Verify cleanup: `test ! -d /tmp/janus-test-proj` should succeed.

    Then commit install.sh + README.md. From `/home/gjohnson/mothership/janus-skills`:
    1. `git add install.sh README.md`
    2. Commit with message:
       ```
       feat(install): add install.sh + README

       install.sh implements D-08 (two install modes), D-09 (symlinks via ln -sfn
       for idempotent reruns), and D-10 (MCP config injection via python3 JSON
       merge — preserves existing mcpServers entries, replaces HOMELAB_IP with
       user-provided IP).

       Resources always install to $HOME/.claude/janus-skills/resources/ regardless
       of skills install mode so @$HOME/-anchored @-includes resolve correctly.

       README covers prerequisites, install steps, skill reference table, and
       troubleshooting.

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```
  </action>
  <verify>
    <automated>test ! -d /tmp/janus-test-proj && git -C /home/gjohnson/mothership/janus-skills log --oneline | head -1 | grep -q "feat(install)" && git -C /home/gjohnson/mothership/janus-skills status --porcelain | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/janus-test-proj` does not exist
    - Latest commit subject starts with `feat(install):`
    - `git -C /home/gjohnson/mothership/janus-skills show --name-only HEAD` lists both `install.sh` and `README.md`
    - Working tree clean (`git status --porcelain` empty)
    - `ls -la $HOME/.claude/janus-skills/resources/` still shows the 5 schema symlinks (smoke install left intact)
  </acceptance_criteria>
  <done>
    install.sh and README.md committed. Smoke test scratch directory removed. The real resource symlinks at `$HOME/.claude/janus-skills/resources/` remain so downstream plans can reference them.
  </done>
</task>

</tasks>

<verification>
- `bash -n /home/gjohnson/mothership/janus-skills/install.sh` returns 0
- `bash /home/gjohnson/mothership/janus-skills/install.sh --help` exits 0 and lists all three flags
- `ls -la $HOME/.claude/janus-skills/resources/` shows 5 symlinks (real install from smoke test left in place)
- Git log on janus-skills shows: initial, resources, install — 3 commits, clean tree
</verification>

<success_criteria>
- install.sh implements all D-08/D-09/D-10 behavior with safe defaults (set -euo pipefail, ln -sfn, JSON merge not overwrite)
- Script is idempotent: rerunning produces identical state, no nested symlinks
- Resources install path is fixed (`$HOME/.claude/janus-skills/resources/`) regardless of mode
- Script committed with descriptive message referencing decisions
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-03-SUMMARY.md` recording:
- Commit hash for install.sh
- Final line count of install.sh
- Smoke test results (--help exit, --project happy path, idempotency rerun)
- Confirmation that resource symlinks at `$HOME/.claude/janus-skills/resources/` exist (5 files) and were created by the smoke run
</output>
