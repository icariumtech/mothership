---
phase: 12-requirements-tracking-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
files_deleted:
  - src/components/gm/ShipStatusPanel.tsx
  - src/components/gm/panels/ShipStatusToolPanel.tsx
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - GRID-05
  - GRID-06
  - GRID-07
  - GRID-08
  - GRID-09
  - GRID-10
must_haves:
  truths:
    - "GRID-01 through GRID-10 are all checked [x] in REQUIREMENTS.md"
    - "GRID-01 through GRID-10 show Complete in the REQUIREMENTS.md Traceability table"
    - "All completed plan entries in ROADMAP.md show [x] checkboxes"
    - "Phases 3, 4, 5 show [x] in the top-level Phases list in ROADMAP.md"
    - "ShipStatusPanel.tsx and ShipStatusToolPanel.tsx no longer exist on disk"
    - "TypeScript typecheck and build still pass after dead code deletion"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "20 edits: 10 checkbox flips + 10 traceability status updates"
      contains: "[x] **GRID-01"
    - path: ".planning/ROADMAP.md"
      provides: "34 checkbox flips across phases 3-5, 7-11, 13"
      contains: "[x] 07-01-PLAN.md"
  key_links: []
---

<objective>
Bring REQUIREMENTS.md and ROADMAP.md into alignment with actual implemented state: check off 10 completed GRID requirements, update 34 ROADMAP plan checkboxes, update the traceability table, and delete two dead code files from the Phase 09 refactor.

Purpose: Close tracking gaps identified in v1.0-MILESTONE-AUDIT.md so downstream phases (15-19, 20) start from an accurate baseline.
Output: Updated REQUIREMENTS.md, updated ROADMAP.md, two dead files removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-requirements-tracking-cleanup/12-CONTEXT.md
@.planning/phases/12-requirements-tracking-cleanup/12-RESEARCH.md
@.planning/phases/12-requirements-tracking-cleanup/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code files (per D-03)</name>
  <files>
    src/components/gm/ShipStatusPanel.tsx (DELETE)
    src/components/gm/panels/ShipStatusToolPanel.tsx (DELETE)
  </files>
  <action>
Delete these two files outright. Both have zero active importers (confirmed by grep across src/).

1. Before deleting, run a final safety grep to confirm no file in src/ imports either component:
   `grep -r "ShipStatusPanel\|ShipStatusToolPanel" src/ --include="*.tsx" --include="*.ts" -l`
   Expected result: only the two files themselves. If ANY other file imports them, STOP and report.

2. Delete both files:
   `rm src/components/gm/ShipStatusPanel.tsx`
   `rm src/components/gm/panels/ShipStatusToolPanel.tsx`

3. Run `npm run typecheck` to confirm no import breakage.

No barrel exports or index files reference these components — no additional cleanup needed.

Commit: `chore(12): delete dead ShipStatusPanel + ShipStatusToolPanel`
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && npm run typecheck</automated>
  </verify>
  <done>
Both files deleted. TypeScript typecheck passes with zero errors. No remaining imports reference either component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Check off GRID-01..10 in REQUIREMENTS.md (per D-01)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read REQUIREMENTS.md and make exactly 20 edits:

**Edit Group 1 — GRID checkboxes (lines 55-64):**
Change `- [ ]` to `- [x]` on all 10 GRID requirement lines. Do NOT modify the description text after the requirement ID. No deviation annotations per D-01. The 10 lines are:
- `- [ ] **GRID-01**: New TypeScript types...` becomes `- [x] **GRID-01**: New TypeScript types...`
- `- [ ] **GRID-02**: YAML map files...` becomes `- [x] **GRID-02**: YAML map files...`
- (same pattern for GRID-03 through GRID-10)

**Edit Group 2 — Traceability table Status column (lines 169-178):**
Change `Pending` to `Complete` for all 10 GRID rows:
- `| GRID-01 | Phase 7 | Pending |` becomes `| GRID-01 | Phase 7 | Complete |`
- (same pattern for GRID-02 through GRID-10)

**DO NOT TOUCH:**
- PORT-03 checkbox (line ~42) — already `[x]`, leave it
- STAT-10..14 entries — remain `[ ]`, deferred to Phase 20
- Phase 12 gap closures section (lines ~213-218) — already correct
- Any prose, section headers, or coverage stats

Commit: `docs(12): check off GRID-01..10 requirements + update traceability table`
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && grep -c '\[x\] \*\*GRID-' .planning/REQUIREMENTS.md | grep -q '^10$' && grep 'GRID-' .planning/REQUIREMENTS.md | grep -c 'Complete' | grep -q '1[01]' && echo "PASS: 10 GRID checkboxes + traceability updated" || echo "FAIL"</automated>
  </verify>
  <done>
All 10 GRID requirements show [x] in the checkbox list. All 10 GRID rows in the Traceability table show Status=Complete. No other lines were modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sync ROADMAP.md plan checkboxes (per D-04)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read ROADMAP.md and flip exactly 34 checkboxes from `[ ]` to `[x]`. This is CHECKBOXES ONLY per D-04 — do NOT edit any prose, headers, success criteria, "X/Y plans executed" strings, or the Progress table.

**Top-level Phases list (3 changes):**
- Phase 3 line: `- [ ] **Phase 3: Encounter Tokens**` becomes `- [x] **Phase 3: Encounter Tokens**`
- Phase 4 line: `- [ ] **Phase 4: NPC Portrait System**` becomes `- [x] **Phase 4: NPC Portrait System**`
- Phase 5 line: `- [ ] **Phase 5: Real-Time Push Architecture**` becomes `- [x] **Phase 5: Real-Time Push Architecture**`

**Phase 3 plan entries (4 changes):** All four 03-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 4 plan entries (4 changes):** All four 04-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 5 plan entries (4 changes):** All four 05-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 7 plan entries (4 changes):** All four 07-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 8 plan entries (4 changes):** All four 08-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 9 plan entries (2 changes):** Both 09-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 10 plan entries (3 changes):** All three 10-*-PLAN.md lines: `[ ]` to `[x]`
**Phase 11 plan entry (1 change):** 11-01-PLAN.md line: `[ ]` to `[x]`
**Phase 13 plan entries (5 changes):** All five 13-*-PLAN.md lines: `[ ]` to `[x]`

**Phase 12 self-check:** Also flip the Phase 12 plan entry:
- `- [ ] 12-01-PLAN.md — REQUIREMENTS.md registration + ROADMAP.md sync + dead code removal` becomes `[x]`
This makes the total 35 checkbox flips.

**DO NOT TOUCH:**
- Phase 6 entry — stays `[ ]` (deferred to v2)
- Phase 14 plan entries — already `[x]`
- Phase 15-19, 20 plan entries — stay `[ ]` (not yet executed)
- Progress table — already accurate
- Any "Depends on", "Goal", "Requirements", "Success Criteria" prose
- Any "X/Y plans executed" or "X/Y plans complete" header text (Phase 4: "3/4 plans executed", Phase 5: "3/4 plans executed", Phase 8: "3/4 plans executed" — leave these as-is even though they are numerically wrong)

Commit: `docs(12): sync ROADMAP.md plan checkboxes to match execution state`
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && UNCHECKED=$(grep -c '^\- \[ \]' .planning/ROADMAP.md); echo "Remaining unchecked: $UNCHECKED"; test "$UNCHECKED" -le 12 && echo "PASS: most checkboxes synced" || echo "FAIL: too many unchecked boxes remain"</automated>
  </verify>
  <done>
34 plan checkboxes flipped from [ ] to [x] across phases 3-5, 7-11, 13. Phase 12 self-checked. Only Phase 6 (deferred), Phases 15-20 (not yet executed) remain [ ]. No prose was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase modifies only planning docs and deletes unused TypeScript files. No runtime code changes. No API surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | .planning/REQUIREMENTS.md | accept | Planning docs are not served to users; git history preserves audit trail |
| T-12-02 | Tampering | .planning/ROADMAP.md | accept | Planning docs are not served to users; git history preserves audit trail |
| T-12-03 | Denial of Service | Dead code deletion | accept | Files have zero importers; typecheck confirms no breakage |

No high-severity threats. This phase has negligible threat surface.
</threat_model>

<verification>
After all three tasks complete:

1. `npm run typecheck` — must pass (confirms dead code deletion broke nothing)
2. `npm run build` — must pass (confirms full build integrity)
3. `grep -c '\[x\] \*\*GRID-' .planning/REQUIREMENTS.md` — must return 10
4. `ls src/components/gm/ShipStatusPanel.tsx 2>/dev/null` — must return nothing (file deleted)
5. `ls src/components/gm/panels/ShipStatusToolPanel.tsx 2>/dev/null` — must return nothing (file deleted)
6. `grep '^\- \[ \]' .planning/ROADMAP.md` — remaining unchecked entries should only be Phase 6 and Phases 15-20
</verification>

<success_criteria>
1. GRID-01 through GRID-10 all show `[x]` in REQUIREMENTS.md (per D-01)
2. GRID-01 through GRID-10 all show `Complete` in REQUIREMENTS.md Traceability table
3. ShipStatusPanel.tsx and ShipStatusToolPanel.tsx deleted from disk (per D-03)
4. 34+ ROADMAP.md plan checkboxes flipped from `[ ]` to `[x]` (per D-04 — checkboxes only, no prose)
5. `npm run typecheck && npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-requirements-tracking-cleanup/12-01-SUMMARY.md`
</output>
