---
phase: 22-renderer-interaction-seams
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - src/components/domain/encounter/EncounterMapRenderer.tsx
autonomous: false
requirements: []
tags:
  - verification
  - smoke-test
  - cleanup

must_haves:
  truths:
    - "TypeScript compiler reports zero errors across the project"
    - "Vite production build completes successfully"
    - "EncounterMapRenderer.tsx imports list is minimal — no orphaned imports left over from the two extractions"
    - "Manual smoke test confirms popover exclusivity AND token-placement behavior in a running browser session"
    - "Renderer LOC has decreased (was 1302 at phase start; expect ~1240–1260 after both extractions)"
  artifacts:
    - path: "src/components/domain/encounter/EncounterMapRenderer.tsx"
      provides: "Verified, cleaned-up renderer with both seams in place"
      contains: "useExclusivePopover<EncounterPopover>"
    - path: ".planning/phases/22-renderer-interaction-seams/22-03-SMOKE-LOG.md"
      provides: "Recorded smoke-test evidence from human verification"
  key_links:
    - from: "src/components/domain/encounter/EncounterMapRenderer.tsx"
      to: "src/hooks/useExclusivePopover.ts + src/hooks/useTokenPlacement.ts"
      via: "named imports"
      pattern: "import \\{ (useExclusivePopover|useTokenPlacement) \\} from"
---

<objective>
Verify that Plans 01 and 02 landed cleanly: TypeScript clean, build green, no orphaned imports or dead code in the renderer, and human-confirmed smoke evidence that all four popover slots and all three drop-guard branches still work end-to-end in the running app. Record the smoke evidence so the phase summary can cite it without rerunning manual steps.

Purpose: Phase 22 closes the renderer interaction-extraction work started in Phase 21. This plan is the gate that says "the renderer is done as a decomposition target." Without explicit verification, regressions in this kind of refactor frequently slip through (popover state coupling, drag handler ref drift, etc.).

Output:
- Verified clean `EncounterMapRenderer.tsx` (orphan imports removed if any remain)
- New `22-03-SMOKE-LOG.md` capturing the human verification evidence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-renderer-interaction-seams/22-CONTEXT.md
@.planning/phases/22-renderer-interaction-seams/22-01-PLAN.md
@.planning/phases/22-renderer-interaction-seams/22-02-PLAN.md

<interfaces>
<!-- Post-extraction renderer surface (after Plans 01 and 02). -->
<!-- These names appear in the renderer and are the verification anchors. -->

In EncounterMapRenderer.tsx after Plans 01 + 02:
- `import { useExclusivePopover } from '../../../hooks/useExclusivePopover';`
- `import { useTokenPlacement } from '../../../hooks/useTokenPlacement';`
- `type EncounterPopover = | { type: 'token'; ... } | { type: 'door'; ... } | { type: 'poi'; ... } | { type: 'room'; ... }` at module scope
- `const { popover, open: openPopover, close: closePopover } = useExclusivePopover<EncounterPopover>();`
- `const { handleDragOver, handleDrop } = useTokenPlacement({ svgRef, unitSize, isGM, onTokenPlace, isCellOccupied, findRoomAtCell });`
- Four render-guard sites narrow on `popover?.type === 'token' | 'door' | 'poi' | 'room'`
- JSX container still consumes `onDragOver={handleDragOver} onDrop={handleDrop}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final renderer cleanup and static verification</name>
  <files>src/components/domain/encounter/EncounterMapRenderer.tsx</files>
  <read_first>
    - src/components/domain/encounter/EncounterMapRenderer.tsx (full file as it stands after Plans 01 + 02)
    - src/hooks/useExclusivePopover.ts (confirm Plan 01 landed)
    - src/hooks/useTokenPlacement.ts (confirm Plan 02 landed)
    - .planning/phases/22-renderer-interaction-seams/22-01-PLAN.md (success criteria of Plan 01 — confirm Step F about unused `useState` was done)
    - .planning/phases/22-renderer-interaction-seams/22-02-PLAN.md (success criteria of Plan 02 — confirm Step D about unused `getGridCell`/`message`/`TokenType` was done)
  </read_first>
  <action>
    Run static checks against `EncounterMapRenderer.tsx` and clean any leftover orphans:

    1. Grep for unused imports likely orphaned by Plans 01 and 02. For each candidate, count remaining non-import usages; if zero, remove from the import statement. Candidates to audit (run these greps explicitly):
       - `useState` from react: `grep -c "useState" src/components/domain/encounter/EncounterMapRenderer.tsx` — if the only match is the import line, remove `useState` from the destructured import.
       - `getGridCell` from `@/utils/svgCoordinates` (or relative path `../../../utils/svgCoordinates`): if `grep -c "getGridCell" src/components/domain/encounter/EncounterMapRenderer.tsx` returns 1 (the import) or 0, drop the import line.
       - `message` from antd: if `grep -c "message\." src/components/domain/encounter/EncounterMapRenderer.tsx` returns 0, drop `import { message } from 'antd';`.
       - `TokenType` from `../../../types/encounterMap`: if `grep -c "TokenType" src/components/domain/encounter/EncounterMapRenderer.tsx` returns 1 (the import) or 0, remove from the type-import group. Keep the rest of the import group intact.

    2. Verify the `EncounterPopover` discriminated union is still defined at module scope above the component function — confirm `grep -n "type EncounterPopover" src/components/domain/encounter/EncounterMapRenderer.tsx` returns exactly one line above the `export function EncounterMapRenderer(` declaration.

    3. Confirm no inline popover useState calls slipped through: `grep -E "useState<\\{|useState<string \\| null>" src/components/domain/encounter/EncounterMapRenderer.tsx` should return zero lines.

    4. Confirm no inline drag callbacks slipped through: `grep -E "const handle(DragOver|Drop) = useCallback" src/components/domain/encounter/EncounterMapRenderer.tsx` should return zero lines.

    5. Run `npx tsc --noEmit -p .` — must exit 0 with no error output. If errors appear, fix them (most likely an unused-import warning under noUnusedLocals, which step 1 above should preempt).

    6. Run `npm run build` — must exit 0.

    7. Capture renderer LOC after cleanup: `wc -l src/components/domain/encounter/EncounterMapRenderer.tsx`. Phase started at 1302 LOC. Record the post-extraction LOC for inclusion in the summary. Expect a net reduction (rough target ≥ 30 LOC removed: 5 useState blocks collapsed to 1, plus 2 inline useCallback blocks removed, minus the new `EncounterPopover` union definition).

    Do NOT introduce new files, refactor any code beyond removing orphan imports, or modify behavior. If a typecheck error appears that requires real changes (not an unused-import case), STOP and surface the diagnostic verbatim instead of fixing speculatively.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&amp;1 | grep -c error | xargs -I{} test {} -eq 0 &amp;&amp; echo "ok: 0 typecheck errors"</automated>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
    <automated>grep -cE "useState<\{|useState<string \| null>" src/components/domain/encounter/EncounterMapRenderer.tsx | xargs -I{} test {} -eq 0 &amp;&amp; echo "ok: no popover useState"</automated>
    <automated>grep -cE "const handle(DragOver|Drop) = useCallback" src/components/domain/encounter/EncounterMapRenderer.tsx | xargs -I{} test {} -eq 0 &amp;&amp; echo "ok: no inline drag callbacks"</automated>
    <automated>wc -l src/components/domain/encounter/EncounterMapRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p .` exits 0
    - `npm run build` exits 0
    - Renderer file contains exactly one `useExclusivePopover<EncounterPopover>(` call site
    - Renderer file contains exactly one `useTokenPlacement({` call site
    - Renderer file contains exactly one `type EncounterPopover` declaration
    - Renderer file contains zero matches for `useState<\{|useState<string \| null>` (no popover useState orphans)
    - Renderer file contains zero matches for `const handle(DragOver|Drop) = useCallback`
    - For each of `useState` (react), `getGridCell`, `message`, `TokenType`: either still used in the file (grep > 1) OR removed from imports (grep == 0)
    - Renderer LOC < 1302 (was the phase-start value); LOC change recorded in this task's execution notes
  </acceptance_criteria>
  <done>Static gates pass, orphan imports gone, LOC reduction recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke verification</name>
  <files>.planning/phases/22-renderer-interaction-seams/22-03-SMOKE-LOG.md</files>
  <read_first>
    - .planning/phases/22-renderer-interaction-seams/22-CONTEXT.md (D-09 — T3 strategy means human smoke is the test bar)
    - .planning/phases/22-renderer-interaction-seams/22-01-PLAN.md must_haves.truths (popover exclusivity behavior)
    - .planning/phases/22-renderer-interaction-seams/22-02-PLAN.md must_haves.truths (token placement guard behavior)
  </read_first>
  <what-built>
    Plans 01 and 02 extracted `useExclusivePopover` and `useTokenPlacement` from `EncounterMapRenderer.tsx`. Task 1 of this plan verified the static gates (typecheck, build, grep gates). What still needs human eyes: confirming the live behavior in a browser matches the prior behavior, since T3 (no @testing-library/react) means human smoke IS the test (per D-09).
  </what-built>
  <how-to-verify>
    Boot the app and walk through both behavior groups. Record outcomes in `.planning/phases/22-renderer-interaction-seams/22-03-SMOKE-LOG.md`.

    Prerequisite: ensure dev server is running. From repo root:
    ```
    ./start_server.sh   # or: npm run dev (Vite) + Django backend per CLAUDE.md
    ```
    Open `/gmconsole/` in a browser, switch to BRIDGE → MAP, then load any encounter map that has POIs and at least two rooms (a Somnus or Patrol Gunboat map satisfies this per MEMORY.md).

    --- Popover exclusivity (Plan 01) ---

    1. **GM right-clicks an empty cell in Room A.** Expected: room context menu appears at cursor position.
    2. **Without closing it, GM right-clicks an empty cell in Room B.** Expected: Room B's context menu appears; Room A's context menu disappears (one popover visible at a time).
    3. **GM left-clicks a placed token.** Expected: token popup appears; any open context menu disappears.
    4. **GM clicks a door (with door-status edit enabled).** Expected: door status popup appears; any open token popup or context menu disappears.
    5. **GM hovers a POI marker.** Expected: POI hover popup appears; if any other popover was open, it disappears.
    6. **GM closes the door popup (via X button or status change).** Expected: door popup gone, no other popover reappears.

    --- Token placement guards (Plan 02) ---

    7. **GM drags a token from the palette onto an empty in-room cell.** Expected: token is placed; no error toast.
    8. **GM drags a token onto a cell that already has a token.** Expected: amber `'Cell is already occupied by another token'` warning toast; no token placed.
    9. **GM drags a token onto a corridor/wall cell (outside any room polygon).** Expected: amber `'Token can only be placed inside a room'` warning toast; no token placed.
    10. **Switch to player view (open `/terminal/` in a second tab; ensure ENCOUNTER view active).** Try to drag a token. Expected: nothing happens — drop is silently rejected because `!isGM` short-circuits both handlers.

    Record each of the 10 steps in `22-03-SMOKE-LOG.md` with one of: `PASS`, `FAIL: <observed>`, or `SKIP: <reason>`. If any step is FAIL, do NOT mark this task complete — report back to the planner with the failure so a follow-up plan can address it.

    The smoke log format:
    ```
    # Phase 22 Smoke Verification — 2026-MM-DD

    | # | Behavior | Result | Notes |
    |---|----------|--------|-------|
    | 1 | Right-click Room A context menu | PASS / FAIL | ... |
    ... 10 rows total ...
    ```
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 smoke steps are PASS and the smoke log is committed; otherwise describe the FAIL observations.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/22-renderer-interaction-seams/22-03-SMOKE-LOG.md` exists
    - Smoke log contains a row for each of the 10 steps with a result column
    - All 10 rows are `PASS` (any FAIL blocks phase completion and routes back to planner)
    - File is committed to git
  </acceptance_criteria>
  <done>Human verified all 10 popover + token-placement behaviors in a live browser; smoke log recorded; phase decomposition closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds no new code paths and no new trust boundaries — it is verification of Plans 01 and 02. The smoke log records evidence about behavior preserved by those two plans; it makes no functional changes.

| Boundary | Description |
|----------|-------------|
| None added | Verification-only plan; no new external attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Repudiation | Smoke verification record | mitigate | Smoke results are captured in a checked-in markdown log (`22-03-SMOKE-LOG.md`) so future regressions can be diffed against the recorded baseline. No runtime mitigation needed. |

This plan is verification + cleanup with no external attack surface. ASVS L1 controls established in prior phases remain in force unchanged.
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit -p .` exits 0
- Build: `npm run build` exits 0
- Grep gates from Task 1 all pass
- Smoke log file exists, has 10 rows, all PASS
</verification>

<success_criteria>
- Renderer is statically clean (no orphan imports, no leftover useState/useCallback blocks the extractions should have removed)
- LOC reduction recorded in summary
- All 10 manual smoke steps confirmed PASS by the user
- Phase 22 ends with the renderer holding zero interaction state machines (per phase goal)
</success_criteria>

<output>
After completion, create `.planning/phases/22-renderer-interaction-seams/22-03-SUMMARY.md` per the standard summary template. The phase-level summary at `.planning/phases/22-renderer-interaction-seams/PHASE-SUMMARY.md` (or equivalent) should reference the smoke log and the final renderer LOC.
</output>
