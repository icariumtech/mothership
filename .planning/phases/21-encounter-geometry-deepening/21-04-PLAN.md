---
phase: 21-encounter-geometry-deepening
plan: 04
type: execute
wave: 3
depends_on: [21-02, 21-03]
files_modified:
  - terminal/data_loader.py
  - tools/svg_to_map.py
  - data/galaxy/**/map/*.yaml  # all existing maps migrated to new format
  - src/components/domain/encounter/EncounterMapRenderer.tsx  # remove legacy adapter
  - src/types/encounterMap.ts                                  # remove legacy DoorDef
autonomous: false  # touches data + manual map verification required

must_haves:
  truths:
    - "Backend serializer emits top-level `doors:` array in Door canonical shape (or AuthoredDoorRel pre-normalize for the GM convenience)"
    - "tools/svg_to_map.py --detect-doors emits AuthoredDoorRel form ({rooms, along, type, status})"
    - "All existing YAML maps under data/galaxy/**/map/ migrated to top-level doors: with B-rel form"
    - "Frontend extractAuthoredDoorsFromRooms adapter (added in 21-03) is removed; loader produces canonical doors directly"
    - "Legacy GridRoom.doors and DoorDef removed from src/types/encounterMap.ts"
    - "Door-status-state ID format migrated: any persisted ${room.id}_door_${index} keys remapped to canonical ids"
    - "All maps render visually identical to before plan 21-03"
    - "npm test, typecheck, build pass; Python tests (if any) pass; Ruff clean"
  artifacts:
    - path: "tools/svg_to_map.py"
      provides: "B-rel emitter for --detect-doors"
    - path: "terminal/data_loader.py"
      provides: "Canonical-shape YAML serializer for encounter maps"
    - path: "data/galaxy/**/map/*.yaml"
      provides: "Migrated maps in B-rel form"
  key_links:
    - from: "svg_to_map.py --detect-doors"
      to: "AuthoredDoorRel YAML"
      via: "shared-edge midpoint → {rooms, along: 0.5, type, status}"
      pattern: "rooms:.*along:"
---

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-encounter-geometry-deepening/21-CONTEXT.md
@CONTEXT.md
@terminal/data_loader.py
@tools/svg_to_map.py
@DATA_DIRECTORY_GUIDE.md
@src/components/domain/encounter/doors/doorNormalizer.ts
</context>

<objective>
Flip the data layer to emit the canonical door shape natively. Remove the legacy nested-door bridge added in 21-03. Migrate existing YAML maps. Migrate persisted door-status state.

Purpose: complete the door model rework end-to-end — YAML, Python, types, frontend, persisted state — so the legacy code path can be deleted.

Output: backend serializer flipped; tool emitter flipped; all map YAMLs in new format; legacy types removed; door-status state migrated.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update tools/svg_to_map.py to emit B-rel</name>
  <action>
Modify `tools/svg_to_map.py --detect-doors` to emit top-level `doors:` array with `AuthoredDoorRel` shape:

```yaml
doors:
  - rooms: [bridge, mess]
    along: 0.5
    type: standard
    status: CLOSED
```

The shared-edge detection logic stays the same (`_shared_edges_with_segments` from the memory note). Output the midpoint as `along: 0.5` rather than `{x, y, angle}`. For corridor-to-corridor doors, same shape.

Door type/status defaults: `type: standard`, `status: CLOSED` unless overridden.

Verify on `~/mothership/charon/sample_ui/somnus.svg` and `~/mothership/charon/sample_ui/Patrol_gunboat_O.474_III.svg` — output matches new schema.
  </action>
</task>

<task type="auto">
  <name>Task 2: Migrate existing YAML maps</name>
  <action>
Walk all `data/galaxy/**/map/*.yaml` files. For each map with rooms-with-nested-doors, transform:

```yaml
# OLD
rooms:
  - id: bridge
    doors:
      - { wall: north, position: 1, type: standard, status: CLOSED }

# NEW
rooms:
  - id: bridge
    # doors: removed
doors:
  - rooms: [bridge, <inferred_other_room>]
    along: 0.5  # approximated from wall+position; may need adjustment
    type: standard
    status: CLOSED
```

Strategy:
- Write a one-shot Python migration script (in `tools/migrate_doors_to_canonical.py`) that:
  1. For each map, walk rooms and their nested doors
  2. For each door, compute its grid position using the legacy `wall`+`position` logic (same as `getDoorSVGPosition` in the renderer pre-21-03)
  3. Find the other room via spatial lookup (cell on the other side of the wall)
  4. Compute `along` as fraction along the shared edge
  5. Emit a top-level `doors:` array
  6. Strip nested `doors` from rooms
- Apply migration to all maps under `data/galaxy/`
- Verify each migrated map loads via `doorNormalizer` without errors (a small Python-side smoke test that calls a Node CLI, OR a Vitest pass that load-tests every YAML)
- Visual diff: load each migrated map in dev server and compare to a screenshot baseline (or eyeball)

Migrated files to track:
- `data/galaxy/sol/earth/uscss_morrigan/map/deck_1.yaml`
- `data/galaxy/sol/earth/uscss_morrigan/map/deck_2.yaml`
- `data/galaxy/kepler-442/kepler-442b/base_alpha/map/main_facility.yaml`
- `data/galaxy/tau-ceti/somnus/map/*.yaml`
- `data/galaxy/tau-ceti/tau-ceti-f/patrol_gunboat/*.yaml`
- (any others under `data/galaxy/**/map/`)
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Django backend serializer</name>
  <action>
In `terminal/data_loader.py`, the encounter-map endpoint should now read maps with top-level `doors:` and pass them through unchanged. If the loader is doing any transformation today (likely just passthrough), verify it doesn't strip or rewrite the new `doors` field.

If the API endpoint serializes maps, ensure:
- `doors` is passed through as-is (the frontend's `doorNormalizer` handles canonicalization)
- Or, optionally, normalize on the backend (call out to a Python port of doorNormalizer) — defer this if non-trivial

Recommended: keep normalizer on the frontend. Backend is pass-through. Simpler.

Run Ruff: `ruff check terminal/`.
  </action>
</task>

<task type="auto">
  <name>Task 4: Remove the legacy adapter from the renderer</name>
  <action>
In `src/components/domain/encounter/EncounterMapRenderer.tsx` (and adjacent files), remove `extractAuthoredDoorsFromRooms` (added in 21-03). The renderer now consumes `mapData.doors` directly via `normalizeDoors(mapData.doors, mapData.rooms)`.

Update `GridEncounterMapData` in `src/types/encounterMap.ts`:
- Add `doors: AuthoredDoor[]` (input shape — what YAML carries)
- Remove `GridRoom.doors?: DoorDef[]`
- Remove the `DoorDef` type (legacy, no longer used)

After this task, the legacy door path is fully gone.
  </action>
</task>

<task type="auto">
  <name>Task 5: Door-status state migration</name>
  <action>
Door-status state (`DoorStatusState`, keyed by door id) is held in:
- ActiveView in SQLite (Django)
- React state in the renderer for in-flight changes

The id format change: from `${room.id}_door_${index}` (legacy) to canonical Door.id (e.g. `bridge__mess__0`).

Strategy: **deterministic id derivation** so existing state survives:
- For maps migrated in Task 2, rerun the legacy id derivation against the migrated map (read the order of doors as they appeared in the legacy YAML, generate `${room.id}_door_${oldIndex}` IDs)
- Build a one-shot mapping `{ legacyId: canonicalId }`
- Migrate any persisted DoorStatusState rows in SQLite using the mapping (small Django data migration, or a one-off management command)
- After migration, the canonical id is the only id in use

Alternative: invalidate door-status state on first load (everything reverts to YAML default). Acceptable since door state is per-session.

Choose whichever is simpler given the actual persisted state at migration time.
  </action>
</task>

<task type="auto">
  <name>Task 6: Verify end-to-end</name>
  <action>
- `npm test` — all tests pass
- `npm run typecheck` — 0 errors
- `npm run build` — succeeds
- `ruff check terminal/ tools/` — clean
- Manual: load somnus, patrol gunboat, base_alpha — all render correctly, doors visible/hidden correctly, door-status changes persist

Update `DATA_DIRECTORY_GUIDE.md` to document the new top-level `doors:` schema.
  </action>
</task>

</tasks>

<commit>
```
feat(phase-21): flip data layer to canonical Door shape

- tools/svg_to_map.py --detect-doors emits AuthoredDoorRel ({rooms, along})
- All existing YAML maps under data/galaxy/**/map/ migrated to top-level
  doors: array with B-rel form (one-off Python migration script in
  tools/migrate_doors_to_canonical.py)
- Legacy GridRoom.doors? and DoorDef removed from types
- Renderer drops legacy extractAuthoredDoorsFromRooms adapter — consumes
  mapData.doors directly
- Door-status state migrated: persisted ${room.id}_door_${index} keys
  remapped to canonical Door.id via deterministic derivation
- DATA_DIRECTORY_GUIDE.md updated for new schema

Door model rework complete end-to-end.
```
</commit>
