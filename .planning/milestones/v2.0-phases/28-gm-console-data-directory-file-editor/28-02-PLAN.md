---
phase: 28-gm-console-data-directory-file-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gm/DataFileTree.tsx
autonomous: true
requirements:
  - D-02

must_haves:
  truths:
    - "DataFileTree renders an Ant Design Tree with lazy-loading folder expansion via loadData"
    - "A filter input above the tree hides non-matching nodes by filename"
    - "Directory nodes show FolderOutlined / FolderOpenOutlined icons; file nodes show FileOutlined"
    - "Non-YAML/non-MD files show an extension badge (Ant Design Tag)"
    - "Selected file node shows teal left border + teal background highlight via selectedKeys prop"
    - "Clicking a file node calls onSelectFile with the full relative path (e.g., campaign/janus.yaml)"
  artifacts:
    - path: "src/components/gm/DataFileTree.tsx"
      provides: "DataFileTree component with loadData, filter, and selection"
      exports: ["DataFileTree", "DataFileTreeProps"]
      min_lines: 80
  key_links:
    - from: "src/components/gm/DataFileTree.tsx"
      to: "gmConsoleApi.listDataDir"
      via: "loadData callback calling listDataDir(node.key as string)"
      pattern: "listDataDir"
    - from: "src/components/gm/DataFileTree.tsx"
      to: "onSelectFile prop"
      via: "Tree onSelect callback"
      pattern: "onSelectFile"
---

<objective>
Build the `DataFileTree` component: an Ant Design Tree with lazy-loaded folder expansion, a filename filter input, file/folder icons, extension badges for non-editable files, and selected-node teal highlighting. This component is self-contained with no dependency on Plan 28-01's ViewRail changes — it only imports from gmConsoleApi.ts (which is written in Plan 28-01, same wave but the DataFileTree import is resolved at build time, not runtime).

Purpose: FileEditorView (Plan 28-03) mounts DataFileTree in the left panel. The tree is the primary navigation surface for the editor.

Output: src/components/gm/DataFileTree.tsx
</objective>

<execution_context>
@/home/gjohnson/.claude/get-shit-done/workflows/execute-plan.md
@/home/gjohnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gjohnson/mothership/charon/.planning/phases/28-gm-console-data-directory-file-editor/28-CONTEXT.md
@/home/gjohnson/mothership/charon/.planning/phases/28-gm-console-data-directory-file-editor/28-UI-SPEC.md
@/home/gjohnson/mothership/charon/.planning/ROADMAP.md
</context>

<interfaces>
<!-- Contracts the executor needs before writing DataFileTree. -->

From src/components/gm/LocationTree.tsx (pattern reference — DO NOT copy verbatim, mirror the approach):
  - Uses Ant Design Tree with treeData prop and expandedKeys + selectedKeys (controlled)
  - DataNode from 'antd/es/tree': { key: string, title: ReactNode, isLeaf?: boolean, children?: DataNode[] }
  - showLine={{ showLeafIcon: false }} on Tree
  - Tree onSelect signature: (keys: React.Key[], info: { selected: boolean, node: ... }) => void
  NOTE: LocationTree does NOT use loadData (it has all data up front). DataFileTree MUST use loadData for lazy expansion.

From src/services/gmConsoleApi.ts (written in Plan 28-01 — treat as already available):
  export interface DataDirEntry { name: string; type: 'file' | 'directory' }
  gmConsoleApi.listDataDir(dir: string): Promise<DataDirEntry[]>
  The dir param is relative to data/ (e.g., '' for root, 'campaign' for campaign/, 'campaign/NPCs' for nested)

Ant Design Tree loadData signature:
  loadData: (treeNode: DataNode) => Promise<void>
  When loadData resolves, Tree expects that treeNode.children has been populated.
  Implementation: update local treeData state; use functional state update to avoid stale closures.
  The treeNode.key is the string path relative to data/ (used as the dir param for listDataDir).

From 28-UI-SPEC.md (color/style values to use inline or in JSX style objects):
  Tree panel background: #111e1e
  Filter input placeholder: 'FILTER FILES...' (ALL-CAPS string literal)
  Font: 'Share Tech Mono', 'Cascadia Code', 'Courier New', monospace
  Selected node: background rgba(74,139,139,0.1), teal left border 2px solid #4a8b8b
  Node hover: background rgba(255,255,255,0.04)
  Expanded dir icon color: #4a8b8b (teal)
  Extension badge: Ant Design Tag color="default" at fontSize 10px, shown only for non-YAML/non-MD/non-directory entries
  Filter input: Ant Design Input size="small", prefix=SearchOutlined, style with 8px padding
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build DataFileTree component</name>
  <files>src/components/gm/DataFileTree.tsx</files>
  <read_first>
    - src/components/gm/LocationTree.tsx — read the full file to understand the Ant Design Tree usage patterns, DataNode import, treeData structure, onSelect/onExpand handlers, and controlled expandedKeys pattern
    - src/components/gm/panels/DocumentsPanel.tsx — skim for any relevant API fetch patterns in GM console context
    - src/components/gm/layout/ViewRail.css — skim to confirm monospace font stack (used for all GM console text)
  </read_first>
  <action>
    Create src/components/gm/DataFileTree.tsx as a new file. This is a fresh component, not a modification of LocationTree.

    Props interface (export as DataFileTreeProps):
      selectedPath: string | null          — currently selected file path (relative to data/)
      onSelectFile: (path: string) => void — called when a file leaf node is clicked

    State:
      treeData: DataNode[]                 — starts as root entries (loaded on mount for dir='')
      expandedKeys: string[]               — controlled expansion state
      filterText: string                   — current filter input value
      loadingKeys: Set<string>             — keys currently loading (for spinner suppression, optional)

    Root load: useEffect on mount, call gmConsoleApi.listDataDir('') to get top-level entries, convert to DataNode[] and set treeData. Each entry: key = entry.name (for root-level, key is the bare name like 'campaign'), isLeaf = entry.type === 'file', title = custom ReactNode (see below).

    loadData implementation:
      async function loadData(node: DataNode): Promise<void>
        const parentPath = node.key as string  // e.g., 'campaign' or 'campaign/NPCs'
        const entries = await gmConsoleApi.listDataDir(parentPath)
        const children: DataNode[] = entries.map(entry => ({
          key: parentPath + '/' + entry.name,
          isLeaf: entry.type === 'file',
          title: buildNodeTitle(entry, parentPath + '/' + entry.name),
        }))
        Update treeData state: find the node with key === parentPath, set its children. Use a recursive helper function updateNodeChildren(nodes: DataNode[], key: string, children: DataNode[]): DataNode[] that walks the tree and returns a new array.

    buildNodeTitle(entry: DataDirEntry, fullPath: string): ReactNode
      For directories: <span style={{display:'flex', alignItems:'center', gap:4, color:'#7ab8b8', fontFamily: monospaceFont}}>  with {isExpanded ? <FolderOpenOutlined style={{color:'#4a8b8b'}}/> : <FolderOutlined style={{color:'#4a8b8b'}}/>} then entry.name </span>
      Note: icon cannot be determined from entry alone at title-build time — for simplicity, always use FolderOutlined in the title element; the Tree component's own expand icon will handle the open/closed visual. Actually, since we're building a custom title, use the expandedKeys state to pick the right icon. Alternatively, render both and toggle via CSS — the simplest approach is: set both icons and let Tree's onExpand update expandedKeys, then re-render titles by rebuilding treeData or by checking expandedKeys inside buildNodeTitle.
      SIMPLEST approach: use a non-stateful title. Use FolderOutlined always for directories in the title; do not try to toggle folder icon — the Ant Design Tree's own +/- expand control is sufficient UX. The spec says "Expanded directory icons: teal (#4a8b8b)" which refers to the icon color, not switching between open/closed folder icons. Use FolderOutlined (teal color) for all directories.
      For files: <span> with <FileOutlined style={{color:'#4a7070'}}/> then entry.name then optionally an extension badge.
      Extension badge: show Ant Design <Tag color="default" style={{fontSize:10, marginLeft:4}}>.ext</Tag> only for extensions that are NOT .yaml, .yml, or .md. Extract extension from entry.name using the last segment after the final dot. If no extension or extension is yaml/yml/md, do not render the tag.

    filterTreeNode prop:
      When filterText is non-empty, pass filterTreeNode={(node) => (node.key as string).toLowerCase().includes(filterText.toLowerCase())} to Tree. This hides non-matching nodes including parent directories that don't match — acceptable per spec ("Only matching nodes visible").

    Selected node styling:
      Use Tree's selectedKeys prop: selectedKeys={selectedPath ? [selectedPath] : []}.
      Override Ant Design Tree's default selected style using a wrapper div with custom CSS. Inject a <style> tag or use className approach. The simplest: wrap in a div with className="data-file-tree" and include a <style> block injected via JSX with the selector .data-file-tree .ant-tree-node-selected which sets background: rgba(74,139,139,0.1) and border-left: 2px solid #4a8b8b. Use dangerouslySetInnerHTML is forbidden — use React's standard <style> element or a separate CSS file. Since no CSS file is listed in files_modified for this task, inject a minimal inline <style> JSX element in the component return, or pass style overrides directly to the Tree blockNode prop.
      Simplest correct approach: use a wrapping div with a style scope. Add className="data-file-tree" to the wrapper. Inside the return, add <style>{`.data-file-tree .ant-tree-node-selected { background: rgba(74,139,139,0.1) !important; border-left: 2px solid #4a8b8b !important; } .data-file-tree .ant-tree-treenode:hover { background: rgba(255,255,255,0.04) !important; }`}</style> via a JSX string (no dangerouslySetInnerHTML needed — <style> elements accept text children in React).

    onSelect handler:
      (keys: React.Key[]) => { if (keys.length > 0) { const key = String(keys[0]); const node = findNode(treeData, key); if (node?.isLeaf) { onSelectFile(key); } } }
      findNode: recursive helper that walks treeData to find node by key.

    Filter input: Ant Design Input with size="small", prefix={<SearchOutlined />}, placeholder="FILTER FILES...", value={filterText}, onChange={(e) => setFilterText(e.target.value)}. Wrap in a div with style={{padding: '8px 8px', borderBottom: '1px solid #1e3333'}}. Font: monospace stack.

    The component return:
      <div style={{display:'flex', flexDirection:'column', height:'100%', background:'#111e1e', overflow:'hidden'}}>
        <div> {/* filter input wrapper */} </div>
        <div style={{flex:1, overflowY:'auto'}} className="data-file-tree">
          <style>...</style>
          <Tree treeData={treeData} loadData={loadData} expandedKeys={expandedKeys} selectedKeys={...} onExpand={(keys) => setExpandedKeys(keys as string[])} onSelect={onSelect} filterTreeNode={filterText ? filterFn : undefined} showLine={{ showLeafIcon: false }} style={{ background: 'transparent', fontSize: 12, fontFamily: monoFont }} />
        </div>
      </div>

    Import from: react (useState, useEffect, useCallback), antd (Tree, Input, Tag), antd/es/tree (DataNode), @ant-design/icons (FolderOutlined, FolderOpenOutlined, FileOutlined, SearchOutlined), @/services/gmConsoleApi (gmConsoleApi, DataDirEntry).

    monospaceFont constant: "'Share Tech Mono', 'Cascadia Code', 'Courier New', monospace"
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && pnpm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DataFileTree.tsx exists at src/components/gm/DataFileTree.tsx
    - DataFileTreeProps interface is exported with selectedPath and onSelectFile props
    - Component imports DataDirEntry and gmConsoleApi from @/services/gmConsoleApi
    - loadData function calls gmConsoleApi.listDataDir(node.key as string) and populates children
    - filterTreeNode prop is conditionally applied when filterText is non-empty
    - onSelect calls onSelectFile only for isLeaf nodes
    - pnpm run typecheck exits 0 with no errors in DataFileTree.tsx
    - Extension badge (Ant Design Tag) is rendered only for extensions that are not yaml, yml, or md
  </acceptance_criteria>
  <done>DataFileTree.tsx exists, TypeScript clean, lazy-loads directories on expand, filters by filename, calls onSelectFile for file nodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Django | DataFileTree calls listDataDir which hits unauthenticated API (trust-network model from Phase 23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information Disclosure | DataFileTree | accept | Listing data/ is intentional GM-only functionality; no player access to GM Console |
</threat_model>

<verification>
- pnpm run typecheck exits 0
- DataFileTree.tsx file exists with correct exports
- File has at least 80 lines (substantive implementation)
</verification>

<success_criteria>
- DataFileTree renders an Ant Design Tree with lazy-loading folder expansion
- Filter input hides non-matching nodes
- File and directory icons distinguished with teal folder color
- Extension badges shown for non-YAML/non-MD files
- onSelectFile called with the full relative path when a file is clicked
- TypeScript clean
</success_criteria>

<output>
Create .planning/phases/28-gm-console-data-directory-file-editor/28-02-SUMMARY.md when done
</output>
