---
phase: 25-map-rotation-controls
plan: 04
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/components/domain/maps/r3f/OrbitScene.tsx
  - src/components/domain/maps/OrbitMap.tsx
autonomous: true
requirements:
  - PHASE-25-GOAL
must_haves:
  truths:
    - "Orbit map shows MapPlaybackControls overlay with play/pause + scrub bar at top-center"
    - "Clicking pause freezes moon and orbital-station positions at their current orbital angles"
    - "While paused, dragging scrub bar advances/reverses moons + stations along their orbits"
    - "The existing animationPaused state (surface marker selection) continues to work and is independent of the new orbitsPaused"
    - "Scrub speed is calibrated to shortest moon/station period when no body selected, selected body's period when one is selected"
    - "Pausing does not disable camera tracking or surface marker selection"
  artifacts:
    - path: "src/components/domain/maps/r3f/OrbitScene.tsx"
      provides: "Adds orbitsPaused + scrubOffsetRef props; threads them to moon/station children alongside existing animationPaused"
      contains: "orbitsPaused"
    - path: "src/components/domain/maps/OrbitMap.tsx"
      provides: "Local isOrbiting state, scrubOffsetRef accumulator, MapPlaybackControls overlay"
      contains: "MapPlaybackControls"
  key_links:
    - from: "src/components/domain/maps/OrbitMap.tsx"
      to: "src/components/domain/maps/r3f/OrbitScene.tsx"
      via: "orbitsPaused + scrubOffsetRef props"
      pattern: "orbitsPaused=\\{|scrubOffsetRef=\\{"
    - from: "OrbitScene.tsx"
      to: "moon/station child components"
      via: "Threading orbitsPaused + scrubOffsetRef alongside animationPaused"
      pattern: "orbitsPaused=\\{"
---

<objective>
Add play/pause + scrub bar to the orbit map (moons + orbital stations). Mirrors Plan 03's pattern for the system map, with one twist: `OrbitScene` already has an internal `animationPaused` flag (used when a surface marker is selected). The new `orbitsPaused` is a SECOND, independent freeze flag — both must be threaded to moon/station child components.

Purpose: Ships D-03, D-04, D-05, D-06, D-08, D-09 for the orbit view. Per RESEARCH.md Gotcha #6, animationPaused and orbitsPaused are independently OR'd inside each moon/station child to compute its effective freeze state.

Output:
- `OrbitScene.tsx` accepts new props and threads them to children
- `OrbitMap.tsx` owns local pause state + scrub accumulator and renders the overlay
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-map-rotation-controls/25-CONTEXT.md
@.planning/phases/25-map-rotation-controls/25-RESEARCH.md
@.planning/phases/25-map-rotation-controls/25-PATTERNS.md
@.planning/phases/25-map-rotation-controls/25-01-SUMMARY.md
@.planning/phases/25-map-rotation-controls/25-03-SUMMARY.md

<interfaces>
<!-- Existing OrbitScene props (line ~64): paused?, plus data/selectedElement/onElementSelect/onReady/transitionState -->
<!-- Existing internal state: animationPaused at line 147 (surface marker selection freeze) -->
<!-- Moon/OrbitalStation children receive animationPaused as a prop at lines 714, 723, 753, 766, 781 -->

```typescript
// New OrbitScene props additions:
orbitsPaused?: boolean;
scrubOffsetRef?: React.RefObject<number>;
```

<!-- Moon/OrbitalStation components must also accept orbitsPaused + scrubOffsetRef OR the parent computes a combined `effectivelyPaused` and passes that. Check Moon.tsx / OrbitalStation.tsx (or wherever the child components live) and prefer the parent-combine approach to minimize child surface changes. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend OrbitScene with orbitsPaused + scrubOffsetRef, thread to children</name>
  <files>src/components/domain/maps/r3f/OrbitScene.tsx</files>
  <read_first>
    - src/components/domain/maps/r3f/OrbitScene.tsx (file being modified — focus on props interface lines ~60-70, paused derivation line ~109, animationPaused state line 147, moon/station child JSX around lines 700-790)
    - .planning/phases/25-map-rotation-controls/25-PATTERNS.md (OrbitScene.tsx modify section)
    - .planning/phases/25-map-rotation-controls/25-RESEARCH.md (Gotcha #6: animationPaused vs orbitsPaused independence)
    - Identify the Moon and OrbitalStation child component files (likely src/components/domain/maps/r3f/orbit/Moon.tsx and OrbitalStation.tsx) — read their props interfaces to know which approach is cleaner
  </read_first>
  <action>
    Modify `src/components/domain/maps/r3f/OrbitScene.tsx`:

    1) Extend the `OrbitSceneProps` interface (around line 60-70) with:
       - `orbitsPaused?: boolean` — JSDoc: "Freeze orbital math from the play/pause control (independent of the internal animationPaused which fires on surface marker selection)."
       - `scrubOffsetRef?: React.RefObject<number>` — JSDoc: "Accumulated scrub offset in radians, applied additively to moon/station orbital angles when frozen."

    2) Destructure both new props in the forwardRef signature alongside `paused: pausedProp = false`:
       - `orbitsPaused: orbitsPausedProp = false,`
       - `scrubOffsetRef,`

    3) After the existing `const paused = pausedProp || storePaused;` (line ~109), do NOT modify `paused`. Add a new derived combined-freeze for orbital math only:
       - `const orbitalMathFrozen = orbitsPausedProp || animationPaused;`
       (This combines the new play/pause flag with the existing surface-marker animationPaused — both independently freeze orbital math. See Gotcha #6.)

    4) Thread the combined flag + scrub ref to moon and orbital-station children. Where children currently receive `animationPaused={animationPaused}` (lines ~714, 723, 753, 766, 781), choose ONE of the two approaches based on what you read from the child components:

       APPROACH A (preferred — minimal child changes): Replace `animationPaused={animationPaused}` with `animationPaused={orbitalMathFrozen}` at every child site. The child treats the prop as "freeze position math" regardless of which upstream flag caused it. Also pass `scrubOffsetRef={scrubOffsetRef}` to each child.

       APPROACH B (if the child uses `animationPaused` for additional non-position concerns, e.g., suppressing animations beyond orbital position): Add a separate `orbitsPaused` prop to the child interface and let the child internally OR the two flags.

       Document which approach you took in the SUMMARY. Default to APPROACH A unless child code makes it incorrect.

    5) The Moon and OrbitalStation child components must accept and apply `scrubOffsetRef` to their orbital angle calculations. If their angle math is structured like SystemScene's `calculatePlanetPosition`, apply the same `currentAngle = initialAngle + orbitalSpeed * elapsedSeconds + scrubOffset` pattern, where `scrubOffset = orbitsPaused ? (scrubOffsetRef?.current ?? 0) : 0`. Update only their position math; do NOT change selection / hover / tracking logic.

    6) Do NOT modify the camera tracking useFrame loop (line ~611) that uses `if (paused) return`. The play/pause button must NOT pause camera tracking — only orbital position math.

    Edge case: if Moon/OrbitalStation use `useFrame` with `startTime` props rather than internal Date.now, the scrub offset becomes an angle additive applied AFTER the period multiplier, matching SystemScene's formula. Mirror that pattern.
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "orbitsPaused" src/components/domain/maps/r3f/OrbitScene.tsx` returns 3 or more (interface, destructure, derivation/usage)
    - `grep -c "scrubOffsetRef" src/components/domain/maps/r3f/OrbitScene.tsx` returns 2 or more (interface + child prop pass)
    - `grep -c "orbitalMathFrozen\|animationPaused" src/components/domain/maps/r3f/OrbitScene.tsx` returns 2 or more (existing animationPaused still present)
    - File still contains existing literal `const paused = pausedProp || storePaused`
    - The camera-tracking useFrame guard `if (paused) return;` (line ~611) is unchanged
    - Moon/OrbitalStation child components receive scrubOffsetRef as a prop (or via a parent-combined value)
    - `pnpm run typecheck` exits 0 — child interface changes (if any) must compile
  </acceptance_criteria>
  <done>OrbitScene accepts and threads orbitsPaused + scrubOffsetRef; surface-marker animationPaused still works independently; camera tracking unaffected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire OrbitMap with isOrbiting state, scrub accumulator, overlay</name>
  <files>src/components/domain/maps/OrbitMap.tsx</files>
  <read_first>
    - src/components/domain/maps/OrbitMap.tsx (file being modified — read import block, forwardRef body, OrbitScene JSX usage around line 229)
    - src/components/domain/maps/r3f/OrbitScene.tsx (just-modified — confirm new prop names)
    - src/components/domain/maps/SystemMap.tsx (pattern reference — Plan 03 implemented the same shape for system map)
    - src/types/orbitMap.ts (confirm orbital_period field names on moons + orbital_stations)
    - .planning/phases/25-map-rotation-controls/25-PATTERNS.md (OrbitMap.tsx modify section)
  </read_first>
  <action>
    Modify `src/components/domain/maps/OrbitMap.tsx` — mirror Plan 03's SystemMap changes:

    1) Imports — extend existing react import line to include `useRef`, `useState`, `useCallback` if not already present. Add:
       - `import { MapPlaybackControls } from './MapPlaybackControls';`

    2) Constants (top of file or above the component, matching SystemMap):
       - `const ORBITAL_PERIOD_TARGET_SECONDS = 10;` (or import if SystemScene exports it)
       - `const SCRUB_TRACK_WIDTH = 300;`

    3) Inside the forwardRef body, after `loadedLocationRef` (~line 78), add:
       - `const [isOrbiting, setIsOrbiting] = useState(true);`
       - `const scrubOffsetRef = useRef<number>(0);`

    4) Define handlers:
       ```
       const handleTogglePlay = useCallback(() => {
         setIsOrbiting((prev) => !prev);
       }, []);

       const handleScrubDelta = useCallback((deltaX: number) => {
         if (!orbitData) return;
         const moons = orbitData.moons ?? [];
         const stations = orbitData.orbital_stations ?? [];
         const allBodies = [...moons, ...stations];
         const periods = allBodies
           .map((b) => (b as { orbital_period?: number }).orbital_period ?? 365)
           .filter((p): p is number => Number.isFinite(p) && p > 0);
         const shortest = periods.length ? Math.min(...periods) : 365;
         // selectedElement may carry an orbital_period; use it if present
         const selectedPeriod = (selectedElement as { orbital_period?: number } | null | undefined)?.orbital_period;
         const period = selectedPeriod ?? shortest;
         const radiansPerPixel = (2 * Math.PI) / (SCRUB_TRACK_WIDTH * (period / ORBITAL_PERIOD_TARGET_SECONDS));
         scrubOffsetRef.current += deltaX * radiansPerPixel;
       }, [orbitData, selectedElement]);
       ```
       Use whatever variable name OrbitMap already uses for the selected entity (e.g., `selectedElement`, `selectedElementData`, `selectedMoon`) — read first, then match.

    5) Pass new props to `<OrbitScene>` (line ~229 area) alongside existing props:
       - `orbitsPaused={!isOrbiting}`
       - `scrubOffsetRef={scrubOffsetRef}`

    6) Inside the container `<div>` wrapping `<Canvas>`, AFTER `<Canvas>`:
       ```
       <MapPlaybackControls
         isPlaying={isOrbiting}
         onTogglePlay={handleTogglePlay}
         showScrub={true}
         onScrubDelta={handleScrubDelta}
       />
       ```

    Per D-08, scrub state is local — do NOT persist `isOrbiting` or `scrubOffsetRef.current` to sceneStore or any server endpoint.
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MapPlaybackControls" src/components/domain/maps/OrbitMap.tsx` returns 2 or more
    - `grep -c "isOrbiting" src/components/domain/maps/OrbitMap.tsx` returns 3 or more
    - `grep -c "scrubOffsetRef" src/components/domain/maps/OrbitMap.tsx` returns 3 or more
    - `grep -c "handleScrubDelta\|onScrubDelta" src/components/domain/maps/OrbitMap.tsx` returns 2 or more
    - File contains literal string `showScrub={true}`
    - File contains literal string `orbitsPaused={!isOrbiting}`
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>OrbitMap renders the overlay, threads isOrbiting + scrub through to OrbitScene, and uses the adaptive scrub-speed formula. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer drag → orbital angle accumulator | Same as SystemMap (Plan 03). Pointer deltas mutate a client-side ref consumed by R3F render math. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Tampering | scrubOffsetRef.current (orbit map) | accept | Same rationale as T-25-05; angles are mod-2π in trig; no server reach. |
| T-25-09 | DoS | Unbounded scrub on many moons/stations | accept | Per-frame cost is O(N) where N=bodies (already paid by existing useFrame); adding scrubOffset is a single float add per body. |

No sensitive data handled. Pointer input only. No server-side exposure.
</threat_model>

<verification>
- `pnpm run typecheck` exits 0
- Visual smoke (deferred to Plan 05): on /gmconsole/ Bridge MAP tab → Orbit view (select a planet first) → click ⏸ → moons/stations freeze; drag scrub → they advance/reverse; surface marker selection still freezes-on-click independently.
</verification>

<success_criteria>
- OrbitScene accepts orbitsPaused + scrubOffsetRef
- Children (Moon, OrbitalStation) honor the combined freeze
- OrbitMap owns local isOrbiting + scrubOffsetRef
- Existing animationPaused (surface marker selection) still works
- Camera tracking is NOT paused by the new flag
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/25-map-rotation-controls/25-04-SUMMARY.md` — document whether APPROACH A (rename `animationPaused` to combined-flag) or APPROACH B (add new prop to child) was used for Moon/OrbitalStation, and note any deviations in scrub formula (e.g., if orbital_stations use a different period field).
</output>
