---
phase: 28-gm-console-data-directory-file-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/views/gm_data.py
  - src/services/gmConsoleApi.ts
  - src/components/gm/layout/ViewRail.tsx
  - src/components/gm/layout/ViewRail.css
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04

must_haves:
  truths:
    - "The data directory list API returns both files and directories so the tree can lazily expand folders"
    - "The GM Console API service can list a directory, read a file, and write a file"
    - "The ViewRail shows a FILE_EDITOR button below the ENCOUNTER button with a CodeOutlined icon"
    - "The DISPLAY button is disabled (opacity 0.3, cursor not-allowed) when gmView is FILE_EDITOR"
    - "Switching to FILE_EDITOR in ViewRail changes gmView state in the parent"
  artifacts:
    - path: "core/views/gm_data.py"
      provides: "Updated api_gm_data_list returning files and directories with type field"
      contains: "is_dir"
    - path: "src/services/gmConsoleApi.ts"
      provides: "listDataDir, readDataFile, writeDataFile methods"
      exports: ["listDataDir", "readDataFile", "writeDataFile"]
    - path: "src/components/gm/layout/ViewRail.tsx"
      provides: "FILE_EDITOR GMViewType + CodeOutlined button + DISPLAY disabled logic"
      contains: "FILE_EDITOR"
  key_links:
    - from: "src/components/gm/layout/ViewRail.tsx"
      to: "GMViewType type export"
      via: "type union extension"
      pattern: "FILE_EDITOR"
    - from: "src/services/gmConsoleApi.ts"
      to: "/api/gm/data/"
      via: "axios api.get with params"
      pattern: "gm/data"
---

<objective>
Wire the three foundational pieces that the FileEditorView depends on: (1) extend the backend list API to return directories alongside files so the tree can lazy-expand; (2) add the three data-file API methods to gmConsoleApi.ts; (3) add the FILE_EDITOR GMViewType to ViewRail with its icon button, bottom-rail placement, and DISPLAY-disabled logic.

Purpose: All downstream plans depend on these contracts. The DataFileTree (Plan 28-02) imports from gmConsoleApi.ts and needs directory entries in the API response. The GMConsole.tsx wiring (Plan 28-03) imports GMViewType and renders based on gmView === 'FILE_EDITOR'.

Output: Updated api_gm_data_list, three new gmConsoleApi methods, FILE_EDITOR in ViewRail.
</objective>

<execution_context>
@/home/gjohnson/.claude/get-shit-done/workflows/execute-plan.md
@/home/gjohnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gjohnson/mothership/charon/.planning/phases/28-gm-console-data-directory-file-editor/28-CONTEXT.md
@/home/gjohnson/mothership/charon/.planning/phases/28-gm-console-data-directory-file-editor/28-UI-SPEC.md
@/home/gjohnson/mothership/charon/.planning/ROADMAP.md
</context>

<interfaces>
<!-- Key interfaces extracted from source files. Executor reads these before touching anything. -->

From src/services/api.ts:
  api = axios.create({ baseURL: '/api', headers: { 'Content-Type': 'application/json' } })
  CSRF token is only added for POST methods; GET/PUT do not need it.
  For PUT with text body, set content_type explicitly via axios config.

From src/components/gm/layout/ViewRail.tsx (current state):
  export type GMViewType = 'STANDBY' | 'BRIDGE' | 'ENCOUNTER';
  interface ViewItem { key: GMViewType; icon: ReactNode; tooltip: string; playerViewKeys: string[]; unreadKey?: ... }
  const VIEW_ITEMS: ViewItem[] = [ ...STANDBY, BRIDGE, ENCOUNTER entries... ]
  Props: { gmView, playerView, onViewChange, onDisplay, unreadCounts }
  DISPLAY button: <button className="view-rail__display" onClick={onDisplay} ...>
  Structure: <nav class="view-rail"> → DISPLAY button → separator div → <div class="view-rail__views"> → map VIEW_ITEMS

From src/services/gmConsoleApi.ts (current exports):
  export const gmConsoleApi = { getLocations, getActiveView, switchView, ... getCampaignDoc }
  Pattern: async function name(): Promise<ReturnType> { const response = await api.get/post/put(...); return response.data }

From core/views/gm_data.py (api_gm_data_list current behavior):
  files = sorted(entry.name for entry in target.iterdir() if entry.is_file())
  return JsonResponse(files, safe=False)
  — returns array of strings (filenames only), no type info, directories excluded
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend api_gm_data_list to return both files and directories</name>
  <files>core/views/gm_data.py</files>
  <read_first>
    - core/views/gm_data.py — read the full api_gm_data_list function (lines 126-160) to understand current implementation before modifying
    - core/tests/test_gm_api.py — read the test_list_files_returns_json test to ensure the change does not break existing tests
  </read_first>
  <action>
    Modify api_gm_data_list in core/views/gm_data.py. Replace the current files-only list with a combined list of files AND directories. The response must be a JSON array of objects, not bare strings, so that callers can distinguish entries. Each object has two fields: "name" (string, basename only) and "type" ("file" or "directory"). Sort the array: directories first (alphabetically), then files (alphabetically). No recursion — only immediate children of the requested directory.

    The new return shape is: [{"name": "campaign", "type": "directory"}, ..., {"name": "janus.yaml", "type": "file"}]

    Use target.iterdir() with entry.name for name and ("directory" if entry.is_dir() else "file") for type. Filter out entries that are neither files nor dirs (e.g., symlinks to non-existent targets) using entry.exists(). Keep the existing path-traversal guard and 400/404 error handling unchanged.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && python manage.py test core.tests.test_gm_api.GmDataApiTests.test_list_files_returns_json --verbosity=2 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - api_gm_data_list returns a JSON array where each element is an object with "name" (string) and "type" ("file" or "directory")
    - Directories appear before files in the sorted output
    - The existing test_list_files_returns_json still passes (it only checks status 200 + application/json content-type)
    - Path traversal guard and 400/404 responses are unchanged
    - The function does not recurse into subdirectories
  </acceptance_criteria>
  <done>api_gm_data_list returns [{name, type}] entries with dirs before files; existing Django test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add listDataDir, readDataFile, writeDataFile to gmConsoleApi.ts + FILE_EDITOR to ViewRail</name>
  <files>src/services/gmConsoleApi.ts, src/components/gm/layout/ViewRail.tsx, src/components/gm/layout/ViewRail.css</files>
  <read_first>
    - src/services/gmConsoleApi.ts — read the full file to understand the existing function patterns, api import, and the export object at the bottom
    - src/services/api.ts — confirm the axios instance base URL and CSRF behavior (GET/PUT don't add CSRF token)
    - src/components/gm/layout/ViewRail.tsx — read the full file: GMViewType union, ViewItem interface, VIEW_ITEMS array, component JSX
    - src/components/gm/layout/ViewRail.css — read the full file to understand existing display/views layout before adding CSS
    - src/components/gm/layout/ToolRail.tsx — skim to understand if it uses any file_editor-relevant patterns (skip if not relevant)
  </read_first>
  <action>
    Part A — gmConsoleApi.ts: Add three new async functions before the export object.

    Function signatures and behaviors:
    - listDataDir(dir: string): Promise{Array of {name: string, type: 'file' | 'directory'}}: calls api.get('/gm/data/', { params: { dir } }) and returns response.data
    - readDataFile(path: string): Promise{string}: calls api.get('/gm/data/' + path, { responseType: 'text' }) and returns response.data as string. Note: the axios base URL is '/api', so the full URL becomes '/api/gm/data/{path}'.
    - writeDataFile(path: string, content: string): Promise{void}: calls api.put('/gm/data/' + path, content, { headers: { 'Content-Type': 'text/plain; charset=utf-8' } }) with a raw string body. PUT does not trigger CSRF in the api interceptor, so no extra header needed.

    Add DataDirEntry interface above the functions: interface DataDirEntry { name: string; type: 'file' | 'directory' }
    Export DataDirEntry from the module (add to the existing export const gmConsoleApi object OR export the interface directly with export interface).
    Add all three functions to the gmConsoleApi export object.

    Part B — ViewRail.tsx: Make four changes:
    1. Extend the GMViewType union: type GMViewType = 'STANDBY' | 'BRIDGE' | 'ENCOUNTER' | 'FILE_EDITOR'
    2. Add CodeOutlined to the @ant-design/icons import line alongside existing icons.
    3. Add a new entry at the end of VIEW_ITEMS: { key: 'FILE_EDITOR', icon: <CodeOutlined />, tooltip: 'File Editor', playerViewKeys: [] }. No unreadKey (no badge).
    4. Add a second separator div between the ENCOUNTER slot and the FILE_EDITOR slot. Wrap the FILE_EDITOR button in its own section below the main view-rail__views div (do NOT put it inside view-rail__views, it must be visually separated at the bottom). The layout must be: DISPLAY → separator → view-rail__views (STANDBY + BRIDGE + ENCOUNTER) → separator → file-editor-section (FILE_EDITOR button only).
    5. Disable the DISPLAY button when gmView === 'FILE_EDITOR': add a disabled prop to the DISPLAY button element (disabled={gmView === 'FILE_EDITOR'}), and apply a CSS modifier class view-rail__display--disabled when disabled. The disabled state: opacity 0.3, cursor not-allowed.

    Part C — ViewRail.css: Add the .view-rail__display--disabled rule: opacity: 0.3; cursor: not-allowed; pointer-events: none. Also add a .view-rail__file-editor-section rule: display: flex; flex-direction: column; align-items: center; margin-top: auto; padding-bottom: 4px — this pushes the file editor button to the very bottom of the rail.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon && pnpm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gmConsoleApi.ts exports DataDirEntry interface with { name: string; type: 'file' | 'directory' }
    - gmConsoleApi.ts exports listDataDir, readDataFile, writeDataFile in the gmConsoleApi object
    - ViewRail.tsx GMViewType union contains 'FILE_EDITOR' as the fourth value
    - ViewRail.tsx imports CodeOutlined from @ant-design/icons
    - ViewRail.tsx renders the FILE_EDITOR button below a second separator, in a section with margin-top: auto (bottom of rail)
    - The DISPLAY button has disabled={gmView === 'FILE_EDITOR'} attribute and class view-rail__display--disabled when disabled
    - pnpm run typecheck exits 0 with no errors
  </acceptance_criteria>
  <done>TypeScript compiles clean; ViewRail shows FILE_EDITOR button at bottom of rail; DISPLAY is disabled when FILE_EDITOR is active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Django | All data-file API calls are unauthenticated (trust-network model per D-09 from Phase 23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | api_gm_data_list | accept | Path traversal guard already in place; directory listing returns names only, no content |
| T-28-01-SC | Tampering | npm package installs | mitigate | @monaco-editor/react installed in Plan 28-03; no new packages in this plan |
</threat_model>

<verification>
- pnpm run typecheck exits 0 after both tasks
- python manage.py test core.tests.test_gm_api.GmDataApiTests passes
- ViewRail FILE_EDITOR entry appears in the rendered DOM below a separator
</verification>

<success_criteria>
- Backend list API returns [{name, type}] objects including directories
- gmConsoleApi has listDataDir, readDataFile, writeDataFile
- GMViewType includes 'FILE_EDITOR'
- ViewRail renders FILE_EDITOR button at bottom; DISPLAY disabled when active
- TypeScript clean
</success_criteria>

<output>
Create .planning/phases/28-gm-console-data-directory-file-editor/28-01-SUMMARY.md when done
</output>
