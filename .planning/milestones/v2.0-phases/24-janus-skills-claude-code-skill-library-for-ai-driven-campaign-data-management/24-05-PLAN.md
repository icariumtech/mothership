---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 05
type: execute
wave: 4
depends_on: [24-02, 24-04]
files_modified:
  - /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-update-galaxy/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-generate-context/SKILL.md
  - /home/gjohnson/mothership/janus-skills/skills/janus-session-prep/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "/janus-add-ship creates data/ships/<slug>/location.yaml with parent_type=orbit (mobile ship) or surface; rejects moon body_slug (Pitfall P5)"
    - "/janus-update-galaxy edits data/galaxy/star_map.yaml in place — adds nebulae, routes, or modifies system visual props"
    - "/janus-generate-context reads a location.yaml plus notes from arguments and writes janus.yaml with generated timestamp + context body"
    - "/janus-session-prep calls get_session_context(), reads relevant janus.yaml files, produces a chat brief (no file write)"
    - "All four SKILL.md files have valid YAML frontmatter with mcp__JanusGM__ tool prefix"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md"
      provides: "D-17 ship registration"
      contains: "parent_type"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-update-galaxy/SKILL.md"
      provides: "D-18 galaxy painter"
      contains: "nebulae"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-generate-context/SKILL.md"
      provides: "D-11 janus.yaml generator"
      contains: "janus.yaml"
    - path: "/home/gjohnson/mothership/janus-skills/skills/janus-session-prep/SKILL.md"
      provides: "D-14 session brief"
      contains: "get_session_context"
  key_links:
    - from: "all 4 SKILL.md files"
      to: "MCP tools"
      via: "mcp__JanusGM__ prefix (case-sensitive)"
      pattern: "mcp__JanusGM__"
    - from: "all 4 SKILL.md files"
      to: "resources"
      via: "@$HOME/.claude/janus-skills/resources/"
      pattern: "@\\$HOME/\\.claude/janus-skills/resources/"
---

<objective>
Author the remaining four skills: janus-add-ship (D-17), janus-update-galaxy (D-18), janus-generate-context (D-11), janus-session-prep (D-14). Combined with plan 24-04, this completes the 8-skill MVP set per D-11..D-18.

Output: Four SKILL.md files in `skills/janus-*/`, each with valid frontmatter, correct MCP tool prefixes, and stable HOME-anchored resource @-includes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-02-SUMMARY.md
@mcp_server.py

<interfaces>
MCP tools (FastMCP server "JanusGM"):
- mcp__JanusGM__list_files(dir)             dir relative to data/
- mcp__JanusGM__read_file(path)             path relative to data/
- mcp__JanusGM__write_file(path, content)   triggers SSE broadcast
- mcp__JanusGM__get_session_context()       active view + tokens + ship + NPC list
- mcp__JanusGM__get_data_schema()           D-07 fallback

Skill-to-resource mapping (RESEARCH.md):
- janus-add-ship          → schema-ships.md
- janus-update-galaxy     → schema-galaxy.md
- janus-generate-context  → schema-galaxy.md + schema-janus-context.md
- janus-session-prep      → schema-janus-context.md

Ship location.yaml shape (verified patrol_gunboat/location.yaml):
- name, type:"ship", description, status:"OPERATIONAL"
- parent_type: orbit | surface
- body_slug: lowercase-hyphenated planet dir (NOT a moon — Pitfall P5)
- system_slug: lowercase-hyphenated system dir
- orbital: {radius, period, angle, inclination, size, icon_type}  (icon_type: ship|station|shipyard)

janus.yaml shape (verified veil-station/janus.yaml):
- generated: ISO 8601 UTC timestamp string
- context: YAML literal block (|) with multi-line free-text body

Pitfalls referenced in skill bodies:
- P1: slug case must match dir name exactly
- P2: id must match filename stem
- P3: never use @./ or @./resources/ in SKILL.md
- P5: body_slug only resolves against direct system children (planets, not moons)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author janus-add-ship and janus-update-galaxy</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-update-galaxy/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/data/ships/patrol_gunboat/location.yaml
    /home/gjohnson/mothership/janus-skills/resources/schema-ships.md
    /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  </read_first>
  <action>
    Follow the same SKILL.md pattern produced by plan 24-04 (read `skills/janus-add-npc/SKILL.md` as the validated reference). Each file: YAML frontmatter then prose sections for objective, schema include, and process steps. End every file with a trailing newline.

    janus-add-ship frontmatter values (exact):
    - name: `janus-add-ship`
    - description: must contain literal substring `Use when` and mention orbit/ship registration (suggested: `"Register a new ship in data/ships/ with a location.yaml that self-injects onto an orbit map via body_slug/system_slug. Use when adding a ship to the campaign."`)
    - argument-hint: `"<ship-name> [parent-system] [parent-body]"`
    - allowed-tools (exactly 3, in this order): `mcp__JanusGM__list_files`, `mcp__JanusGM__read_file`, `mcp__JanusGM__write_file`

    janus-add-ship body sections (in order):
    - objective paragraph stating output: `data/ships/<slug>/location.yaml` (and optional deckplan stub)
    - schema include block containing exactly the single line `@$HOME/.claude/janus-skills/resources/schema-ships.md`
    - process: numbered list (12–16 steps) that must include all of the following requirements:
      * Step parses arguments for ship name, optional system slug, optional body slug
      * Step derives ship slug via `name.lower().replace(" ", "_")` (underscores — matches existing patrol_gunboat convention)
      * Step calls `list_files("ships")` to detect slug collision; refuses or prompts on collision
      * Step branches by ship kind: orbit (default) vs surface vs static
      * Orbit branch validates `read_file("galaxy/<system_slug>/system_map.yaml")` succeeds
      * Orbit branch enforces Pitfall P5 by verifying `<body_slug>` is a direct child of the system directory (a planet) and NOT a moon (nested deeper). On violation, refuse with explicit error citing Pitfall P5.
      * Orbit branch prompts for `orbital` sub-fields: radius (default 32), period (default 90), angle (default 135), inclination (default 0), size (default 1.5), icon_type (default `ship`; allow `station`, `shipyard`)
      * Surface branch sets `parent_type: surface` and omits `orbital`
      * Step enforces Pitfall P1: both slugs match `^[a-z0-9-]+$`; normalize + warn on case-changed input
      * Step writes `ships/<ship-slug>/location.yaml` via `write_file`
      * Step offers optional deckplan stub at `ships/<ship-slug>/map/main.yaml` with empty rooms list
      * Step confirms paths written and reminds GM about Pitfalls P1 and P5

    janus-update-galaxy frontmatter values (exact):
    - name: `janus-update-galaxy`
    - description: contains `Use when` (suggested: `"Edit data/galaxy/star_map.yaml to add nebulae, travel routes, or modify star system visual properties. The 'galaxy painter' skill. Use when adjusting the visual galaxy map."`)
    - argument-hint: `"<action> [args]"` (action keyword: `add-nebula | add-route | edit-system`)
    - allowed-tools (exactly 2, in this order): `mcp__JanusGM__read_file`, `mcp__JanusGM__write_file`

    janus-update-galaxy body sections (in order):
    - objective: read star_map.yaml, apply one of three sub-actions, write back. Explicitly state this skill does NOT create new systems (that is `janus-add-system`).
    - schema include: exact line `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
    - process: numbered list (12–18 steps) covering all of:
      * Parse arguments for action keyword (prompt if missing)
      * `read_file("galaxy/star_map.yaml")` + parse YAML
      * Branch add-nebula: prompt for name, position [x,y,z], color (default 0x5aaa9a), size (default 50), particle_count (default 3000), opacity (default 0.15), type (default `emission`); append to `nebulae[]`
      * Branch add-route: prompt for `from` and `to` system names (must exist in `systems[]` — refuse if missing); prompt for color (default 0x5a7a9a), route_type (default `major_trade`; allow `minor_trade`, `restricted`, `military`), travel_time_days (default 50); append to `routes[]`
      * Branch edit-system: prompt for system by name (must exist — refuse if missing); display current values for color, size, type, label, position, info.description, info.population; prompt for new values (blank = keep)
      * Re-serialize preserving all top-level keys (camera, systems, routes, nebulae); note the PyYAML hex-color caveat — if dump emits decimals for `0xRRGGBB` values, post-process to restore hex literal form
      * `write_file("galaxy/star_map.yaml", updated_content)`
      * Confirm to user + mention SSE broadcast will refresh the player star map

    After writing both files, run frontmatter validation:
    `for f in skills/janus-add-ship/SKILL.md skills/janus-update-galaxy/SKILL.md; do python3 -c "import yaml,sys; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name'] in ('janus-add-ship','janus-update-galaxy'); assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools'])"; done`

    Do NOT commit yet — Task 3 commits all 4 skills together.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && for f in skills/janus-add-ship/SKILL.md skills/janus-update-galaxy/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name'] in ('janus-add-ship','janus-update-galaxy'); assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK',f)" || exit 1; done && grep -q "parent_type" skills/janus-add-ship/SKILL.md && grep -q "body_slug" skills/janus-add-ship/SKILL.md && grep -q "nebulae" skills/janus-update-galaxy/SKILL.md && grep -q "routes" skills/janus-update-galaxy/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Both SKILL.md files exist at the listed paths
    - Both parse via the python yaml frontmatter check (name matches dir, `Use when` present, all allowed-tools start with `mcp__JanusGM__`)
    - `skills/janus-add-ship/SKILL.md` `allowed-tools` is exactly 3 entries (list_files, read_file, write_file) — verify by parsing the list
    - `skills/janus-update-galaxy/SKILL.md` `allowed-tools` is exactly 2 entries (read_file, write_file) — verify by parsing
    - `skills/janus-add-ship/SKILL.md` contains literal tokens `parent_type`, `body_slug`, `system_slug`, `orbital`
    - `skills/janus-add-ship/SKILL.md` mentions Pitfall P5 — grep for `moon` AND `planet` both find matches; grep for `P5` or `body_slug only` finds a match
    - `skills/janus-update-galaxy/SKILL.md` contains literal tokens `nebulae`, `routes`, `star_map.yaml`
    - `skills/janus-update-galaxy/SKILL.md` mentions the PyYAML hex color caveat — grep for `hex` or `0x` finds a match
    - Neither file contains `@./resources/` or `@resources/` (Pitfall P3)
    - Both files contain the exact line `@$HOME/.claude/janus-skills/resources/schema-ships.md` or `@$HOME/.claude/janus-skills/resources/schema-galaxy.md` respectively
    - Both files end with a newline (`tail -c 1` returns 0x0a)
    - Per-file line count between 50 and 200
  </acceptance_criteria>
  <done>
    janus-add-ship and janus-update-galaxy SKILL.md files exist with valid frontmatter, narrow tool surface, correct schema references, and process steps documenting all branches + pitfalls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author janus-generate-context and janus-session-prep</name>
  <files>
    /home/gjohnson/mothership/janus-skills/skills/janus-generate-context/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-session-prep/SKILL.md
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/data/galaxy/anchor-system/veil-station/janus.yaml
    /home/gjohnson/mothership/charon/data/galaxy/anchor-system/veil-station/location.yaml
    /home/gjohnson/mothership/janus-skills/resources/schema-janus-context.md
    /home/gjohnson/mothership/janus-skills/resources/schema-galaxy.md
    /home/gjohnson/mothership/janus-skills/skills/janus-add-npc/SKILL.md
  </read_first>
  <action>
    Same skill pattern as Task 1.

    janus-generate-context frontmatter (exact):
    - name: `janus-generate-context`
    - description: contains `Use when` (suggested: `"Read a location.yaml and write a sibling janus.yaml capturing the AI-curated GM context for that location. Core skill for the Obsidian→JANUS notes pipeline. Use when generating or refreshing a location's runtime context."`)
    - argument-hint: `"<location-path> [notes-or-source-text]"`
    - allowed-tools (exactly 2, in this order): `mcp__JanusGM__read_file`, `mcp__JanusGM__write_file`

    Body sections:
    - objective: states output `data/<location-path>/janus.yaml` (sibling to location.yaml), describes input (location.yaml + free-text notes), names the consumer (JANUS AI runtime reads janus.yaml during play).
    - TWO schema includes (must include BOTH on separate lines):
      * `@$HOME/.claude/janus-skills/resources/schema-galaxy.md`
      * `@$HOME/.claude/janus-skills/resources/schema-janus-context.md`
    - process: numbered list (8–12 steps) requiring:
      * Parse arguments: location path (relative to `data/`, e.g., `galaxy/anchor-system/veil-station` or `ships/patrol_gunboat`) + optional notes blob (free-text from Obsidian export or user prompt)
      * `read_file("<location-path>/location.yaml")` — abort if not found
      * If a sibling `janus.yaml` already exists: `read_file("<location-path>/janus.yaml")` to capture prior context for refresh-style edits; ask user whether to overwrite or merge
      * Synthesize a context body following the veil-station/janus.yaml shape: header line ([LOCATION NAME] — [TYPE/CLASS]), status sentence, then a 1–3 paragraph operational context summary, written in-character for GM use (terse, technical, atmospheric)
      * Generate ISO 8601 UTC timestamp for `generated` field
      * Build janus.yaml content: top-level `generated` and `context` (use YAML literal block `|`)
      * `write_file("<location-path>/janus.yaml", content)` — triggers SSE broadcast which refreshes the JANUS terminal automatically
      * Confirm output path + summarize the generated context length

    janus-session-prep frontmatter (exact):
    - name: `janus-session-prep`
    - description: contains `Use when` (suggested: `"Compose a structured GM session brief from live game state plus persisted janus.yaml context files. Use when starting a session or refreshing situational awareness mid-game."`)
    - argument-hint: `"[focus-area]"`
    - allowed-tools (exactly 3, in this order): `mcp__JanusGM__get_session_context`, `mcp__JanusGM__list_files`, `mcp__JanusGM__read_file`

    Body sections:
    - objective: states this is a READ-ONLY skill (no file writes). Output is a structured brief printed to the chat — never a YAML file.
    - schema include: exact single line `@$HOME/.claude/janus-skills/resources/schema-janus-context.md`
    - process: numbered list (8–12 steps) requiring:
      * Parse optional focus-area argument (e.g., `crew`, `current-location`, `npc:<id>`)
      * Call `get_session_context()` — returns active_view, current_location, encounter state if any, ship status, recent message activity, NPC roster
      * From `current_location` derive the location path; `read_file("<location-path>/janus.yaml")` if exists, swallow error if missing
      * For each NPC present at the location: `read_file("campaign/npcs/<id>.yaml")` (capped at 5 NPCs to keep context lean — list remaining)
      * Optionally `read_file("campaign/ship/<ship>.yaml")` for ship status detail
      * If focus-area=`npc:<id>`: skip generic NPC pull, fetch only the specified NPC
      * Compose a Markdown-structured brief with these sections, in this order: `Active View`, `Current Location` (location.yaml summary + janus.yaml context body if present), `Ship Status`, `NPCs Present`, `Open Threads` (derived from recent activity), `Suggested GM Beats` (1–3 short prompts based on context — these are LLM judgements, not data fetches)
      * Print the brief directly to the chat — do NOT call `write_file` (no file writes per the read-only constraint)

    Both files end with newline. Validate frontmatter via the same python3 yaml check from Task 1.

    Do NOT commit yet — Task 3 commits all 4 skills together.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && for f in skills/janus-generate-context/SKILL.md skills/janus-session-prep/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name'] in ('janus-generate-context','janus-session-prep'); assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK',f)" || exit 1; done && grep -q "janus.yaml" skills/janus-generate-context/SKILL.md && grep -q "schema-janus-context.md" skills/janus-generate-context/SKILL.md && grep -q "schema-galaxy.md" skills/janus-generate-context/SKILL.md && grep -q "get_session_context" skills/janus-session-prep/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Both SKILL.md files exist at listed paths
    - Both parse via python yaml frontmatter check (name matches dir, `Use when` present, all allowed-tools start with `mcp__JanusGM__`)
    - `skills/janus-generate-context/SKILL.md` `allowed-tools` is exactly 2 entries (read_file, write_file)
    - `skills/janus-generate-context/SKILL.md` contains BOTH `@$HOME/.claude/janus-skills/resources/schema-galaxy.md` AND `@$HOME/.claude/janus-skills/resources/schema-janus-context.md` (two separate include lines)
    - `skills/janus-generate-context/SKILL.md` mentions `generated` and `context` fields (the two janus.yaml top-level keys)
    - `skills/janus-generate-context/SKILL.md` mentions ISO 8601 UTC timestamp generation
    - `skills/janus-session-prep/SKILL.md` `allowed-tools` is exactly 3 entries: get_session_context, list_files, read_file (note: NO write_file — this is the only read-only skill)
    - `skills/janus-session-prep/SKILL.md` explicitly states it does not write files — grep for `read-only` or `no file write` or `do NOT call write_file` finds a match
    - `skills/janus-session-prep/SKILL.md` contains the literal token `get_session_context`
    - Neither file contains `@./resources/` or `@resources/` (Pitfall P3)
    - Both files end with newline
    - Per-file line count between 50 and 200
  </acceptance_criteria>
  <done>
    janus-generate-context and janus-session-prep SKILL.md files exist with valid frontmatter, correct tool surfaces (write tool only for generate-context; no write tool for session-prep), and the two-schema include for generate-context.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit all 4 skills from this plan</name>
  <files>
    (no new files — commit existing artifacts from Tasks 1 and 2)
  </files>
  <read_first>
    /home/gjohnson/mothership/janus-skills/skills/janus-add-ship/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-update-galaxy/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-generate-context/SKILL.md
    /home/gjohnson/mothership/janus-skills/skills/janus-session-prep/SKILL.md
  </read_first>
  <action>
    From `/home/gjohnson/mothership/janus-skills`:
    1. Stage all 4 skills produced in Tasks 1 and 2:
       `git add skills/janus-add-ship/SKILL.md skills/janus-update-galaxy/SKILL.md skills/janus-generate-context/SKILL.md skills/janus-session-prep/SKILL.md`
    2. Commit with this message:
       ```
       feat(skills): add 4 remaining skills (D-11, D-14, D-17, D-18)

       - janus-add-ship          (ships/<slug>/location.yaml with body_slug self-injection)
       - janus-update-galaxy     (galaxy painter — nebulae, routes, system visual props)
       - janus-generate-context  (Obsidian→janus.yaml pipeline core)
       - janus-session-prep      (read-only GM brief composer)

       Completes the 8-skill MVP set. All skills use mcp__JanusGM__ tool
       prefix and HOME-anchored @-includes for portability across global and
       project install modes.

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```
    3. Verify clean tree: `git status --porcelain` must be empty.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/janus-skills && [ "$(find skills -name SKILL.md | wc -l)" = "8" ] && git log --oneline | head -1 | grep -q "feat(skills)" && [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `find /home/gjohnson/mothership/janus-skills/skills -name SKILL.md` returns exactly 8 files (4 from plan 24-04 + 4 from this plan)
    - The 8 SKILL.md directory names match exactly: janus-add-npc, janus-add-location, janus-add-system, janus-add-body, janus-add-ship, janus-update-galaxy, janus-generate-context, janus-session-prep
    - `git -C /home/gjohnson/mothership/janus-skills log --oneline | head -1` subject begins `feat(skills):` and mentions `D-11, D-14, D-17, D-18`
    - `git -C /home/gjohnson/mothership/janus-skills status --porcelain` returns empty
    - All 8 SKILL.md files pass the python yaml frontmatter check in one loop: `for f in skills/*/SKILL.md; do python3 -c "import yaml; c=open('$f').read(); p=c.split('---'); fm=yaml.safe_load(p[1]); assert fm['name']; assert 'Use when' in fm['description']; assert all(t.startswith('mcp__JanusGM__') for t in fm['allowed-tools']); print('OK',fm['name'])"; done` succeeds for all 8
  </acceptance_criteria>
  <done>
    All 8 skills committed. The MVP skill set is complete. Working tree clean.
  </done>
</task>

</tasks>

<verification>
- `find /home/gjohnson/mothership/janus-skills/skills -name SKILL.md` returns exactly 8 files
- One-liner validation pass for all 8 frontmatters
- No SKILL.md contains forbidden `@./resources/` or `@resources/` patterns
- `git log --oneline` on janus-skills shows: initial → resources → install → skills-batch-A → skills-batch-B (5 commits total)
</verification>

<success_criteria>
- All 4 remaining MVP skills (D-11, D-14, D-17, D-18) exist with valid frontmatter
- janus-session-prep is the only skill without write_file in allowed-tools (read-only)
- janus-generate-context is the only skill with TWO schema @-includes (galaxy + janus-context)
- All committed in one logical commit
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-05-SUMMARY.md` recording:
- Commit hash
- Per-skill line count + allowed-tools count for all 4 in this plan
- Sanity table: skill name → allowed-tools count → schema includes (1 or 2)
- Confirmation all 8 SKILL.md files pass the frontmatter check
</output>
