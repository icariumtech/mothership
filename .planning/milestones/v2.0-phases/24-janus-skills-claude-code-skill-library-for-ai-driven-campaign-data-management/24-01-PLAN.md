---
phase: 24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/gjohnson/mothership/janus-skills/.gitignore
  - /home/gjohnson/mothership/janus-skills/mcp-config-template.json
  - /home/gjohnson/mothership/janus-skills/resources/.gitkeep
  - /home/gjohnson/mothership/janus-skills/skills/.gitkeep
autonomous: true
requirements: []

must_haves:
  truths:
    - "A standalone git repo exists at ~/mothership/janus-skills with no charon coupling"
    - "Repo has resources/ and skills/ directories ready for content"
    - "mcp-config-template.json is valid JSON with the JanusGM HTTP server entry"
  artifacts:
    - path: "/home/gjohnson/mothership/janus-skills/.git"
      provides: "Git history root for the new repository"
    - path: "/home/gjohnson/mothership/janus-skills/mcp-config-template.json"
      provides: "Claude Code mcpServers config template with HOMELAB_IP placeholder"
      contains: '"type": "http"'
    - path: "/home/gjohnson/mothership/janus-skills/resources"
      provides: "Directory for the 5 schema resource files"
    - path: "/home/gjohnson/mothership/janus-skills/skills"
      provides: "Directory for the 8 SKILL.md subdirectories"
  key_links:
    - from: "mcp-config-template.json"
      to: "FastMCP server name"
      via: "mcpServers key 'JanusGM' must match FastMCP('JanusGM') in mcp_server.py"
      pattern: '"JanusGM"'
---

<objective>
Scaffold the new standalone `janus-skills` repository at `~/mothership/janus-skills/` (sibling to charon, NOT inside it). Initialize git, create the `resources/` and `skills/` directories, and write the `mcp-config-template.json` template. This plan establishes the empty shell that all subsequent plans populate.

Purpose: All downstream plans (resources, skills, install.sh, README) write files into this repository. Without the directory + git init, none of them can run.

Output: A clean git repo at `~/mothership/janus-skills/` with the required top-level layout and one config artifact (`mcp-config-template.json`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
@.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
@mcp_server.py

<interfaces>
<!-- The FastMCP server name string is the single source of truth for tool prefix and mcpServers key. -->

From mcp_server.py line 22:
- `mcp = FastMCP("JanusGM")` — this string ("JanusGM", case-sensitive) is both:
  - the `mcpServers` key in `.claude/settings.json`
  - the tool name prefix: `mcp__JanusGM__<tool>`

MCP HTTP transport config shape (from research, verified):
```json
{
  "mcpServers": {
    "JanusGM": {
      "type": "http",
      "url": "http://<server-ip>:8001/mcp/",
      "description": "..."
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize janus-skills repo with directory skeleton</name>
  <files>
    /home/gjohnson/mothership/janus-skills/.git/ (created by git init)
    /home/gjohnson/mothership/janus-skills/.gitignore
    /home/gjohnson/mothership/janus-skills/resources/.gitkeep
    /home/gjohnson/mothership/janus-skills/skills/.gitkeep
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-CONTEXT.md
    /home/gjohnson/mothership/charon/.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
  </read_first>
  <action>
    Create directory `/home/gjohnson/mothership/janus-skills/` (sibling to charon — must NOT be inside charon). Inside it:
    1. Run `git init` to create `.git/`. Set the initial branch to `main` (use `git init --initial-branch=main` or `git symbolic-ref HEAD refs/heads/main`).
    2. Create `.gitignore` containing exactly these lines: `.DS_Store`, `*.swp`, `*.swo`, `.idea/`, `.vscode/`, `node_modules/`, `__pycache__/`.
    3. Create empty subdirectories `resources/` and `skills/` and add a `.gitkeep` file inside each so git tracks them (the directories will be populated by later plans).
    4. Stage `.gitignore`, `resources/.gitkeep`, `skills/.gitkeep` (do NOT commit yet — commit happens after Task 2 completes so the initial commit includes the MCP config template).
    Per D-01 through D-10: this repo is standalone — do not create symlinks back to charon, do not reference charon paths in repo contents, do not nest under charon.
  </action>
  <verify>
    <automated>test -d /home/gjohnson/mothership/janus-skills/.git && test -d /home/gjohnson/mothership/janus-skills/resources && test -d /home/gjohnson/mothership/janus-skills/skills && test -f /home/gjohnson/mothership/janus-skills/.gitignore && cd /home/gjohnson/mothership/janus-skills && git rev-parse --is-inside-work-tree</automated>
  </verify>
  <acceptance_criteria>
    - `/home/gjohnson/mothership/janus-skills/.git/HEAD` exists and contains `ref: refs/heads/main`
    - `/home/gjohnson/mothership/janus-skills/.gitignore` exists and contains the exact line `__pycache__/`
    - `/home/gjohnson/mothership/janus-skills/resources/.gitkeep` exists (zero bytes is fine)
    - `/home/gjohnson/mothership/janus-skills/skills/.gitkeep` exists (zero bytes is fine)
    - `git -C /home/gjohnson/mothership/janus-skills status --porcelain` lists `.gitignore`, `resources/.gitkeep`, `skills/.gitkeep` as either staged or untracked
    - `/home/gjohnson/mothership/janus-skills/` is NOT a subdirectory of `/home/gjohnson/mothership/charon/` (verify with `realpath` — paths must be siblings)
  </acceptance_criteria>
  <done>
    Standalone git repo initialized at `~/mothership/janus-skills/` on branch `main` with `resources/` and `skills/` subdirectories tracked via `.gitkeep`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write mcp-config-template.json + initial commit</name>
  <files>
    /home/gjohnson/mothership/janus-skills/mcp-config-template.json
  </files>
  <read_first>
    /home/gjohnson/mothership/charon/mcp_server.py
    /home/gjohnson/mothership/charon/.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-RESEARCH.md
  </read_first>
  <action>
    Create `/home/gjohnson/mothership/janus-skills/mcp-config-template.json` as a JSON document with this exact top-level shape:
    - Top-level key: `mcpServers` (object)
    - Inside `mcpServers`, one key: `JanusGM` (string, case-sensitive — must match `FastMCP("JanusGM")` in `mcp_server.py` line 22, per D-10)
    - Inside the `JanusGM` object, three string fields:
      - `type`: exact value `"http"`
      - `url`: exact value `"http://HOMELAB_IP:8001/mcp/"` (the literal string `HOMELAB_IP` is the placeholder that `install.sh` will replace; do NOT use angle brackets, do NOT use 127.0.0.1)
      - `description`: exact value `"JANUS GM campaign data server — reads and writes campaign YAML files"`
    Use 2-space indentation. End with a trailing newline.

    After writing the file: validate it parses with `python3 -c "import json,sys; json.load(open('/home/gjohnson/mothership/janus-skills/mcp-config-template.json'))"` — this MUST succeed.

    Then create the initial commit. From `/home/gjohnson/mothership/janus-skills`:
    1. `git add .gitignore resources/.gitkeep skills/.gitkeep mcp-config-template.json`
    2. Commit with message:
       ```
       chore: initialize janus-skills repository

       Scaffold standalone repo with resources/ and skills/ directories
       and the Claude Code MCP server config template (D-10).

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       ```
    Do NOT push (no remote yet). Do NOT touch charon git state.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/home/gjohnson/mothership/janus-skills/mcp-config-template.json')); assert d['mcpServers']['JanusGM']['type']=='http'; assert 'HOMELAB_IP' in d['mcpServers']['JanusGM']['url']; assert d['mcpServers']['JanusGM']['url'].endswith(':8001/mcp/'); print('OK')" && git -C /home/gjohnson/mothership/janus-skills log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON (python3 json.load succeeds)
    - Path `mcpServers.JanusGM.type` equals string `"http"`
    - Path `mcpServers.JanusGM.url` equals exact string `"http://HOMELAB_IP:8001/mcp/"`
    - Path `mcpServers.JanusGM.description` is a non-empty string
    - `git -C /home/gjohnson/mothership/janus-skills log --oneline` shows exactly one commit (the initial)
    - `git -C /home/gjohnson/mothership/janus-skills status --porcelain` returns empty (working tree clean)
    - `grep -c '"JanusGM"' /home/gjohnson/mothership/janus-skills/mcp-config-template.json` returns `1` (exactly one occurrence)
  </acceptance_criteria>
  <done>
    `mcp-config-template.json` exists, parses as JSON with the correct `mcpServers.JanusGM` shape, and the initial commit on `main` includes the gitignore, resource/skill placeholders, and the config template.
  </done>
</task>

</tasks>

<verification>
- Run `ls -la /home/gjohnson/mothership/janus-skills/` and confirm: `.git/`, `.gitignore`, `mcp-config-template.json`, `resources/`, `skills/`
- Run `git -C /home/gjohnson/mothership/janus-skills log` and confirm exactly one commit on branch `main`
- Run `realpath /home/gjohnson/mothership/janus-skills /home/gjohnson/mothership/charon` and confirm the two paths share the same parent directory `/home/gjohnson/mothership` (sibling check)
- `python3 -c "import json; json.load(open('/home/gjohnson/mothership/janus-skills/mcp-config-template.json'))"` succeeds with no output
</verification>

<success_criteria>
- janus-skills repo is a standalone git repository at `~/mothership/janus-skills/`, NOT nested in charon
- `resources/` and `skills/` subdirectories exist (placeholder-tracked) ready for downstream plans
- `mcp-config-template.json` is valid JSON, uses the literal `HOMELAB_IP` placeholder, server key is exactly `"JanusGM"` matching `FastMCP("JanusGM")`
- Initial commit on branch `main` includes all four tracked files
</success_criteria>

<output>
After completion, create `.planning/phases/24-janus-skills-claude-code-skill-library-for-ai-driven-campaign-data-management/24-01-SUMMARY.md` recording:
- Confirmed sibling layout (paths verified)
- Initial commit hash
- Exact content of `mcp-config-template.json` (so downstream plans can rely on its shape)
</output>
