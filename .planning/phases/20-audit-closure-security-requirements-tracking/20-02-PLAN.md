---
phase: 20-audit-closure-security-requirements-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STAT-10
  - STAT-11
  - STAT-12
  - STAT-13
  - STAT-14
  - ANIM-VIEW
  - ANIM-OVERLAY
  - ANIM-ROOM
  - ANIM-BRIDGE
must_haves:
  truths:
    - "STAT-10, STAT-11, STAT-12, STAT-13, STAT-14 are all `[x]` in REQUIREMENTS.md (currently `[ ]`)"
    - "ANIM-VIEW, ANIM-OVERLAY, ANIM-ROOM, ANIM-BRIDGE appear as `[x]` requirement entries with descriptions in REQUIREMENTS.md"
    - "Traceability table contains rows for STAT-10..14 with status `Complete` (currently `Pending`)"
    - "Traceability table contains rows for ANIM-VIEW/OVERLAY/ROOM/BRIDGE mapped to Phase 13 with status `Complete`"
    - "Coverage count comment updated from 47 to 51 with the +4 ANIM- accounting line edit"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes (STAT-10..14 flipped) + new ANIM-* section + updated coverage count + new traceability rows"
      contains: "ANIM-VIEW"
  key_links:
    - from: ".planning/REQUIREMENTS.md ANIM-* section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "matching req IDs in both list and table"
      pattern: "ANIM-VIEW \\| Phase 13 \\| Complete"
---

<objective>
Bring `.planning/REQUIREMENTS.md` into alignment with the actual implemented state of Phases 13 and 14 by:
1. Flipping STAT-10..14 checkboxes from `[ ]` to `[x]` (Phase 14 is complete; the existing Phase 14 VERIFICATION.md proves this — only the checkboxes lag, per RESEARCH.md "REQUIREMENTS.md Change Specification").
2. Adding a new "Atmospheric Animations (Phase 13)" section with ANIM-VIEW, ANIM-OVERLAY, ANIM-ROOM, ANIM-BRIDGE entries (currently referenced in ROADMAP.md but never registered).
3. Updating the traceability table: STAT-10..14 status `Pending` → `Complete`; adding 4 new ANIM-* rows mapped to Phase 13.
4. Updating the coverage count comment from "47 total" to "51 total" with the explicit "+ 4 ANIM-*" accounting line.

Per CONTEXT.md D-01 + RESEARCH.md, GRID-01..10 are already `[x]` (Phase 12 did this) — do NOT touch GRID checkboxes.
Per RESEARCH.md Pitfall 3, do NOT create a new Phase 14 VERIFICATION.md (one already exists at `VERIFICATION.md` with `status: human_needed`, 12/12 truths verified).

Purpose: Eliminate the audit gap "STAT-10..14 unchecked, ANIM-* unregistered" so the v1.0 milestone REQUIREMENTS.md formally reflects what was built.

Output: Single edit to `.planning/REQUIREMENTS.md` covering checkboxes, new section, traceability rows, and coverage count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/20-audit-closure-security-requirements-tracking/20-CONTEXT.md
@.planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md
@.planning/phases/14-rework-bridge-status-tab-ship-systems-remove-armor-add-react/VERIFICATION.md
@.planning/phases/13-atmospheric-ui-animations-for-player-facing-transitions-and-element-reveals/13-05-SUMMARY.md

<interfaces>
<!-- Existing REQUIREMENTS.md format (verified by reading the file). Match this exact pattern. -->

```markdown
- [x] **REQ-ID**: description text
```

<!-- Traceability table row format (verified at REQUIREMENTS.md line 140+). -->
```markdown
| REQ-ID | Phase N | Complete |
```

<!-- Section heading format (verified at REQUIREMENTS.md "### GM Console UI (Phase 8)"). -->
```markdown
### Atmospheric Animations (Phase 13)
```

<!-- Coverage count line — currently at REQUIREMENTS.md lines 192–195. -->
**Coverage:**
- v1 requirements: 47 total (29 original + 12 GMUI-* + 1 SHIP-01 + 5 STAT-10..14; AUDI-01..03 moved to v2)
- Mapped to phases: 47
- Unmapped: 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip STAT-10..14 checkboxes from [ ] to [x] (5 lines)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — need lines 24–28 for the checkbox section, plus context around the surrounding STAT-* requirements)
    - .planning/phases/14-rework-bridge-status-tab-ship-systems-remove-armor-add-react/VERIFICATION.md (proves implementation: 12/12 truths verified)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, locate the `### Ship Status` section. Five lines currently start with `- [ ] **STAT-1`:

    - Line 24: `- [ ] **STAT-10**: STATUS tab uses dual terminal-readout panel layout ...`
    - Line 25: `- [ ] **STAT-11**: Reactor added as 5th ship system ...`
    - Line 26: `- [ ] **STAT-12**: Ship resource tracking for fuel, food, O2, cryopods, and escape pods ...`
    - Line 27: `- [ ] **STAT-13**: GM can modify reactor status/condition and all resource values ...`
    - Line 28: `- [ ] **STAT-14**: Status change-flash animation (600ms row highlight on SSE update) ...`

    Replace `[ ]` with `[x]` on each of those five lines. Do NOT change the description text — only the checkbox state.

    Implements D-01 (formalize Phase 14 completion in REQUIREMENTS.md without re-running tests).

    Do NOT touch:
    - LOGS-01..03, STAT-01..06 (already `[x]`)
    - TOKN-01..05, PORT-01..05, RTMA-01..04, GRID-01..10, GMUI-*, SHIP-01 (already `[x]`)
    - Any v2 requirement (the indented bullets without checkboxes under v2 sections)
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon &amp;&amp; grep -c "^- \[x\] \*\*STAT-1[01234]\*\*:" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*STAT-1[01234]\*\*:" .planning/REQUIREMENTS.md` outputs `5` (all five flipped to `[x]`)
    - `grep -c "^- \[ \] \*\*STAT-1[01234]\*\*:" .planning/REQUIREMENTS.md` outputs `0` (none remain unchecked)
    - `grep "^- \[x\] \*\*STAT-10\*\*:" .planning/REQUIREMENTS.md` returns the line still containing the original phrase "dual terminal-readout panel layout" (description preserved)
    - `grep "^- \[x\] \*\*STAT-14\*\*:" .planning/REQUIREMENTS.md` returns the line still containing "600ms row highlight" (description preserved)
    - File still contains exactly one occurrence of `### Ship Status` (section heading not duplicated)
  </acceptance_criteria>
  <done>
    All five STAT-10..14 entries are `[x]` with their original description text preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Atmospheric Animations (Phase 13) section with ANIM-* requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (need the section structure — specifically the placement of "### GM Console UI (Phase 8)" and "### Player Ship Map (Phase 10)")
    - .planning/phases/13-atmospheric-ui-animations-for-player-facing-transitions-and-element-reveals/13-05-SUMMARY.md (evidence of implementation — all 4 systems APPROVED)
    - .planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md (REQUIREMENTS.md Change Specification — exact wording suggested for ANIM-* descriptions)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, insert a new section heading and four requirement entries between the existing "### GM Console UI (Phase 8)" section block (which ends with the GMUI-DISPLAY entry around line 78) and the "### Player Ship Map (Phase 10)" section (around line 80).

    The new section to add (verbatim):

    ```markdown
    ### Atmospheric Animations (Phase 13)

    - [x] **ANIM-VIEW**: View-to-view transitions use glitch-out / dark-frame / fade-in animation sequence via the `useViewTransition` hook; `ViewStatusOverlay` displays a per-view typewriter boot label during fade-in
    - [x] **ANIM-OVERLAY**: Dialog overlays (CharonDialog, DocumentDialog, CommTerminalDialog) animate in with CRT-style entrance effects and fade-out with scale-down on dismiss
    - [x] **ANIM-ROOM**: Encounter room reveal plays a CRT digital flicker animation; tokens in newly revealed rooms cascade in with ~200ms delay; multi-room reveals stagger 75ms apart
    - [x] **ANIM-BRIDGE**: Bridge view panels animate in with a staggered fade-slide sequence (~100ms per item, ~1.2s total) on view load
    ```

    Insert with one blank line above the heading and one blank line after the last bullet (matching the spacing of surrounding sections).

    The descriptions are based on RESEARCH.md "Phase 13 — Atmospheric UI Animations" inventory (which derives them from 13-01..13-04 SUMMARY evidence and 13-UAT.md test results).
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon &amp;&amp; grep -c "^- \[x\] \*\*ANIM-\(VIEW\|OVERLAY\|ROOM\|BRIDGE\)\*\*:" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Atmospheric Animations (Phase 13)" .planning/REQUIREMENTS.md` outputs `1` (new section heading present)
    - `grep -c "^- \[x\] \*\*ANIM-\(VIEW\|OVERLAY\|ROOM\|BRIDGE\)\*\*:" .planning/REQUIREMENTS.md` outputs `4` (all four entries present and checked)
    - `grep "ANIM-VIEW" .planning/REQUIREMENTS.md` matches a line containing the phrase "useViewTransition"
    - `grep "ANIM-OVERLAY" .planning/REQUIREMENTS.md` matches a line containing the phrase "CharonDialog"
    - `grep "ANIM-ROOM" .planning/REQUIREMENTS.md` matches a line containing the phrase "CRT digital flicker"
    - `grep "ANIM-BRIDGE" .planning/REQUIREMENTS.md` matches a line containing the phrase "staggered fade-slide"
    - The new section appears between "### GM Console UI (Phase 8)" and "### Player Ship Map (Phase 10)" — verified by reading file: `awk '/^### GM Console UI/,/^### Player Ship Map/' .planning/REQUIREMENTS.md | grep -c "ANIM-"` outputs `4`
  </acceptance_criteria>
  <done>
    New section `### Atmospheric Animations (Phase 13)` exists with all four ANIM-* entries as `[x]`, placed between the GM Console UI and Player Ship Map sections.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update traceability table (STAT-10..14 → Complete; add 4 ANIM-* rows) and coverage count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 138–195 — traceability table + coverage count comment block)
    - .planning/phases/20-audit-closure-security-requirements-tracking/20-RESEARCH.md (REQUIREMENTS.md Change Specification — exact format for traceability rows + coverage count update)
  </read_first>
  <action>
    Three edits in `.planning/REQUIREMENTS.md`, all in the lower half of the file (Traceability section + Coverage block):

    **Edit A — STAT-10..14 status rows (lines 150–154 currently say `Pending`):**
    Replace these five rows so each ends with `Complete` instead of `Pending`. Keep the leading `| STAT-XX | Phase 14 |` columns identical:
    - `| STAT-10 | Phase 14 | Complete |`
    - `| STAT-11 | Phase 14 | Complete |`
    - `| STAT-12 | Phase 14 | Complete |`
    - `| STAT-13 | Phase 14 | Complete |`
    - `| STAT-14 | Phase 14 | Complete |`

    Do NOT modify any other row in that table (LOGS-01..03 should stay `Pending` if they currently show `Pending` — those are out of scope for this phase per CONTEXT D-01 boundary; verify before editing — leave non-STAT rows untouched).

    **Edit B — Add 4 ANIM-* rows to the traceability table:**
    After the SHIP-01 row (currently the last `Phase 10 | Complete` row near line 190), append these four rows:
    ```
    | ANIM-VIEW | Phase 13 | Complete |
    | ANIM-OVERLAY | Phase 13 | Complete |
    | ANIM-ROOM | Phase 13 | Complete |
    | ANIM-BRIDGE | Phase 13 | Complete |
    ```
    Insert before the `**Coverage:**` line/block. Maintain the table's pipe-delimited structure (the existing table has no closing row).

    **Edit C — Update the Coverage count comment (currently at line 193):**
    Replace this exact line:
    ```
    - v1 requirements: 47 total (29 original + 12 GMUI-* + 1 SHIP-01 + 5 STAT-10..14; AUDI-01..03 moved to v2)
    ```
    With:
    ```
    - v1 requirements: 51 total (29 original + 12 GMUI-* + 1 SHIP-01 + 5 STAT-10..14 + 4 ANIM-*; AUDI-01..03 moved to v2)
    ```
    And replace `Mapped to phases: 47` with `Mapped to phases: 51` on the line below.
  </action>
  <verify>
    <automated>cd /home/gjohnson/mothership/charon &amp;&amp; grep -c "^| STAT-1[01234] | Phase 14 | Complete |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| STAT-1[01234] | Phase 14 | Complete |" .planning/REQUIREMENTS.md` outputs `5` (all five STAT-10..14 rows now `Complete`)
    - `grep -c "^| STAT-1[01234] | Phase 14 | Pending |" .planning/REQUIREMENTS.md` outputs `0` (no STAT-10..14 rows still Pending)
    - `grep -c "^| ANIM-\(VIEW\|OVERLAY\|ROOM\|BRIDGE\) | Phase 13 | Complete |" .planning/REQUIREMENTS.md` outputs `4` (all four ANIM-* traceability rows present)
    - `grep -c "v1 requirements: 51 total" .planning/REQUIREMENTS.md` outputs `1` (count updated to 51)
    - `grep -c "v1 requirements: 47 total" .planning/REQUIREMENTS.md` outputs `0` (old count fully replaced)
    - `grep -c "Mapped to phases: 51" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -c "+ 4 ANIM-\*" .planning/REQUIREMENTS.md` outputs `1` (accounting line includes ANIM contribution)
  </acceptance_criteria>
  <done>
    Traceability table reflects STAT-10..14 as Complete and includes ANIM-VIEW/OVERLAY/ROOM/BRIDGE rows mapped to Phase 13. Coverage line reads `51 total` with `+ 4 ANIM-*` in the breakdown.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs | No runtime trust boundary — this plan only edits Markdown text in `.planning/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Repudiation | `.planning/REQUIREMENTS.md` traceability | accept | Documentation-only change; git commit log provides immutable audit trail of who flipped which checkboxes when |
</threat_model>

<verification>
## Phase-level checks (run after all three tasks complete)

1. `grep -c "^- \[x\] \*\*STAT-1[01234]\*\*:" .planning/REQUIREMENTS.md` outputs `5`.
2. `grep -c "^- \[x\] \*\*ANIM-\(VIEW\|OVERLAY\|ROOM\|BRIDGE\)\*\*:" .planning/REQUIREMENTS.md` outputs `4`.
3. `grep -c "^| STAT-1[01234] | Phase 14 | Complete |" .planning/REQUIREMENTS.md` outputs `5`.
4. `grep -c "^| ANIM-" .planning/REQUIREMENTS.md` outputs `4`.
5. `grep -c "v1 requirements: 51 total" .planning/REQUIREMENTS.md` outputs `1`.
6. `grep -c "GRID-0\[1-9\]\|GRID-10" .planning/REQUIREMENTS.md` outputs ≥10 (GRID rows untouched, still present in both list and table).
7. `wc -l .planning/REQUIREMENTS.md` shows roughly +6 to +9 lines vs. baseline (4 ANIM list entries + 1 section heading + 4 traceability rows = ~9 net lines added; minus 0 deletions).
</verification>

<success_criteria>
- All five STAT-10..14 are checked off in the requirement list AND show `Complete` in the traceability table.
- Four ANIM-* requirements are formally registered with descriptions and traceability rows mapped to Phase 13.
- Coverage count comment reflects the new total (51) with explicit `+ 4 ANIM-*` accounting.
- GRID-01..10 rows untouched (already done by Phase 12).
- No new VERIFICATION.md created for Phase 14 (it already exists per Pitfall 3).
</success_criteria>

<output>
After completion, create `.planning/phases/20-audit-closure-security-requirements-tracking/20-02-SUMMARY.md`
</output>
