---
phase: 25-map-rotation-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/components/domain/maps/GalaxyMap.tsx
autonomous: true
requirements:
  - PHASE-25-GOAL
must_haves:
  truths:
    - "Galaxy map shows MapPlaybackControls overlay at top-center"
    - "Clicking the play/pause button toggles sceneStore.animations.autoRotate"
    - "Button reflects current autoRotate state (⏸ when rotating, ▶ when paused)"
    - "Galaxy auto-rotate does NOT auto-resume 5s after a button pause (manual pause is sticky)"
    - "No scrub bar appears on the galaxy map"
  artifacts:
    - path: "src/components/domain/maps/GalaxyMap.tsx"
      provides: "Galaxy map wrapper now imports MapPlaybackControls and wires it to sceneStore"
      contains: "MapPlaybackControls"
  key_links:
    - from: "src/components/domain/maps/GalaxyMap.tsx"
      to: "src/stores/sceneStore.ts"
      via: "useSceneStore + setAutoRotate"
      pattern: "setAutoRotate"
    - from: "src/components/domain/maps/GalaxyMap.tsx"
      to: "src/components/domain/maps/MapPlaybackControls.tsx"
      via: "import + JSX usage"
      pattern: "<MapPlaybackControls"
---

<objective>
Wire `MapPlaybackControls` into `GalaxyMap.tsx` as a sibling-to-Canvas overlay. Galaxy map only needs the play/pause button (showScrub=false) — it toggles the existing `sceneStore.animations.autoRotate` flag that `GalaxyControls.tsx` already respects.

Purpose: Galaxy is the simplest of the three map integrations (no orbital math, no scene changes). Ships D-01, D-02, D-13 per CONTEXT.md.

Critical gotcha: The pause button MUST call only `setAutoRotate(false)` — NOT `recordInteraction()`. Otherwise GalaxyControls' 5-second auto-resume timer (lines 294-298) would resume rotation 5s after every manual pause.

Output: Updated `GalaxyMap.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-map-rotation-controls/25-CONTEXT.md
@.planning/phases/25-map-rotation-controls/25-RESEARCH.md
@.planning/phases/25-map-rotation-controls/25-PATTERNS.md
@.planning/phases/25-map-rotation-controls/25-01-SUMMARY.md

<interfaces>
<!-- From sceneStore (already exists) -->
```typescript
// src/stores/sceneStore.ts
useAnimationState(): { autoRotate: boolean; ... }
setAutoRotate(enabled: boolean): void
```

<!-- From new component (Wave 1) -->
```typescript
// src/components/domain/maps/MapPlaybackControls.tsx
import { MapPlaybackControls } from './MapPlaybackControls';
// Props: isPlaying, onTogglePlay, showScrub, onScrubDelta?
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire MapPlaybackControls into GalaxyMap.tsx</name>
  <files>src/components/domain/maps/GalaxyMap.tsx</files>
  <read_first>
    - src/components/domain/maps/GalaxyMap.tsx (file being modified — read current import block, forwardRef body, JSX return)
    - src/components/domain/maps/MapPlaybackControls.tsx (Wave 1 output — confirm props contract)
    - src/stores/sceneStore.ts (confirm useSceneStore, useAnimationState, setAutoRotate exports and signatures)
    - src/components/domain/maps/r3f/galaxy/GalaxyControls.tsx (confirm 5s auto-resume only fires when lastInteractionTime is set — so setAutoRotate(false) alone is sticky)
    - .planning/phases/25-map-rotation-controls/25-PATTERNS.md (GalaxyMap.tsx modify section: import additions, store wiring, JSX overlay)
  </read_first>
  <action>
    Modify `src/components/domain/maps/GalaxyMap.tsx`:

    1) Imports — add to existing react import line (or as a new line if cleaner):
       - `useCallback` (if not already present in the react import)
       Add new imports below existing imports:
       - `import { useSceneStore, useAnimationState } from '@/stores/sceneStore';`
       - `import { MapPlaybackControls } from './MapPlaybackControls';`

    2) Inside the `forwardRef` body, after existing refs/state, add:
       - `const animations = useAnimationState();`
       - `const setAutoRotate = useSceneStore((state) => state.setAutoRotate);`
       - `const handleTogglePlay = useCallback(() => { setAutoRotate(!animations.autoRotate); }, [animations.autoRotate, setAutoRotate]);`

       Do NOT call `recordInteraction()` anywhere — see 25-RESEARCH.md Gotcha #1.

    3) JSX return — inside the existing `<div className={containerClass}>` wrapper, add `<MapPlaybackControls>` as a sibling AFTER `<Canvas>` (so it stacks above):
       ```
       <MapPlaybackControls
         isPlaying={animations.autoRotate}
         onTogglePlay={handleTogglePlay}
         showScrub={false}
       />
       ```

    Do NOT modify the `<Canvas>` block, GalaxyScene props, or any other behavior. Do NOT add a new container div — the overlay is a sibling inside the existing container.
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MapPlaybackControls" src/components/domain/maps/GalaxyMap.tsx` returns 2 or more (import + JSX)
    - `grep -c "useAnimationState\|useSceneStore" src/components/domain/maps/GalaxyMap.tsx` returns 2 or more
    - `grep -c "setAutoRotate" src/components/domain/maps/GalaxyMap.tsx` returns 1 or more
    - File contains literal string `showScrub={false}`
    - File does NOT contain literal string `recordInteraction` (per gotcha #1)
    - `pnpm run typecheck` exits 0
    - Existing `<Canvas>` JSX block remains structurally intact (no removed children of Canvas)
  </acceptance_criteria>
  <done>GalaxyMap.tsx renders the overlay; typecheck passes; auto-rotate toggle is wired without triggering the 5s resume timer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → Zustand store | A button click crosses into shared application state (sceneStore). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | autoRotate boolean flip | accept | Only valid value is boolean negation; no injection surface. Existing setAutoRotate is typed `(enabled: boolean) => void`. |
| T-25-04 | Repudiation | No audit log of pause/resume | accept | UI control with no security relevance; logging not required. |

No sensitive data handled. UI input only.
</threat_model>

<verification>
- `pnpm run typecheck` exits 0
- Visual check (deferred to Plan 05): clicking pause button on /gmconsole/ Bridge MAP tab → galaxy stops rotating and does NOT resume after 5 seconds.
</verification>

<success_criteria>
- GalaxyMap.tsx imports and renders MapPlaybackControls
- Button click toggles sceneStore.animations.autoRotate
- No recordInteraction() call exists in the new code path
- showScrub=false (no scrub bar on galaxy)
</success_criteria>

<output>
Create `.planning/phases/25-map-rotation-controls/25-02-SUMMARY.md` — note the exact line range where MapPlaybackControls was inserted and confirm whether recordInteraction was correctly omitted.
</output>
