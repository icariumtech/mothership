---
phase: 29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-
plan: 03
type: execute
wave: 3
depends_on: ['29-02']
files_modified:
  - src/components/domain/encounter/EncounterMapRenderer.tsx
  - src/components/gm/MapPreview.tsx
  - src/components/gm/views/deckplan/DeckSelector.tsx
  - src/components/gm/views/deckplan/DeckplanPreviewPane.tsx
  - src/components/gm/views/FileEditorView.tsx
  - src/components/gm/views/FileEditorView.css
autonomous: true
requirements: [EDIT-02, EDIT-03]
must_haves:
  truths:
    - "Opening a deckplan.yaml in the GM Console file editor shows a live map preview below Monaco"
    - "The preview renders from the in-memory Monaco buffer, not a re-fetch"
    - "A deck selector tab strip lets the GM switch which deck the preview renders"
    - "Editing YAML in Monaco updates the preview (debounced), keeping last-good on parse error"
    - "Non-deckplan files render exactly as before (no preview pane)"
  artifacts:
    - path: "src/components/gm/views/deckplan/DeckplanPreviewPane.tsx"
      provides: "Preview pane wrapping MapPreview, driven by Monaco buffer text + selected deck"
      min_lines: 40
    - path: "src/components/gm/views/deckplan/DeckSelector.tsx"
      provides: "Deck tab strip keyed by deck id"
    - path: "src/components/gm/views/FileEditorView.css"
      provides: "60/40 resizable split-pane + deck-tab + preview-pane styles"
  key_links:
    - from: "FileEditorView.tsx"
      to: "DeckplanPreviewPane"
      via: "isDeckplan(selectedPath) gate, mounted below Monaco"
      pattern: "isDeckplan"
    - from: "DeckplanPreviewPane"
      to: "EncounterMapRenderer onPoiClick/onPoiMove/onEmptyCellClick"
      via: "new editor-mode props"
      pattern: "onPoiClick|onPoiMove|onEmptyCellClick"
---

<objective>
Add the live map preview surface to the GM Console file editor: extend
`EncounterMapRenderer`/`MapPreview` with editor-mode POI props (`onPoiClick`,
`onPoiMove`, `onEmptyCellClick`, `editable`), build the `DeckplanPreviewPane` and
`DeckSelector`, and mount them below Monaco inside `FileEditorView` (gated on
`isDeckplan(path)`) with a 60/40 resizable split.

Purpose: Delivers EDIT-02 (live preview below Monaco) and EDIT-03 (deck selector). The
preview must render from the in-memory buffer so unsaved edits show live. This plan stops
at rendering + the callback prop surface; Plan 04 wires those callbacks to Monaco edits.

Output: Renderer prop additions, two new editor components, and the split-pane FileEditorView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-RESEARCH.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-PATTERNS.md
@.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md

<interfaces>
<!-- From Plan 02 (already built, type-clean): -->
import { buildIdRangeMap, deckToMapData } from './deckplan/useDeckplanModel';
deckToMapData(parsedDeckplan: any, deckId: string): GridEncounterMapData | null

<!-- Existing EncounterMapRenderer props (src/components/domain/encounter/EncounterMapRenderer.tsx:42-63): -->
interface EncounterMapRendererProps {
  mapData; hull?; roomVisibility; isGM?: boolean;
  onRoomToggle?: (roomId, visible) => void;
  onTokenMove?: (id, x, y, roomId) => void;
  /* + others */
}
<!-- Existing MapPreview props (src/components/gm/MapPreview.tsx:46-74): fill?, isGM?, onRoomToggle?, onTokenMove? -->

<!-- TokenLayer drag pattern to mirror (src/components/domain/encounter/TokenLayer.tsx:90-91, 189-268): -->
const pendingDrag = useRef<{id; startX; startY} | null>(null); const DRAG_THRESHOLD = 5;
// screenToSVG → inverseRotatePoint → snapToGrid from src/utils/svgCoordinates.ts

<!-- isDeckplan helper to ADD alongside getLanguage()/isImage() in FileEditorView.tsx: -->
function isDeckplan(path: string): boolean { return path.split('/').pop() === 'deckplan.yaml'; }

<!-- New props this plan ADDS to renderer/MapPreview (callbacks only; wiring in Plan 04): -->
editable?: boolean
onPoiClick?: (poiId: string) => void
onPoiMove?: (poiId: string, x: number, y: number) => void
onEmptyCellClick?: (x: number, y: number) => void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editor-mode POI props to EncounterMapRenderer + thread through MapPreview</name>
  <files>src/components/domain/encounter/EncounterMapRenderer.tsx, src/components/gm/MapPreview.tsx</files>
  <read_first>
    - src/components/domain/encounter/EncounterMapRenderer.tsx (props interface ~42-63; renderPoi ~934-1000; isGM handling)
    - src/components/domain/encounter/TokenLayer.tsx (lines 90-91, 189-268 — pendingDrag/DRAG_THRESHOLD, screenToSVG→inverseRotatePoint→snapToGrid, ghost rendering)
    - src/components/gm/MapPreview.tsx (props ~46-74; fill wrapper ~174-198; how it forwards onRoomToggle/onTokenMove)
    - src/utils/svgCoordinates.ts (screenToSVG, inverseRotatePoint, snapToGrid, getGridCell — reuse, no changes)
    - src/types/encounterMap.ts (PoiData)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md (POI drag amber #c9a050 outline; 5px threshold; empty-cell click semantics)
  </read_first>
  <action>
    Add four optional props to EncounterMapRendererProps: `editable?: boolean`,
    `onPoiClick?: (poiId) => void`, `onPoiMove?: (poiId, x, y) => void`,
    `onEmptyCellClick?: (x, y) => void`. Implement an editor-only POI interaction layer (a
    sibling `PoiEditLayer` component is preferred per RESEARCH Open Question 2, OR extend
    renderPoi inline — implementer's choice, but keep runtime token behavior untouched):
    - When `editable`, a non-drag click on a POI calls `onPoiClick(poi.id)` (do NOT toggle
      visibility — editor mode only jumps). Cursor `pointer`.
    - Mirror TokenLayer's `pendingDrag` ref + `DRAG_THRESHOLD = 5`: on pointerdown over a POI,
      arm pendingDrag; on move past threshold enter drag; on pointerup compute grid cell via
      screenToSVG → inverseRotatePoint → snapToGrid and call `onPoiMove(poi.id, gridX, gridY)`.
      Render the drag ghost/outline in amber `#c9a050` per UI-SPEC (vs token mode's color).
    - When `editable`, a click on an empty cell (no room/POI hit) calls
      `onEmptyCellClick(gridX, gridY)` with the snapped grid coords.
    All four props are inert when `editable` is false/absent — runtime encounter rendering is
    unchanged. Thread the same four props through MapPreview's props and forward them to
    EncounterMapRenderer (same forwarding pattern it already uses for onRoomToggle/onTokenMove).
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -3 && grep -cE "onPoiClick|onPoiMove|onEmptyCellClick|editable" src/components/domain/encounter/EncounterMapRenderer.tsx src/components/gm/MapPreview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EncounterMapRendererProps` and `MapPreviewProps` both declare `editable?`, `onPoiClick?`, `onPoiMove?`, `onEmptyCellClick?`
    - MapPreview forwards all four props to EncounterMapRenderer
    - POI drag uses `DRAG_THRESHOLD = 5` and the amber `#c9a050` drag affordance
    - With `editable` unset, no behavior change: existing token/room/door interactions render identically (verify existing usages still typecheck)
    - `pnpm run typecheck` passes
  </acceptance_criteria>
  <done>Renderer + MapPreview expose an editor-mode POI callback surface gated by `editable`, runtime behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build DeckSelector + DeckplanPreviewPane</name>
  <files>src/components/gm/views/deckplan/DeckSelector.tsx, src/components/gm/views/deckplan/DeckplanPreviewPane.tsx</files>
  <read_first>
    - src/components/gm/MapPreview.tsx (fill wrapper, prop surface incl. the new props from Task 1)
    - src/components/gm/views/deckplan/useDeckplanModel.ts (deckToMapData, parse helpers from Plan 02)
    - src/components/gm/views/FileEditorView.tsx (lines 1-44 imports + MONOSPACE_FONT + SAVE-button styling ~230-255 for color/typography conventions)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md (deck selector spec: 32px tab strip, #0d1616 bg, active amber #c9a050 + 2px bottom border, inactive #4a7070 hover #7ab8b8, 11px/600/uppercase/1px; selection keyed by deck id with default/decks[0] fallback; preview header "MAP PREVIEW"; empty/parse-error copy)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-PATTERNS.md (DeckSelector + DeckplanPreviewPane sections, color/typography tokens)
  </read_first>
  <action>
    DeckSelector.tsx: a horizontal tab strip over a decks list (`{id, name?}[]`), props
    `decks`, `selectedDeckId`, `onSelect`. Active tab amber `#c9a050` text + 2px amber
    bottom-border; inactive `#4a7070` text hover `#7ab8b8`; label = deck.name or id-uppercased
    fallback; 11px/600/uppercase/letter-spacing 1px monospace; strip height 32px, background
    `#0d1616`, bottom border `#1e3333`. Use Ant `Tabs type="card"` styled to match, or a
    custom button row (implementer's choice per UI-SPEC discretion).
    DeckplanPreviewPane.tsx (analog: MapPreview): props include the deckplan YAML text (or
    parsed deckplan), `selectedDeckId`, and pass-through callbacks `onRoomClick`/`onPoiClick`/
    `onPoiMove`/`onEmptyCellClick` (forwarded into MapPreview as onRoomToggle/onPoiClick/etc).
    Internally: parse the buffer text (debounced ~200ms via useDeckplanModel helpers), keep the
    LAST successfully-parsed deckplan in state so a mid-edit parse error does NOT blank the
    preview (Pitfall 2). Derive the selected deck's GridEncounterMapData via `deckToMapData`.
    Render a "MAP PREVIEW" header label (Label role, #4a7070) + the DeckSelector + the map.
    Mount `<MapPreview fill isGM editable mapData={...} roomVisibility={{}} ...>`. Selection
    persistence keyed by deck id; if the selected id vanishes after re-parse, fall back to the
    `default:true` deck then `decks[0]` (Pitfall 1). Empty-state: only show
    `NO PREVIEW AVAILABLE` + body copy if the VERY FIRST parse of a freshly-opened file fails;
    otherwise keep the stale-but-rendered preview.
  </action>
  <verify>
    <automated>pnpm run typecheck 2>&1 | tail -3 && test -f src/components/gm/views/deckplan/DeckSelector.tsx && test -f src/components/gm/views/deckplan/DeckplanPreviewPane.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `DeckSelector.tsx` renders one tab per deck, marks the selected deck with amber `#c9a050` + 2px bottom border, and calls `onSelect` with a deck id
    - Deck label falls back to id-uppercased when `name` is absent
    - `DeckplanPreviewPane.tsx` mounts `MapPreview` with `editable` and `fill` and derives map data via `deckToMapData`
    - On parse error after a good parse, the pane keeps the last-good preview (does not blank)
    - Selection is keyed by deck id with default/decks[0] fallback when the id disappears
    - `pnpm run typecheck` passes
  </acceptance_criteria>
  <done>Deck selector + preview pane components exist, type-clean, parse-error-resilient.</done>
</task>

<task type="auto">
  <name>Task 3: Mount preview below Monaco in FileEditorView with 60/40 resizable split</name>
  <files>src/components/gm/views/FileEditorView.tsx, src/components/gm/views/FileEditorView.css</files>
  <read_first>
    - src/components/gm/views/FileEditorView.tsx (getLanguage/isImage ~19-32; onEditorMount ~105-110; renderEditorContent monaco block ~196-218; render tree/editor-area ~220-277; LS_KEY persistence ~17)
    - src/components/gm/views/FileEditorView.css (editor-area ~19-25, monaco ~50-53, error-banner ~55-63, empty-state ~65-73)
    - src/components/gm/views/deckplan/DeckplanPreviewPane.tsx (from Task 2)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-UI-SPEC.md (Preview pane placement: below Monaco, 60/40 default, resizable 4px visual / 44px hit target, ratio persisted in localStorage; pane bg #080e0e, 1px #1e3333 top border)
    - .planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-PATTERNS.md (FileEditorView.tsx + .css sections)
  </read_first>
  <action>
    Add `function isDeckplan(path)` alongside getLanguage/isImage (returns true when the
    basename is exactly `deckplan.yaml`, per D-07). In the Monaco branch of
    renderEditorContent, when `isDeckplan(selectedPath)` AND the file language is yaml, render
    the existing Monaco editor in a top sub-pane and mount `<DeckplanPreviewPane>` in a bottom
    sub-pane, passing the current `content` (buffer text) and a `selectedDeckId` state. The
    preview must read from `content` (in-memory buffer), never a re-fetch (RESEARCH anti-pattern).
    Non-deckplan files keep the existing single-pane Monaco render unchanged.
    For Task 3 the four preview callbacks (onPoiClick/onPoiMove/onEmptyCellClick/onRoomClick)
    may be passed as no-op or console placeholders — Plan 04 wires them to Monaco. (Leave a
    clearly-named TODO referencing Plan 04 so the wiring target is unambiguous.)
    In FileEditorView.css: add a 60/40 vertical split — Monaco sub-pane `flex: 0 0 60%` (or a
    ratio read from localStorage, same LS_KEY-style persistence as last-opened-path), preview
    pane `flex: 0 0 40%` with `background:#080e0e` and `border-top:1px solid #1e3333`. Add a
    resize handle rule (4px visual, 44px hit target per Spacing exceptions) that updates +
    persists the split ratio. Add a deck-tab bar rule (32px, `#0d1616`, bottom border
    `#1e3333`) if not styled inline in DeckSelector. Reuse `.gm-file-editor-view__empty-state`
    styling for the NO PREVIEW AVAILABLE state.
  </action>
  <verify>
    <automated>pnpm run typecheck && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `isDeckplan()` exists and returns true only for a `deckplan.yaml` basename
    - When a `deckplan.yaml` is the open file, FileEditorView renders Monaco (top) + DeckplanPreviewPane (bottom); when any other file is open, the preview pane is NOT rendered
    - The preview consumes the in-memory `content` state (no extra GET to re-fetch the file)
    - CSS defines a 60/40 default split with a resize handle (4px visual / 44px hit) and persists the ratio in localStorage
    - `pnpm run typecheck && pnpm run build` both succeed
  </acceptance_criteria>
  <done>Opening a deckplan.yaml shows a live, deck-switchable preview below Monaco; other files unchanged; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Monaco buffer → preview render | Hand-edited YAML parsed client-side for rendering |
| POI fields → JSX render | POI name/description/icon rendered in hover popovers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Information Disclosure | XSS via POI label/description in popover | accept | Pre-existing render path; React JSX auto-escapes; this plan adds no dangerouslySetInnerHTML |
| T-29-07 | Denial of Service | parse error blanks/crashes preview | mitigate | DeckplanPreviewPane keeps last-good parse + try/catch (Pitfall 2); debounced re-parse |
| T-29-SC | Tampering | npm installs | mitigate | No new packages — antd/yaml/monaco already installed; N/A |
</threat_model>

<verification>
- `pnpm run typecheck && pnpm run build` succeed
- Manual smoke (wave merge, EDIT-02/03): open `data/ships/patrol_gunboat/deckplan.yaml` in the GM Console file editor → preview appears below Monaco; deck selector switches the rendered deck; editing YAML updates the preview; opening a non-deckplan file shows no preview pane
</verification>

<success_criteria>
- EDIT-02: live preview below Monaco for deckplan.yaml files
- EDIT-03: deck selector switches the rendered deck
- Preview is buffer-driven and parse-error-resilient; non-deckplan files unaffected
</success_criteria>

<output>
Create `.planning/phases/29-interactive-deckplan-map-editor-with-live-preview-yaml-sync-/29-03-SUMMARY.md` when done
</output>
